services:
  civweave:
    build: .
    ports:
      - "8787:8787"
    environment:
      HOST: 0.0.0.0
      PORT: 8787
      DATA_DIR: /app/data
    volumes:
      - civweave-data:/app/data
volumes:
  civweave-data:
