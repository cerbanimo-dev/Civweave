services:
  civweave-host-node:
    build: .
    ports:
      - "8787:8787"
    environment:
      HUB_NAME: Local Civweave Host Node
      DATA_DIR: /app/data
      # HUB_TOKEN: replace-with-a-private-token
    volumes:
      - ./data:/app/data
    restart: unless-stopped
