{
  "name": "commonweave-clean-baseline",
  "version": "0.1.0",
  "private": true,
  "description": "A plain HTML, CSS, and JavaScript Commonweave baseline with five connected systems, one AI vault, and a local-first mesh.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "node scripts/stage-transformers-assets.mjs --soft && node scripts/stage-qrcode.mjs --soft",
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node scripts/verify-baseline.mjs",
    "setup:local": "node scripts/stage-transformers-assets.mjs && node scripts/stage-qrcode.mjs && node scripts/ensure-minilm-model.mjs",
    "model:pull": "node scripts/ensure-minilm-model.mjs",
    "model:check": "node scripts/ensure-minilm-model.mjs --check"
  },
  "dependencies": {
    "@huggingface/transformers": "3.8.1",
    "qrcode": "1.5.4",
    "ws": "8.18.3"
  }
}
