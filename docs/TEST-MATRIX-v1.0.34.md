# Test matrix v1.0.34

- Parse the complete five-part Working Campus source after adding the Weaveling submit handler.
- Verify the Working Campus includes and styles its composer controls.
- Simulate credential save, session loss, and restoration from device-local storage.
- Verify the Merlin compatibility API delegates to `CommonweaveAssistantV141` and preserves provider/model metadata.
- Verify the recovery layer suppresses the recursive Cerbanimo validator binder before the native validator loads.
- Verify the install boundary and additive service worker deliver v160 and all changed core surfaces.
- Re-run the existing Commonweave startup and repository regression suites.
