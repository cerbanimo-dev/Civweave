# PR summary v1.0.34

This change restores the Commonweave guide loop across the Working Campus and native cabinets, makes explicitly saved credentials durable on the current device, and removes a recursive Cerbanimo validation render cycle.
