# Validation notes

The v248 repair is intended to be merged only after the exact PR head passes the dedicated mobile-chat verifier and the existing shared-chat, Working Campus, service-worker, Cloudflare, package-completion, version, and realm checks.

No physical Android device test is claimed by repository CI. The regression contract is derived from the supplied Android screenshot and the runtime ownership/caching code paths.
