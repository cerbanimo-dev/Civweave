import assert from 'node:assert/strict';
import vm from 'node:vm';
import {readFile} from 'node:fs/promises';
const read=path=>readFile(new URL(`../${path}`,import.meta.url),'utf8');
const [worker,runtime,bootstrap,bridge,policy,assistant,spine,manager,pulse,registry]=await Promise.all(['public/app/local-ai/worker-v266.js','public/app/local-ai/runtime-v266.js','public/app/local-ai/bootstrap-v266.js','public/app/local-ai/runtime-bridge-v266.js','public/app/local-ai/small-model-policy-v283.js','public/app/assistant-runtime-v141.js','public/app/fast-interactive-runtime-v192.js','public/app/local-ai/download-manager-v267.js','public/app/local-ai/test-pulse-v269.js','public/app/local-ai/model-registry-v266.js'].map(read));
for(const source of [worker,runtime,bootstrap,bridge,policy,assistant,spine,manager,pulse,registry])new Function(source);
const checks=[];const check=(name,value)=>{assert.ok(value,name);checks.push(name)};
check('Transformers stays local-only',worker.includes('hf.env.allowLocalModels=true')&&worker.includes('hf.env.allowRemoteModels=false'));
check('cache adapter uses artifact-specific pinned revisions',worker.includes('revisionFor=(spec,path)=>artifactFor(spec,path)?.revision||spec.revision')&&worker.includes("status:404,statusText:'Downloaded model cache miss'"));
check('worker uses causal LM instead of generic pipeline',worker.includes('AutoTokenizer.from_pretrained')&&worker.includes('AutoModelForCausalLM.from_pretrained')&&!worker.includes('hf.pipeline('));
check('worker manually controls thinking template',worker.includes('apply_chat_template')&&worker.includes('enable_thinking:Boolean(thinking)'));
check('worker streams and measures true token timing',worker.includes('new hfRuntime.TextStreamer')&&worker.includes('token_callback_function')&&worker.includes('ttftMs')&&worker.includes('tokensPerSecond'));
check('worker compacts context and reports clipping',worker.includes('function compactContext(')&&worker.includes('promptTokenBudget')&&worker.includes('nearTokenLimit')&&worker.includes('endedWithEOS'));
check('worker probes WebGPU and retains WASM',worker.includes('navigator.gpu.requestAdapter')&&worker.includes("backend:'wasm'"));
check('runtime is v283 and keeps repair/fallback',runtime.includes("VERSION='1.0.85-local-ai-runtime-v283-small-model-fast-path'")&&runtime.includes('manager.repair')&&runtime.includes('backend-fallback-download')&&runtime.includes('LOCAL_BACKEND_CAPABILITY_UNAVAILABLE'));
check('runtime passes contexts, artifacts, thinking and prompt budgets',runtime.includes('artifacts:spec.artifacts')&&runtime.includes('contextWindowTokens:spec.contextWindowTokens')&&runtime.includes('thinking:Boolean(requestOptions.thinking)')&&runtime.includes('promptTokenBudget:requestOptions.promptTokenBudget'));
check('registry keeps hidden q8 compatibility model and contexts',registry.includes("id:'qwen3-0.6b-q8-wasm'")&&registry.includes('hidden:true')&&registry.includes('contextWindowTokens:40_960'));
check('registry has Smol metadata-only revision pin',registry.includes('SMOL_TEMPLATE_REVISION')&&registry.includes('artifactRevision'));
check('bootstrap combines v282 compatibility identity with v283 fast path',bootstrap.includes("VERSION='1.0.83-local-ai-bootstrap-v282-inference-health'")&&bootstrap.includes("REVISION='1.0.85-local-ai-bootstrap-v283-small-model-fast-path'")&&bootstrap.includes('smallModelFastPath:true')&&bootstrap.includes('cacheResolvedInference:true'));
check('bridge registers v283 middleware and thinking profiles',bridge.includes("MIDDLEWARE_ID='downloaded-local-v283'")&&bridge.includes('function thinkingFor(request,spec)')&&bridge.includes('continuation-requested'));
check('bridge reports usage, TTFT and continuation state',bridge.includes('inputTokens:Number(m.promptTokens||0)')&&bridge.includes('ttftMs:m.ttftMs')&&bridge.includes('completionValidation'));
check('pulse runs direct 32-token non-thinking health test',pulse.includes('maxNewTokens:32')&&pulse.includes('thinking:false')&&pulse.includes('integrityReady'));
check('manager rejects poisoned metadata',manager.includes('validateArtifactResponse')&&manager.includes("reason:'invalid-json'")&&manager.includes("reason:'html'"));
check('assistant dynamically reads shared runtime',assistant.includes('const runtime=()=>globalThis.CivweaveModelRuntime||null')&&assistant.includes('await rt.generate'));
check('runtime spine handles middleware before base',spine.includes('if(out?.handled)')&&spine.includes("handledBy='base-runtime'"));
let localCalls=0,baseCalls=0,captured=null,thinkingSeen=null;const partial=[];
const spec={id:'qwen3-1.7b-q4f16',label:'Qwen 3 1.7B',estimatedBytes:1_470_000_000,healthTimeoutMs:600000,contextWindowTokens:40960,workingContextTokens:4096,generation:{thinkingSupported:true},capabilities:{interactive:true,structuredOutput:true,agenticReasoning:true,code:true,tools:false,externalResearch:false,vision:false}};
const baseRuntime={version:'base-test-runtime',generate:async()=>{baseCalls+=1;return{status:'success',actual:{provider:'base'},outputText:'base'}}};
const sandbox={console,Date,Promise,Error,Object,Boolean,Number,String,Math,performance:{now:()=>10},CustomEvent:class{constructor(type,init={}){this.type=type;this.detail=init.detail}},dispatchEvent:()=>true,addEventListener:()=>{},CivweaveModelRuntime:baseRuntime,CivweaveLocalModelDownloadV266:{selection:()=>({active:true,id:spec.id})},CivweaveLocalModelRuntimeV266:{activeSpec:()=>spec,generate:async request=>{localCalls+=1;thinkingSeen=request.thinking;request.onToken?.({text:'Local ',index:0});request.onToken?.({text:'answer',index:1});return{text:'{"answer":"Local answer"}',json:{answer:'Local answer'},elapsedMs:12,streamed:true,backend:'wasm',fallbackUsed:true,executionId:'qwen3-0.6b-q8-wasm',executionLabel:'Qwen CPU',contextWindowTokens:40960,workingContextTokens:2048,context:{inputTokens:42,originalTokens:42,promptTokenBudget:4096,compacted:false,droppedMessages:0},completion:{generatedTokens:8,requestedMaxTokens:384,nearTokenLimit:false,completionReason:'stop',endedWithEOS:true},metrics:{promptTokens:42,generatedTokens:8,ttftMs:120,generationMs:800,tokensPerSecond:10}}}},CivweaveAICapabilityBrokerV268:{supportsLocalRequest:()=>({ok:true,reason:'qualified',requirements:{profile:'interactive'}})},CivweaveFastInteractiveV192:{register:(id,hooks,priority)=>{captured={id,hooks,priority};return()=>{}},proxy:()=>baseRuntime,base:()=>baseRuntime}};sandbox.globalThis=sandbox;
vm.createContext(sandbox);vm.runInContext(policy,sandbox,{filename:'small-model-policy-v283.js'});vm.runInContext(bridge,sandbox,{filename:'runtime-bridge-v266.js'});assert.equal(captured?.id,'downloaded-local-v283');
const request={purpose:'guide',executionProfile:'interactive',responseFormat:'json',config:{stream:true},onEvent:e=>{if(e.phase==='partial')partial.push(e)},messages:[{role:'user',content:'Analyze this.'}]};
const handled=await captured.hooks.handle(request);assert.equal(handled.handled,true);assert.equal(thinkingSeen,false);assert.equal(handled.result.usage.inputTokens,42);assert.equal(handled.result.timing.ttftMs,120);assert.equal(handled.result.completionValidation.valid,true);assert.equal(partial.length,2);assert.equal(baseCalls,0);checks.push('interactive local route disables thinking and streams measured metrics');
sandbox.CivweaveLocalModelRuntimeV266.generate=async request=>{thinkingSeen=request.thinking;return{text:'agentic',json:null,elapsedMs:10,streamed:false,backend:'webgpu',context:{inputTokens:10,originalTokens:10,compacted:false},completion:{generatedTokens:5,nearTokenLimit:false,completionReason:'stop',endedWithEOS:true},metrics:{promptTokens:10,generatedTokens:5,generationMs:100}}};await captured.hooks.handle({...request,responseFormat:'text',executionProfile:'agentic',config:{stream:false}});assert.equal(thinkingSeen,true);checks.push('agentic local route enables thinking');
sandbox.CivweaveLocalModelRuntimeV266.generate=async()=>{throw Object.assign(new Error('needs GPU'),{code:'LOCAL_BACKEND_CAPABILITY_UNAVAILABLE'})};assert.equal(await captured.hooks.handle({...request,executionProfile:'agentic'}),null);checks.push('unsafe agentic fallback escalates through base route');
sandbox.CivweaveLocalModelRuntimeV266.generate=async()=>{throw new Error('simulated local inference failure')};await assert.rejects(()=>captured.hooks.handle(request),/simulated local inference failure/);assert.equal(baseCalls,0);checks.push('ordinary local inference failure remains visible');
console.log(JSON.stringify({ok:true,revision:'local-model-cache-loader-v283-fast-path',checks:checks.length,features:{artifactRevisionCache:true,canonicalCausalLM:true,tokenizerAwareContext:true,continuationValidation:true,ttft:true,interactiveThinkingOff:true,agenticThinkingOn:true,wasmCompatibilityFallback:true,capabilitySafeEscalation:true,noSilentBaseFallback:true}},null,2));
