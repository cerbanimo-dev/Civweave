import fs from 'node:fs';
import vm from 'node:vm';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';

const floorPath='public/app/guide-generation-floor-v1.js';
const loaderPath='public/app/shared-guide-surface-v236.js';
const streamPath='public/app/guide-stream-thinking-v249.js';
const localRuntimePath='public/app/local-chat-runtime-v295.js';
for(const path of [floorPath,loaderPath,streamPath,localRuntimePath])execFileSync(process.execPath,['--check',path],{stdio:'inherit'});
const floorSource=fs.readFileSync(floorPath,'utf8'),loaderSource=fs.readFileSync(loaderPath,'utf8'),streamSource=fs.readFileSync(streamPath,'utf8'),localRuntimeSource=fs.readFileSync(localRuntimePath,'utf8');
const registrations=[],listeners=new Map();
const context={globalThis:null,CustomEvent:class{constructor(type,init={}){this.type=type;this.detail=init.detail}},dispatchEvent(){return true},addEventListener(type,handler){listeners.set(type,handler)},queueMicrotask(handler){handler()},CivweaveFastInteractiveV192:{register(id,hooks,priority){registrations.push({id,hooks,priority});return()=>{}}},console};
context.globalThis=context;vm.createContext(context);vm.runInContext(floorSource,context,{filename:floorPath});
const api=context.CivweaveGuideGenerationFloorV1;
assert.ok(api,'guide generation floor API missing');
assert.equal(api.floorTokens,900);assert.equal(api.planningFloorTokens,1800);assert.equal(api.weavelingPlanFloorTokens,2400);
assert.equal(api.localExecutionContractV1,true);assert.equal(api.localGuideStreaming,true);assert.equal(api.weavelingPlannerRecovery,true);assert.equal(api.mossLearningGoalPlanner,true);
assert.equal(api.learningGoalText('I want to learn Tarot.'),true);assert.equal(api.learningGoalText('What does the Fool card mean?'),false);
const registration=registrations.find(row=>row.id==='guide-generation-floor-v1');assert.ok(registration);assert.equal(registration.priority,-1000);
const before=request=>{const out=registration.hooks.before(request);return out?.request||out};
let request=before({purpose:'civweave-guide-response-v141',config:{maxTokens:96,responseLengthClass:'short'}});assert.equal(request.config.maxTokens,900);
request=before({purpose:'civweave-guide-response-v141',config:{provider:'downloaded-local',route:'downloaded-local',maxTokens:900,stream:false},capabilityRequirements:{planning:true,structuredOutput:true},task:{text:'Make me a plan to create a community garden',requirements:{planning:true,structuredOutput:true}},messages:[{role:'system',content:'You are Weaveling.'},{role:'user',content:'Make me a plan to create a community garden'}]});
assert.equal(request.config.maxTokens,1800);assert.equal(request.config.stream,true);assert.equal(request.__civweaveGuidePlanningContract,true);assert.equal(request.__civweaveGuideLocalExecutionContract,true);assert.match(request.messages[0].content,/produce the plan now/i);assert.match(request.messages[0].content,/do not say that the local AI model is incapable/i);
request=before({purpose:'civweave-weaveling-intention-json-v190',executionProfile:'interactive',config:{provider:'downloaded-local',route:'downloaded-local',model:'gemma4-e2b-it-q4f16',stream:false},context:{guide:{system:'civweave'},userMessage:'Make me a plan to create a community garden'},messages:[{role:'system',content:'You are Weaveling.'},{role:'user',content:'Create the reviewable weave.'}],schema:{type:'object'}});
assert.equal(api.weavelingStructuredPlanRequest(request),true);assert.equal(request.config.maxTokens,2400,'Weaveling structured local planning must get the larger budget');assert.equal(request.config.stream,true,'Weaveling structured local planning must stream');assert.equal(request.capabilityRequirements.planning,true);assert.equal(request.capabilityRequirements.structuredOutput,true);assert.equal(request.__civweaveGuidePlatformPlanning,'civweave-reviewable-weave-v1');assert.match(request.messages[0].content,/never replace the requested plan with commentary about model capability/i);
request=before({purpose:'civweave-guide-response-v141',config:{provider:'downloaded-local',maxTokens:900,stream:false},context:{guide:{system:'living-school'}},task:{text:'I want to learn Tarot.'},messages:[{role:'system',content:'You are Moss.'},{role:'user',content:'I want to learn Tarot.'}]});
assert.equal(request.artifactKind,'learning-path');assert.equal(request.__civweaveGuidePlatformPlanning,'living-school-learning-journey-v1');assert.equal(request.config.maxTokens,1800);assert.equal(request.config.stream,true);
let delegated=0,platformRuns=0;
context.CivweaveAssistantV141={respond:async options=>{delegated+=1;if(options.systemId==='civweave')throw new Error('simulated structured local failure');return{provider:'delegate'}}};
context.CivweaveUnifiedChatSystemV1={activeTheme:()=> 'living-school',curriculumIntent:()=>false,runLivingSchoolCurriculum:async options=>{platformRuns+=1;return{provider:'living-school-learning-engine',request:options}}};
context.CivweaveIntentionPlanner={buildPlan:({text})=>({id:'plan-1',title:'Community garden',wish:text,state:'review',assumptions:[],paths:[]}),persist:plan=>({id:'item-1',state:'review',plan}),format:()=> 'Recovered reviewable community-garden weave.'};
assert.equal(api.installPlatformGuideGuards(),true);
let result=await context.CivweaveAssistantV141.respond({text:'I want to learn Tarot.',systemId:'living-school',history:[]});assert.equal(result.provider,'living-school-learning-engine');assert.equal(platformRuns,1);assert.equal(delegated,0);
result=await context.CivweaveAssistantV141.respond({text:'Make me a plan to create a community garden',systemId:'civweave',history:[]});assert.equal(result.provider,'civweave-planner-recovery');assert.match(result.response.answer,/Recovered reviewable/);assert.equal(result.response.requiresConsent,true);assert.equal(delegated,1);
await context.CivweaveAssistantV141.respond({text:'What does the Fool card mean?',systemId:'living-school',history:[]});assert.equal(delegated,2);
request=before({purpose:'civweave-guide-response-v141',config:{provider:'downloaded-local',maxTokens:900,stream:false},messages:[{role:'user',content:'Hello'}]});assert.equal(request.config.stream,true);assert.match(request.messages[0].content,/downloaded-local execution contract/i);
request=before({purpose:'civweave-guide-response-v141',config:{provider:'gemini',maxTokens:900,stream:false},messages:[{role:'user',content:'Hello'}]});assert.equal(request.config.stream,false);
const ordinary={purpose:'background-indexing',config:{maxTokens:96}};assert.equal(before(ordinary),ordinary);
assert.ok(streamSource.includes('.cw350-bubble,.cw237-bubble'),'stream renderer does not target canonical v350 bubbles');
assert.ok(streamSource.includes('visibleWeavelingPlan'),'structured Weaveling planning has no visible stream projection');
assert.ok(streamSource.includes('function patchAssistant()'),'local-provider assistant bypass is not connected to the token renderer');
assert.ok(streamSource.includes('localAuthorityTokenBridge:true'),'local-provider token bridge diagnostic is missing');
assert.ok(streamSource.includes("dispatchEvent(new CustomEvent('civweave:model-event'"),'local assistant tokens are not published to the canonical model-event stream');
assert.ok(localRuntimeSource.includes("maxNewTokens=1024"),'local chat runtime does not expose a long-output default');
assert.ok(localRuntimeSource.includes('maxOutputTokens:4096'),'local chat runtime does not expose its long-output ceiling');
assert.ok(!localRuntimeSource.includes('tps*30,48,128'),'retired 30-second/128-token truncation budget returned');
assert.ok(!/maxNewTokens\s*=.*128/.test(localRuntimeSource),'local chat runtime still hard-caps output at 128 tokens');
assert.ok(loaderSource.includes('/app/guide-generation-floor-v1.js'),'shared guide loader is not delivering the generation floor');
assert.ok(loaderSource.includes('/app/local-chat-runtime-v295.js?v=1.0.118-streaming-long-output'),'shared guide loader is not cache-busting the long-output local runtime');
assert.ok(loaderSource.includes('/app/guide-stream-thinking-v249.js?v=1.0.121-local-authority'),'shared guide loader is not delivering the local-authority token bridge');
console.log('PASS local guide long-output authority, Weaveling/Moss platform planning, plan recovery, and visible v350 token streaming are active.');