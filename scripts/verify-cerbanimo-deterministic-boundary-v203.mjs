import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
import path from 'node:path';
import vm from 'node:vm';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),read=relative=>readFile(path.join(root,relative),'utf8');
const [broker,boundary,spine,registry,bridge,bootstrap]=await Promise.all([read('public/app/ai-capability-broker-v268.js'),read('public/app/cerbanimo-deterministic-boundary-v203.js'),read('public/app/fast-interactive-runtime-v192.js'),read('public/app/local-ai/model-registry-v266.js'),read('public/app/local-ai/runtime-bridge-v266.js'),read('public/app/local-ai/bootstrap-v266.js')]);
assert(broker.includes("VERSION='1.0.67-ai-capability-broker-v271-semantics'"));assert(boundary.includes("VERSION='1.0.8-cerbanimo-authority-boundary-v268'"));assert(boundary.includes("BROKER_SRC='/app/ai-capability-broker-v268.js?v=1.0.67-v271'"));assert(!boundary.includes('DETERMINISTIC_PROVIDER_BOUNDARY'));assert(boundary.includes("consequentialActions:'deterministic-contracts'"));assert(spine.includes('__civweaveRuntimeSpineV271:true')&&spine.includes('function register('));assert(registry.includes('agenticReasoning:true')&&registry.includes('externalResearch:false'));assert(registry.includes('contextWindowTokens'));assert(bridge.includes("MIDDLEWARE_ID='downloaded-local-v274'")&&bridge.includes('runtimeSpine.register(MIDDLEWARE_ID,middleware(),100)'));assert(bridge.includes('function thinkingFor(request)'));assert(bootstrap.indexOf('ai-capability-broker-v268.js')<bootstrap.indexOf('fast-interactive-runtime-v192.js')&&bootstrap.indexOf('fast-interactive-runtime-v192.js')<bootstrap.indexOf('runtime-bridge-v266.js'));
class MemoryStorage{constructor(seed={}){this.rows=new Map(Object.entries(seed))}getItem(key){return this.rows.has(key)?this.rows.get(key):null}setItem(key,value){this.rows.set(key,String(value))}removeItem(key){this.rows.delete(key)}}
const profilesKey='civweave-model-profiles-v1',listeners=new Map();class FakeCustomEvent{constructor(type,init={}){this.type=type;this.detail=init.detail}}
const localStorage=new MemoryStorage({[profilesKey]:JSON.stringify({interactive:{provider:'minilm',route:'minilm',model:'Xenova/all-MiniLM-L6-v2'}})});let baseCalls=0;const originalGenerate=async request=>{baseCalls+=1;return{status:'success',actual:{provider:'base-runtime'},request}},originalRespond=async()=>({provider:'gemini'});
const sandbox={console,Date,Promise,Map,Set,Object,JSON,Math,RegExp,String,Number,Boolean,Array,Error,performance:{now:()=>10},localStorage,CustomEvent:FakeCustomEvent,dispatchEvent(event){sandbox.lastEvent=event;return true},addEventListener(name,handler){listeners.set(name,handler)},CivweaveModelRuntime:Object.freeze({version:'base',generate:originalGenerate}),CivweaveAssistantV141:{respond:originalRespond},globalThis:null};sandbox.globalThis=sandbox;vm.createContext(sandbox);
vm.runInContext(broker,sandbox);vm.runInContext(boundary,sandbox);assert.equal(sandbox.CivweaveAICapabilityBrokerV268.selectedProvider(),'semantic-local');assert.equal(sandbox.CivweaveModelRuntime.generate,originalGenerate);assert.equal(sandbox.CivweaveAssistantV141.respond,originalRespond);assert.equal(sandbox.CivweaveCerbanimoDeterministicBoundaryV203.legacyProviderWall,false);assert.equal(sandbox.CivweaveCerbanimoDeterministicBoundaryV203.authority.consequentialActions,'deterministic-contracts');
vm.runInContext(spine,sandbox);assert.equal(sandbox.CivweaveModelRuntime.__civweaveRuntimeSpineV271,true);vm.runInContext(registry,sandbox);let selectedId='qwen3-1.7b-q4f16',localCalls=0,lastThinking=null;sandbox.CivweaveLocalModelDownloadV266={selection:()=>({active:true,id:selectedId})};sandbox.CivweaveLocalModelRuntimeV266={activeSpec:()=>sandbox.CivweaveLocalModelRegistryV266.byId(selectedId),async generate(request){localCalls+=1;lastThinking=request.thinking;request.onToken?.({text:'ok',index:0});return{text:'{"ok":true}',json:{ok:true},elapsedMs:12,streamed:Boolean(request.stream),metrics:{promptTokens:12,generatedTokens:4,ttftMs:50,tokensPerSecond:8}}}};vm.runInContext(bridge,sandbox);assert.equal(sandbox.CivweaveLocalModelBridgeV266.registered,true);
const localAgent=await sandbox.CivweaveModelRuntime.generateAgentic({purpose:'cerbanimo-plan-code',code:true,messages:[{role:'user',content:'Plan a bounded refactor'}],responseFormat:'json'});assert.equal(localAgent.actual.provider,'downloaded-local');assert.equal(lastThinking,true);assert.equal(localCalls,1);assert.equal(baseCalls,0);
const legacyThreat=await sandbox.CivweaveModelRuntime.generateAgentic({purpose:'anarchadia-threat-model',background:true,requiresTools:true,config:{service:'anarchadia'},messages:[{role:'user',content:'Analyze this local charter diff for abuse paths.'}],responseFormat:'text'});assert.equal(legacyThreat.actual.provider,'downloaded-local');assert.equal(localCalls,2);assert.equal(baseCalls,0);assert.equal(legacyThreat.runtimeSpine.trace.some(row=>row.id==='capability-normalizer'&&row.changed),true);
const toolAgent=await sandbox.CivweaveModelRuntime.generateAgentic({purpose:'living-school-live-research',requiresTools:true,externalResearch:true,messages:[{role:'user',content:'Search the current web'}]});assert.equal(toolAgent.actual.provider,'base-runtime');assert.equal(baseCalls,1);selectedId='qwen3-0.6b-q4f16';const smallAgent=await sandbox.CivweaveModelRuntime.generateAgentic({purpose:'complex-agentic-plan',messages:[{role:'user',content:'Do a complex multi-step project analysis'}]});assert.equal(smallAgent.actual.provider,'base-runtime');assert.equal(baseCalls,2);const smallInteractive=await sandbox.CivweaveModelRuntime.generateInteractive({purpose:'civweave-guide-response-v141',messages:[{role:'user',content:'Help me understand this JavaScript code'}]});assert.equal(smallInteractive.actual.provider,'downloaded-local');assert.equal(lastThinking,false);assert.equal(localCalls,3);const decision=sandbox.CivweaveAICapabilityBrokerV268.decide({executionProfile:'agentic',requiresTools:true});assert.equal(decision.route,'base-runtime');assert.match(decision.reason,/tools/i);
console.log(JSON.stringify({ok:true,revision:'ai-runtime-spine-v274-local-health',localitySeparatedFromDeterminism:true,authorityBoundaryPreserved:true,singleRuntimeSpine:true,localAgenticReasoning:true,interactiveThinkingOff:true,agenticThinkingOn:true,toolTasksEscalate:true,smallModelAgenticGate:true,interactiveLocalPreserved:true,localCalls,baseCalls},null,2));
