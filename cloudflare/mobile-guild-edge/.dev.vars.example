# Required during Deploy to Cloudflare.
# Leave the template value empty so Cloudflare shows an unmistakably empty secret field.
# Paste the exact one-time pairing code shown by Civweave after local Guild creation.
CIVWEAVE_GUILD_CLAIM_TOKEN=
