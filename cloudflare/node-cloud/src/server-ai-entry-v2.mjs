import baseWorker from './server-ai-entry-v1.mjs';
import{CivweaveCloudNode}from'./cloud-node-membership-v1.mjs';
import{CivweaveCapacityAccount}from'./capacity-membership-resident-v1.mjs';
import{nodeIdFromHostname,normalizeNodeId}from'./index.mjs';
export{CivweaveCloudNode,CivweaveCapacityAccount};
const enc=new TextEncoder();
const clean=(value,max=4000)=>String(value??'').trim().slice(0,max);
const cors={'access-control-allow-origin':'*','access-control-allow-methods':'GET, POST, OPTIONS','access-control-allow-headers':'content-type, x-civweave-node-id','access-control-max-age':'86400'};
const json=(value,status=200)=>Response.json(value,{status,headers:{'cache-control':'no-store',...cors}});
async function hashCredential(value){const secret=clean(value,400);if(!/^[A-Za-z0-9_-]{40,200}$/.test(secret))throw Object.assign(new TypeError('A valid device login credential is required.'),{status:400});const digest=await crypto.subtle.digest('SHA-256',enc.encode(`civweave.host-login-credential.v1\n${secret}`));return[...new Uint8Array(digest)].map(byte=>byte.toString(16).padStart(2,'0')).join('');}
function residentId(value){const id=clean(value,180);if(!/^[A-Za-z0-9:_-]{12,180}$/.test(id))throw Object.assign(new TypeError('A valid device resident id is required.'),{status:400});return id;}
function resolveNodeId(request,env){const url=new URL(request.url);return normalizeNodeId(request.headers.get('x-civweave-node-id')||url.searchParams.get('nodeId')||nodeIdFromHostname(url.hostname,env.NODE_DOMAIN||'nodes.commonweave.earth'));}
function nodeOrigin(nodeId,env){return `https://${nodeId}.${clean(env.NODE_DOMAIN,255)||'nodes.commonweave.earth'}`;}
async function capacityPost(env,pathname,body){const stub=env.CAPACITY?.get(env.CAPACITY.idFromName('civweave-account'));if(!stub)throw Object.assign(new Error('Capacity binding is unavailable.'),{status:503});const response=await stub.fetch(`https://capacity.internal${pathname}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const payload=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(payload.error||`Capacity returned HTTP ${response.status}.`),{status:response.status});return payload;}
async function signNode(env,nodeId,rawText){const stub=env.NODES?.get(env.NODES.idFromName(nodeId));if(!stub)throw Object.assign(new Error('Node binding is unavailable.'),{status:503});const response=await stub.fetch('https://node.internal/internal/sign-request',{method:'POST',headers:{'content-type':'application/json','x-civweave-node-id':nodeId},body:rawText});const payload=await response.json().catch(()=>({}));if(!response.ok||!payload.signatureHeader)throw Object.assign(new Error(payload.error||'Host node could not sign the membership request.'),{status:response.status||503});return payload.signatureHeader;}
async function coreMembership(env,nodeId,body){const rawText=JSON.stringify(body),signature=await signNode(env,nodeId,rawText);const response=env.CORE?.fetch?await env.CORE.fetch('https://civweave-core.internal/api/money-edge/memberships',{method:'POST',headers:{'content-type':'application/json','x-civweave-node-signature':signature},body:rawText}):await fetch(new URL('/api/money-edge/memberships',clean(env.CIVWEAVE_CORE_ORIGIN,2000)||'https://api.commonweave.earth'),{method:'POST',headers:{'content-type':'application/json','x-civweave-node-signature':signature},body:rawText});const payload=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(payload.error||`Core returned HTTP ${response.status}.`),{status:response.status});return payload.membership;}
async function beginMembership(request,env,nodeId){try{const input=await request.json().catch(()=>({})),userId=residentId(input.userId),tierId=clean(input.tierId||'member',80).toLowerCase();if(!['member','maker','builder','steward'].includes(tierId))throw Object.assign(new RangeError('Unknown Civweave membership tier.'),{status:400});const preparation=await capacityPost(env,'/members/prepare-membership',{nodeId,userId,loginCredentialHash:await hashCredential(input.credential)}),origin=nodeOrigin(nodeId,env);const membership=await coreMembership(env,nodeId,{nodeId,userId,tierId,idempotencyKey:`paid-seat:${nodeId}:${userId}:${tierId}`,successUrl:`${origin}/api/commerce/membership/return?result=success&nodeId=${encodeURIComponent(nodeId)}`,cancelUrl:`${origin}/api/commerce/membership/return?result=cancelled&nodeId=${encodeURIComponent(nodeId)}`});return json({ok:true,schema:'civweave.paid-seat-checkout.v1',nodeId,preparation,membership,checkout:membership},201);}catch(error){return json({ok:false,error:String(error?.message||error)},Number.isSafeInteger(error?.status)?error.status:500);}}
function returnToInstaller(request,env,nodeId){const source=new URL(request.url),target=new URL('/',clean(env.CIVWEAVE_CANONICAL_INSTALL_ORIGIN,2000)||'https://civweave.pages.dev');target.searchParams.set('host',nodeOrigin(nodeId,env));target.searchParams.set('node',nodeId);target.searchParams.set('membership',source.searchParams.get('result')==='success'?'success':'cancelled');return Response.redirect(target.href,303);}
export default{async fetch(request,env,ctx){const url=new URL(request.url),nodeId=resolveNodeId(request,env);if(nodeId&&request.method==='OPTIONS'&&url.pathname.startsWith('/api/commerce/'))return new Response(null,{status:204,headers:cors});if(nodeId&&request.method==='POST'&&url.pathname==='/api/commerce/membership/prejoin')return beginMembership(request,env,nodeId);if(nodeId&&request.method==='GET'&&url.pathname==='/api/commerce/membership/return')return returnToInstaller(request,env,nodeId);return baseWorker.fetch(request,env,ctx);},async scheduled(controller,env,ctx){if(typeof baseWorker.scheduled==='function')return baseWorker.scheduled(controller,env,ctx);}};
