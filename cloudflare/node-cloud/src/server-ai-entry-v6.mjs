import baseWorker,{CivweaveAccountDirectory} from './server-ai-entry-v5.mjs';
import {CivweaveCloudNode,creatorProvenanceInternalToken} from './cloud-node-provenance-v1.mjs';
import {CivweaveCapacityAccount} from './capacity-guildkeeper-v1.mjs';
import {nodeIdFromHostname,normalizeNodeId} from './index.mjs';

export {CivweaveCloudNode,CivweaveCapacityAccount,CivweaveAccountDirectory};
export const GUILDKEEPER_ADMIN_ROUTE='/api/fabric/capacity/guildkeepers';
export const CREATOR_PROVENANCE_ROUTE='/api/node/creator-provenance';
const enc=new TextEncoder();
const json=(value,status=200,headers={})=>Response.json(value,{status,headers:{'cache-control':'no-store','access-control-allow-origin':'*','access-control-allow-methods':'GET, POST, OPTIONS','access-control-allow-headers':'authorization, content-type, x-civweave-node-id',...headers}});
const clean=(value,max=180)=>String(value??'').trim().slice(0,max);
async function secretEqual(left,right){const a=String(left||''),b=String(right||'');if(!a||!b)return false;const [da,db]=await Promise.all([crypto.subtle.digest('SHA-256',enc.encode(a)),crypto.subtle.digest('SHA-256',enc.encode(b))]);const aa=new Uint8Array(da),bb=new Uint8Array(db);let diff=aa.length^bb.length;for(let i=0;i<Math.min(aa.length,bb.length);i++)diff|=aa[i]^bb[i];return diff===0}
function capacityStub(env){if(!env.CAPACITY)throw Object.assign(new Error('Capacity Durable Object binding is unavailable.'),{status:503});return env.CAPACITY.get(env.CAPACITY.idFromName('civweave-account'))}
function nodeStub(env,nodeId){if(!env.NODES)throw Object.assign(new Error('Guild Durable Object binding is unavailable.'),{status:503});const id=normalizeNodeId(nodeId);if(!id)throw Object.assign(new Error('Guild node ID is required.'),{status:400});return{id,stub:env.NODES.get(env.NODES.idFromName(id))}}
async function guildkeeperAdmin(request,env,action){const token=String(request.headers.get('authorization')||'').replace(/^Bearer\s+/i,'');if(!await secretEqual(token,env.NODE_FABRIC_OPERATOR_TOKEN))return json({ok:false,error:'forbidden'},403);const response=await capacityStub(env).fetch(`https://capacity.internal/guildkeepers/${action}`,{method:'POST',headers:{'content-type':'application/json'},body:await request.text()});return new Response(response.body,{status:response.status,headers:response.headers})}
async function authenticatedMember(request,env,ctx){const source=new URL(request.url),sessionUrl=new URL('/api/ai/node/session',source.origin),explicitNode=clean(source.searchParams.get('nodeId')||request.headers.get('x-civweave-node-id'),180);if(explicitNode)sessionUrl.searchParams.set('nodeId',explicitNode);const forwardedHeaders=new Headers({accept:'application/json'}),authorization=clean(request.headers.get('authorization'),20000);if(authorization)forwardedHeaders.set('authorization',authorization);if(explicitNode)forwardedHeaders.set('x-civweave-node-id',explicitNode);const response=await baseWorker.fetch(new Request(sessionUrl,{method:'GET',headers:forwardedHeaders}),env,ctx),payload=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(payload.error||'Guild member session is invalid.'),{status:response.status||401});const nodeId=normalizeNodeId(payload.nodeId||payload.member?.nodeId||explicitNode),userId=clean(payload.userId||payload.member?.userId,180);if(!nodeId||!userId)throw Object.assign(new Error('Guild member session did not resolve a resident.'),{status:401});return Object.freeze({nodeId,userId,member:payload.member||null,quota:payload.quota||null})}
async function provenanceDo(env,nodeId,path,input,method='POST'){const target=nodeStub(env,nodeId),token=await creatorProvenanceInternalToken(env),headers={'x-civweave-node-id':target.id,'x-civweave-internal-provenance':token};if(method==='POST')headers['content-type']='application/json';return target.stub.fetch(`https://node.internal${path}?nodeId=${encodeURIComponent(target.id)}`,{method,headers,body:method==='POST'?JSON.stringify(input||{}):undefined})}
async function creatorProvenance(request,env,ctx,action){try{if(action==='receipt'){const url=new URL(request.url),nodeId=normalizeNodeId(url.searchParams.get('nodeId')||request.headers.get('x-civweave-node-id')||nodeIdFromHostname(url.hostname,env.NODE_DOMAIN||'nodes.commonweave.earth'));if(!nodeId)throw Object.assign(new Error('Guild node ID is required for a provenance receipt.'),{status:400});const input=await request.json().catch(()=>({})),object=input.object||(input.payload?.kind==='civweave.creation-receipt.v1'?input.payload:null);if(!object)throw Object.assign(new Error('Signed creation receipt object is required.'),{status:400});return provenanceDo(env,nodeId,'/internal/creator-provenance/receipt',{object})}const member=await authenticatedMember(request,env,ctx),input=await request.json().catch(()=>({}));if(action==='requests')return provenanceDo(env,member.nodeId,'/internal/creator-provenance/audit/requests',input);if(action==='evidence')return provenanceDo(env,member.nodeId,'/internal/creator-provenance/audit/evidence',input);throw Object.assign(new Error('Unsupported Creator provenance action.'),{status:404})}catch(error){return json({ok:false,error:String(error?.message||error)},Number.isSafeInteger(error?.status)?error.status:500)}}
function apiRequest(url){return url.pathname.includes('/api/')}
function expensiveRequest(request,url){if(request.method!=='POST')return false;return ['/api/ai/node/generate','/api/ai/node/validation','/api/browser/tool'].some(path=>url.pathname.endsWith(path))}
async function digestKey(value){const bytes=new Uint8Array(await crypto.subtle.digest('SHA-256',enc.encode(String(value||''))));return[...bytes].map(byte=>byte.toString(16).padStart(2,'0')).join('')}
async function rateActor(request,url){const nodeId=clean(request.headers.get('x-civweave-node-id')||url.searchParams.get('nodeId')||'guild',180),authorization=String(request.headers.get('authorization')||'').trim();if(authorization)return`${nodeId}:auth:${await digestKey(authorization)}`;if(request.method==='POST'&&url.pathname.endsWith('/api/ai/node/session')){const input=await request.clone().json().catch(()=>({})),userId=clean(input?.userId,180);if(userId)return`${nodeId}:resident:${await digestKey(userId)}`}const ip=clean(request.headers.get('cf-connecting-ip')||'anonymous',80);return`${nodeId}:public:${await digestKey(`${ip}\n${url.pathname}`)}`}
function limited(kind,retryAfter=60){return json({ok:false,error:'Guild request rate limit exceeded.',code:'CIVWEAVE_GUILD_RATE_LIMIT',kind,retryAfter},429,{'content-type':'application/json; charset=utf-8','retry-after':String(retryAfter),'access-control-expose-headers':'retry-after'})}
async function enforceRateLimits(request,env,url){if(request.method==='OPTIONS'||!apiRequest(url))return null;const actor=await rateActor(request,url);if(env.GUILD_API_RATE_LIMITER?.limit){const result=await env.GUILD_API_RATE_LIMITER.limit({key:`api:${actor}`});if(!result?.success)return limited('api')}if(expensiveRequest(request,url)&&env.GUILD_AI_RATE_LIMITER?.limit){const result=await env.GUILD_AI_RATE_LIMITER.limit({key:`ai:${actor}`});if(!result?.success)return limited('ai')}return null}
export default{
  async fetch(request,env,ctx){const url=new URL(request.url);if(request.method==='OPTIONS'&&url.pathname.startsWith(CREATOR_PROVENANCE_ROUTE))return new Response(null,{status:204,headers:{'access-control-allow-origin':'*','access-control-allow-methods':'POST, OPTIONS','access-control-allow-headers':'authorization, content-type, x-civweave-node-id','access-control-max-age':'86400'}});const rateLimited=await enforceRateLimits(request,env,url);if(rateLimited)return rateLimited;const prefix=`${GUILDKEEPER_ADMIN_ROUTE}/`,action=url.pathname.startsWith(prefix)?url.pathname.slice(prefix.length):'';if(['appoint','remove','status'].includes(action)){if(request.method!=='POST')return json({ok:false,error:'Method not allowed.'},405);return guildkeeperAdmin(request,env,action)}if(url.pathname===`${CREATOR_PROVENANCE_ROUTE}/receipt`&&request.method==='POST')return creatorProvenance(request,env,ctx,'receipt');if(url.pathname===`${CREATOR_PROVENANCE_ROUTE}/audit/requests`&&request.method==='POST')return creatorProvenance(request,env,ctx,'requests');if(url.pathname===`${CREATOR_PROVENANCE_ROUTE}/audit/evidence`&&request.method==='POST')return creatorProvenance(request,env,ctx,'evidence');return baseWorker.fetch(request,env,ctx)},
  async scheduled(controller,env,ctx){if(typeof baseWorker.scheduled==='function')return baseWorker.scheduled(controller,env,ctx)}
};
