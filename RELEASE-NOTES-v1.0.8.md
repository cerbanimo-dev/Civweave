# Civweave Host Node Hub v1.0.8

- Repairs Anarchadia IndexedDB stores and first-run persistence.
- Opens Anarchadia directly into its visual hall by default.
- Adds persistent version badges and visual/legacy interface toggles.
- Makes Weaveling generate wish-specific capability maps through the configured interactive model, with a local fallback only when model generation is unavailable.
- Bumps service-worker generations and embedded app build identifiers.
