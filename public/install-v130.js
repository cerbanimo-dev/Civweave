(()=>{
'use strict';
const VERSION='1.0.6';
const ENTRY='/app/installed-entry-v146.html?system=commonweave';
const WORKER_REVISION='device-package-r41-no-native-dialog';
const ADDITIONS_REVISION='working-campus-additions-v183-settings-diagnostics';
const PREVIOUS_ADDITIONS_REVISION='working-campus-additions-v182-settings-single-pass';
const WORKER_BUILD=`${VERSION}-${WORKER_REVISION}-${ADDITIONS_REVISION}`;
const WORKER_URL=`/service-worker-v156.js?v=${WORKER_BUILD}`;
const PREPARE_TIMEOUT_MS=120000;
const AUTO_RESET_KEY='commonweave.device-package.auto-reset.v106-r45';
let installPrompt=null,registration=null,packageReady=false,packageStatus=null,packageError=null,preparing=false;
const workerWaits=new WeakMap();
const $=selector=>document.querySelector(selector);
const help=message=>{const node=$('#install-help');if(node)node.textContent=message};
const pause=ms=>new Promise(resolve=>setTimeout(resolve,ms));
function standalone(){return navigator.standalone===true||['standalone','fullscreen','minimal-ui','window-controls-overlay'].some(mode=>matchMedia(`(display-mode: ${mode})`).matches)}
function isIOS(){return /iphone|ipad|ipod/.test(navigator.userAgent.toLowerCase())}
function rootScope(reg){try{const scope=new URL(reg.scope);return scope.origin===location.origin&&scope.pathname==='/'}catch{return false}}
function workerUrls(reg){return[reg?.installing?.scriptURL,reg?.waiting?.scriptURL,reg?.active?.scriptURL].filter(Boolean)}
function currentWorker(reg){return workerUrls(reg).some(value=>{try{const url=new URL(value);return url.pathname==='/service-worker-v156.js'&&url.searchParams.get('v')===WORKER_BUILD}catch{return false}})}
function showPackage(status={}){packageStatus=status;$('#package-state').textContent=packageReady?'complete':packageError?'failed':'preparing';const missing=Array.isArray(status.missing)?status.missing.length:0,total=Number(status.assetCount||0)+Number(status.additions?.assetCount||0);$('#package-assets').textContent=packageReady&&total?`${total} core files`:missing?`${missing} missing files`:total?`${total} core files`:'checking core';$('#local-mode').textContent='v1.0.6 · deterministic · reusable diagnostic log levels'}
function guidance(){const button=$('#install-app');if(standalone()){button.disabled=false;button.textContent='Open Commonweave v1.0.6';help('The v1.0.6 settings diagnostics package is ready. Add ?cwlog=debug to enable the persistent log dock.');return}if(packageError){button.disabled=false;button.textContent='Reset and retry package';help(`Core package preparation failed: ${packageError.message}. Tap reset and retry to remove the incomplete worker and cache.`);return}if(preparing||!packageReady){button.disabled=true;button.textContent='Preparing v1.0.6…';help('Caching the five software surfaces and the reusable settings diagnostics layer. Provider and model code remain asleep.');return}if(installPrompt){button.disabled=false;button.textContent='Install Commonweave v1.0.6';help('The v1.0.6 deterministic offline package is ready to install.');return}button.disabled=false;button.textContent=isIOS()?'Show iPhone/iPad instructions':'Show installation instructions';help(isIOS()?'In Safari, use Share → Add to Home Screen.':'Use the browser’s Install app command if it is not offered automatically.')}
function failPackage(error){packageReady=false;packageError=error instanceof Error?error:new Error(String(error||'Unknown package error'));showPackage({...packageStatus,error:packageError.message});guidance()}
async function clearPackageCaches(){if(!('caches'in window))return;const keys=await caches.keys();await Promise.allSettled(keys.filter(key=>key.startsWith('commonweave-')||key.startsWith('cwext-')).map(key=>caches.delete(key)))}
async function resetDevicePackage(){help('Removing the superseded package…');if('serviceWorker'in navigator){const regs=await navigator.serviceWorker.getRegistrations();await Promise.allSettled(regs.filter(rootScope).map(reg=>reg.unregister()))}await clearPackageCaches();registration=null;await pause(350);const next=new URL(location.href);next.searchParams.set('package-reset',Date.now().toString(36));location.replace(next.href)}
function askWorker(type,timeoutMs=10000){return new Promise(resolve=>{const worker=registration?.waiting||registration?.active||registration?.installing;if(!worker)return resolve(null);const channel=new MessageChannel(),timer=setTimeout(()=>resolve(null),timeoutMs);channel.port1.onmessage=event=>{clearTimeout(timer);resolve(event.data||null)};worker.postMessage({type},[channel.port2])})}
function waitForWorker(worker){if(!worker)return Promise.resolve();if(workerWaits.has(worker))return workerWaits.get(worker);const promise=new Promise((resolve,reject)=>{let done=false,timer=0;const finish=error=>{if(done)return;done=true;clearTimeout(timer);worker.removeEventListener('statechange',check);error?reject(error):resolve()};const check=()=>{if(worker.state==='installed'||worker.state==='activated')finish();else if(worker.state==='redundant')finish(new Error('The browser rejected the v1.0.6 package because a required file could not be cached.'));else help(`Preparing v1.0.6 · ${worker.state||'working'}…`)};worker.addEventListener('statechange',check);timer=setTimeout(()=>finish(new Error('Core package preparation timed out.')),PREPARE_TIMEOUT_MS);check()});workerWaits.set(worker,promise);return promise}
async function confirmReady(){const status=await askWorker('GET_DEVICE_PACKAGE_STATUS');if(!status||status.type!=='COMMONWEAVE_DEVICE_PACKAGE')throw new Error('The device worker did not return core-package readiness.');packageReady=Boolean(status.ready);if(!packageReady)throw new Error(`${(status.missing||[]).length||'Some'} required core-package files are missing.`);const additions=await askWorker('GET_ADDITIONS_STATUS');if(!additions||additions.type!=='COMMONWEAVE_ADDITIONS_STATUS')throw new Error('The shared-tools layer did not return package readiness.');packageReady=Boolean(packageReady&&additions.ready);if(!packageReady)throw new Error(`${(additions.missing||[]).length||'Some'} shared-tool files are missing.`);packageError=null;const combined={...status,baseWorkerRevision:WORKER_REVISION,additionsRevision:ADDITIONS_REVISION,previousAdditionsRevision:PREVIOUS_ADDITIONS_REVISION,additions,missing:[...(status.missing||[]),...(additions.missing||[])]};showPackage(combined);guidance();return combined}
async function preparePackage(){if(preparing)return;preparing=true;packageReady=false;packageError=null;showPackage({});guidance();try{if(!('serviceWorker'in navigator))throw new Error('This browser does not support service workers.');const existing=await navigator.serviceWorker.getRegistration('/');if(existing&&!currentWorker(existing)&&sessionStorage.getItem(AUTO_RESET_KEY)!=='1'){sessionStorage.setItem(AUTO_RESET_KEY,'1');await existing.unregister();await clearPackageCaches();await pause(250)}registration=await navigator.serviceWorker.register(WORKER_URL,{scope:'/',updateViaCache:'none'});await registration.update().catch(()=>{});await waitForWorker(registration.installing||registration.waiting||registration.active);if(registration.waiting)registration.waiting.postMessage({type:'SKIP_WAITING'});await navigator.serviceWorker.ready;registration=await navigator.serviceWorker.getRegistration('/');await confirmReady()}catch(error){failPackage(error)}finally{preparing=false;guidance()}}
async function installOrOpen(){if(packageError)return resetDevicePackage();if(standalone()){location.assign(ENTRY);return}if(installPrompt){const prompt=installPrompt;installPrompt=null;await prompt.prompt();const choice=await prompt.userChoice.catch(()=>null);if(choice?.outcome==='accepted')help('Commonweave v1.0.6 installation accepted.');else guidance();return}help(isIOS()?'Use Safari Share → Add to Home Screen, then open Commonweave from the new icon.':'Open the browser menu and choose Install app or Add to Home screen.')}
addEventListener('beforeinstallprompt',event=>{event.preventDefault();installPrompt=event;guidance()});
addEventListener('appinstalled',()=>{installPrompt=null;help('Commonweave v1.0.6 is installed.')});
$('#install-app')?.addEventListener('click',installOrOpen);
$('#check-update')?.addEventListener('click',async()=>{help('Checking for v1.0.6…');try{registration=registration||await navigator.serviceWorker.getRegistration('/');await registration?.update();await confirmReady();help('Commonweave v1.0.6 is ready.')}catch(error){failPackage(error)}});
showPackage({});guidance();preparePackage();
})();
