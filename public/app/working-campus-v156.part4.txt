ress.filter(x=>x!==i);p.status=p.progress.length===p.steps.length?'completed':p.progress.length?'active':'ready';if(state.plan.paths.every(x=>x.status==='completed'))state.plan.state='completed';state.plan.updatedAt=now();canonical();handoffs();render()}));$$('[data-open]').forEach(b=>b.addEventListener('click',()=>openRealm(b.dataset.open)));$$('[data-load]').forEach(b=>b.addEventListener('click',()=>{const item=parse(localStorage.getItem(I),[]).find(x=>x.id===b.dataset.load);if(item){state.plan=structuredClone(item.plan);state.wish=state.plan.wish;state.stage=state.plan.state==='review'?'review':'active';state.view='weave';render()}}))}function badges(){for(const id of Object.keys(R)){const b=document.querySelector(`[data-badge="${id}"]`),n=!state.plan?0:id==='anarchadia'?1:state.plan.paths.filter(p=>p.realm===id&&p.status!=='completed').length;b.hidden=!n;b.textContent=n}}function openRealm(id){const realm=R[id];if(!realm)return;const inbox=parse(localStorage.getItem(Q),[]).filter(x=>x.target===id&&x.payload?.weaveId===state.plan?.id),context={type:'commonweave:context',version:'1.1',model,privacy:{automaticCrossAppEffects:false,secretsShared:false,handoffsRequireReview:true},intention:state.plan?{id:state.plan.id,title:state.plan.title,wish:state.plan.wish,outcome:state.plan.outcome,state:state.plan.state}:null};localStorage.setItem(C,JSON.stringify(context));localStorage.setItem(H,JSON.stringify({target:id,weaveId:state.plan?.id||'',wish:state.plan?.wish||state.wish,plan:state.plan,packets:inbox,preparedAt:now()}));const url=new URL(realm.url,location.origin);if(state.plan?.id)url.searchParams.set('weave',state.plan.id);url.searchParams.set('source','commonweave');location.href=url.href}function toast(text){const t=$('#toast');t.textContent=text;t.hidden=false;setTimeout(()=>t.hidden=true,2600)}function exportPlan(){if(!state.plan)return toast('Create a weave first.');const blob=new Blob([JSON.stringify({schema:'commonweave.portable-weave.v1',exportedAt:now(),plan:state.plan},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='commonweave-weave.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}const GEMINI_ENDPOINT='https://generativelanguage.googleapis.com/v1beta',GEMINI_MODEL='gemini-3.5-flash-lite';
function syncModelFields(){const route=$('#model-route').value,isGemini=route==='gemini';$('#gemini-options').hidden=!isGemini;$('#test-gemini').hidden=!isGemini;$('#test-antigravity').hidden=!isGemini;if(isGemini){if(!clean($('#model-name').value))$('#model-name').value=GEMINI_MODEL;if(!clean($('#model-endpoint').value))$('#model-endpoint').value=GEMINI_ENDPOINT}}
function settings(){const d=$('#settings');$('#model-route').value=model.route||'gemini';$('#model-name').value=model.model||($('#model-route').value==='gemini'?GEMINI_MODEL:'');$('#model-endpoint').value=model.endpoint||($('#model-route').value==='gemini'?GEMINI_ENDPOINT:'');$('#model-key').value=sessionStorage.getItem(S)||parse(sessionStorage.getItem('commonweave-model-session'),{}).apiKey||'';$('#remote-consent').checked=Boolean(model.consent);$('#agentic-enabled').checked=Boolean(model.agenticEnabled);$('#agentic-model').value=model.agenticModel||'antigravity';$('#model-test-status').textContent='No provider test has run.';syncModelFields();d.showModal()}
function currentModelSetup(){const route=$('#model-route').value,key=clean($('#model-key').value),consent=$('#remote-consent').checked,interactive={route,provider:route,model:clean($('#model-name').value)||(route==='gemini'?GEMINI_MODEL:''),endpoint:clean($('#model-endpoint').value)||(route==='gemini'?GEMINI_ENDPOINT:''),externalConsent:consent};return{route,key,consent,agenticEnabled:route==='gemini'&&$('#agentic-enabled').checked,interactive,agentic:{route:'gemini',provider:'gemini',model:clean($('#agentic-model').value)||'antigravity',endpoint:interactive.endpoint,externalConsent:consent}}}
function persistModel(setup){model={...setup.interactive,consent:setup.consent,agenticEnabled:setup.agenticEnabled,agenticModel:setup.agentic.model};localStorage.setItem(M,JSON.stringify(model));localStorage.setItem('commonweave.universal-ai.v127',JSON.stringify({route:model.route,provider:model.provider,model:model.model,endpoint:model.endpoint,consent:model.consent,agenticEnabled:model.agenticEnabled}));localStorage.setItem('commonweave-shared-model',JSON.stringify({model:setup.interactive,savedAt:now()}));localStorage.setItem('commonweave-model-profiles-v1',JSON.stringify({interactive:setup.interactive,agentic:setup.agenticEnabled?setup.agentic:null,agenticEnabled:setup.agenticEnabled,savedAt:now()}));if(setup.key){sessionStorage.setItem(S,setup.key);sessionStorage.setItem('commonweave-model-session',JSON.stringify({apiKey:setup.key,remoteConsent:setup.consent,provider:setup.route,savedAt:now()}))}else{sessionStorage.removeItem(S);sessionStorage.removeItem('commonweave-model-session')}}
function ensureModelRuntime(){if(globa