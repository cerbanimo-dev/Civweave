(()=>{
'use strict';
const VERSION='1.0.69-local-ai-bridge-v274-health';
if(globalThis.CivweaveLocalModelBridgeV266?.version===VERSION)return;
const MIDDLEWARE_ID='downloaded-local-v274';
const priorBridge=globalThis.CivweaveLocalModelBridgeV266;let original=priorBridge?.base?.()||null,patched=null,registered=false;
const downloads=()=>globalThis.CivweaveLocalModelDownloadV266,local=()=>globalThis.CivweaveLocalModelRuntimeV266,broker=()=>globalThis.CivweaveAICapabilityBrokerV268,spine=()=>globalThis.CivweaveFastInteractiveV192;
const selected=()=>{const value=downloads()?.selection?.();return Boolean(value?.active&&value?.id)};
function timeoutFor(spec,request){const explicit=Number(request.config?.timeoutMs||request.timeoutMs||0);if(explicit)return Math.max(180000,explicit);return Number(spec?.healthTimeoutMs||360000)}
function routeDecision(request,spec){const b=broker();if(b?.supportsLocalRequest){const support=b.supportsLocalRequest(spec,request);return{useLocal:Boolean(support?.ok),reason:support?.reason||'capability broker rejected local route',requirements:support?.requirements||null}}const profile=request.executionProfile||'interactive';return{useLocal:profile==='interactive',reason:profile==='interactive'?'legacy interactive local route':'agentic local routing requires the capability broker',requirements:{profile}}}
const wantsStream=request=>Boolean(request.config?.stream??request.stream);
function thinkingFor(request){const explicit=request.config?.thinking??request.thinking;if(explicit!=null)return Boolean(explicit);return String(request.executionProfile||'interactive')==='agentic'}
function resultFrom(request,run,started,decision,requestId,events,thinking){const spec=local().activeSpec(),m=run.metrics||{},structuredRequested=Boolean(request.schema||request.responseFormat==='json'||request.responseFormat==='structured');return{schema:'civweave-model-result-1.0',requestId,purpose:String(request?.purpose||'interactive'),status:'success',requested:{provider:'downloaded-local',model:spec?.id||'local-model',endpoint:'',executionProfile:request?.executionProfile||'interactive'},actual:{provider:'downloaded-local',model:spec?.id||'local-model'},outputText:run.text||'',outputJson:run.json||null,usage:{inputTokens:Number(m.promptTokens||0),outputTokens:Number(m.generatedTokens||0),totalTokens:Number(m.promptTokens||0)+Number(m.generatedTokens||0),costCents:0,remainingCents:0},timing:{startedAt:new Date(Date.now()-(performance.now()-started)).toISOString(),completedAt:new Date().toISOString(),elapsedMs:run.elapsedMs||Math.round(performance.now()-started),ttftMs:m.ttftMs??null,coldStartMs:m.coldStartMs||0,warmupMs:m.warmupMs||0,tokensPerSecond:m.tokensPerSecond||0},events:events.slice(-120),diagnostics:[{code:'LOCAL_MODEL',message:'Generated entirely on this device from the explicitly selected downloaded model.'},{code:'CAPABILITY_ROUTE',message:decision.reason,requirements:decision.requirements||null},{code:'LOCAL_CONTEXT',message:`Model window ${Number(spec?.contextWindowTokens||0).toLocaleString()} tokens; Civweave working default ${Number(spec?.workingContextTokens||0).toLocaleString()} tokens.`,promptTokens:m.promptTokens||0},{code:'LOCAL_THINKING',message:thinking?'Thinking template enabled for this agentic request.':'Thinking template disabled for responsive interactive generation.'},{code:'LOCAL_STREAMING',message:run.streamed?'Downloaded-local text streamed incrementally from WebGPU.':'Downloaded-local generation returned as one completed result.'}],stream:{requested:wantsStream(request),used:Boolean(run.streamed)},structured:{requested:structuredRequested,valid:!structuredRequested||Boolean(run.json),repairAttempts:0},capabilityRouting:{schema:'civweave.ai-capability-route.v1',route:'downloaded-local',reason:decision.reason,requirements:decision.requirements||null},localMetrics:m}}
async function runLocal(request,spec,decision){const started=performance.now(),requestId=`local-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`,events=[];let accumulated='';const emit=(phase,detail={})=>{const event={schema:'civweave-model-event-1.0',requestId,phase,provider:'downloaded-local',model:spec.id,purpose:String(request?.purpose||'interactive'),executionProfile:request.executionProfile||'interactive',at:new Date().toISOString(),...detail};events.push(event);try{request.onEvent?.(event)}catch{}try{dispatchEvent(new CustomEvent('civweave:model-event',{detail:event}))}catch{}return event};const thinking=thinkingFor(request);try{const run=await local().generate({messages:request.messages||[],maxNewTokens:request.config?.maxTokens||request.maxTokens||1024,temperature:request.config?.temperature??request.temperature,thinking,timeoutMs:timeoutFor(spec,request),stream:wantsStream(request),onToken:token=>{const text=String(token?.text||'');if(!text)return;accumulated+=text;const event=emit('partial',{text,accumulatedText:accumulated,index:Number(token?.index)||0});try{dispatchEvent(new CustomEvent('civweave:local-model-token',{detail:event}))}catch{}},onProgress:p=>{const detail={phase:'local-model-progress',provider:'downloaded-local',model:spec.id,executionProfile:request.executionProfile||'interactive',...p};try{request.onProgress?.(detail)}catch{}try{dispatchEvent(new CustomEvent('civweave:local-model-inference-progress',{detail}))}catch{}}});const result=resultFrom(request,run,started,decision,requestId,events,thinking);if(result.structured.requested&&!result.structured.valid)throw Object.assign(new Error('The downloaded local model did not return valid structured output for this request.'),{code:'LOCAL_MODEL_STRUCTURED_OUTPUT_FAILED',localResult:result});return result}catch(error){try{dispatchEvent(new CustomEvent('civweave:local-model-error',{detail:{message:String(error?.message||error),code:error?.code||'LOCAL_MODEL_FAILED',model:spec?.id||'',executionProfile:request.executionProfile||'interactive'}}))}catch{}throw error}}
function middleware(){return{async handle(request){if(!selected())return null;const spec=local()?.activeSpec?.();if(!spec)throw Object.assign(new Error('A downloaded local model is selected, but its runtime has not attached yet.'),{code:'LOCAL_MODEL_NOT_READY'});const decision=routeDecision(request,spec);if(!decision.useLocal)return null;return{handled:true,result:await runLocal(request,spec,decision)}}}}
function register(){const runtimeSpine=spine();if(!runtimeSpine?.register)return false;runtimeSpine.register(MIDDLEWARE_ID,middleware(),100);registered=true;patched=runtimeSpine.proxy?.()||globalThis.CivweaveModelRuntime;original=runtimeSpine.base?.()||original;try{dispatchEvent(new CustomEvent('civweave:local-model-bridge-installed',{detail:{version:VERSION,mode:'runtime-spine',middleware:MIDDLEWARE_ID,selected:downloads()?.selection?.()||null,capabilityAware:Boolean(broker()),streaming:true,thinkingProfiles:true}}))}catch{}return true}
async function legacyGenerate(request={}){if(!selected())return original.generate(request);const spec=local()?.activeSpec?.();if(!spec)throw Object.assign(new Error('A downloaded local model is selected, but its runtime has not attached yet.'),{code:'LOCAL_MODEL_NOT_READY'});const decision=routeDecision(request,spec);if(!decision.useLocal)return original.generate(request);return runLocal(request,spec,decision)}
function patchLegacy(){if(register())return true;const current=globalThis.CivweaveModelRuntime;if(!current?.generate)return false;if(current===patched)return true;if(!original)original=current;const base=original||current;patched=Object.freeze({...current,__civweaveLocalBridgeV266:true,__civweaveLocalBridgeVersion:VERSION,baseVersion:base.version,version:`${base.version}+local-v274`,generate:legacyGenerate,generateInteractive:request=>legacyGenerate({...request,executionProfile:'interactive'}),generateAgentic:request=>legacyGenerate({...request,executionProfile:'agentic'})});globalThis.CivweaveModelRuntime=patched;return true}
function patch(){return register()||patchLegacy()}
addEventListener('civweave:runtime-spine-ready',register);addEventListener('civweave:model-runtime-ready',patch);addEventListener('civweave:local-model-selection',patch);addEventListener('pageshow',patch);patch();
globalThis.CivweaveLocalModelBridgeV266=Object.freeze({version:VERSION,patch,register,selected,routeDecision,thinkingFor,middlewareId:MIDDLEWARE_ID,base:()=>original,patched:()=>patched,get registered(){return registered},streaming:true,timingDiagnostics:true});
})();