(()=>{
'use strict';
const VERSION='1.0.86-local-ai-primary-route-v283-hard-local';
if(globalThis.CivweaveLocalAIPrimaryRouteV283?.version===VERSION)return;
const ROUTE='downloaded-local',FORM_SELECTOR='form[data-cw-cleanroom-form]',SELECTION_KEY='civweave.local-ai.selection.v266',SETTINGS_KEY='civweave.universal-ai.v127',PROFILES_KEY='civweave-model-profiles-v1';
const parse=(value,fallback={})=>{try{return JSON.parse(value)??fallback}catch{return fallback}};
const controller=()=>globalThis.CivweaveModelSettingsControllerV173,manager=()=>globalThis.CivweaveLocalModelDownloadV266,registry=()=>globalThis.CivweaveLocalModelRegistryV266,field=(form,name)=>form?.elements?.namedItem?.(name)||null;
function selection(){const live=manager()?.selection?.();if(live?.active&&live.id)return live;try{return parse(localStorage.getItem(SELECTION_KEY),{active:false,id:null})}catch{return{active:false,id:null}}}
function label(value=selection()){if(!value?.active||!value.id)return'';return registry()?.byId?.(value.id)?.label||value.id}
function baseRoute(form){const saved=String(controller()?.readState?.()?.route||form?.dataset?.cwEmbeddedFallbackRoute||'deterministic');return saved===ROUTE?'deterministic':saved}
function ensureOption(form){const route=field(form,'route');if(!route)return null;if(!route.querySelector(`option[value="${ROUTE}"]`)){const option=document.createElement('option');option.value=ROUTE;option.textContent='Embedded local AI (downloaded model)';route.querySelector('option[value="deterministic"]')?.after(option)}return route}
function ensurePanel(form){let panel=form.querySelector('[data-panel="downloaded-local"]');if(panel)return panel;panel=document.createElement('section');panel.className='cw-clean-panel';panel.dataset.panel='downloaded-local';panel.hidden=true;panel.innerHTML='<div><h3>Embedded local AI</h3><p data-embedded-local-copy></p></div>';form.querySelector('[data-panel="deterministic"]')?.after(panel);return panel}
function apply(form,{preferSelection=false}={}){const route=ensureOption(form),panel=ensurePanel(form);if(!route||!panel)return false;const selected=selection();if(!form.dataset.cwEmbeddedFallbackRoute)form.dataset.cwEmbeddedFallbackRoute=baseRoute(form);if(preferSelection&&selected.active&&selected.id)route.value=ROUTE;const local=route.value===ROUTE;if(local){form.querySelector('[data-panel="deterministic"]')?.setAttribute('hidden','');form.querySelector('[data-panel="remote"]')?.setAttribute('hidden','');panel.hidden=false;const copy=panel.querySelector('[data-embedded-local-copy]');if(copy)copy.innerHTML=selected.active&&selected.id?`<b>${label(selected)}</b> is the active local-only interactive model. Civweave will not fall through to a Guild or cloud provider while this route is selected.`:'Download a model below and tap <b>Use locally</b>. Then save this route.'}else panel.hidden=true;return true}
function restoreBase(form){const route=ensureOption(form);if(!route||route.value!==ROUTE)return;route.value=form.dataset.cwEmbeddedFallbackRoute||baseRoute(form)||'deterministic';route.dispatchEvent(new Event('change',{bubbles:true}))}
function localDetail(selected){const interactive={route:ROUTE,provider:ROUTE,model:selected.id,endpoint:'',externalConsent:false};return{version:VERSION,route:ROUTE,primaryRoute:ROUTE,primaryModel:selected.id,interactive,agentic:null,agenticEnabled:false,localSelection:selected,savedAt:new Date().toISOString()}}
function persistLocal(selected){if(!selected?.active||!selected.id)return null;const at=new Date().toISOString(),interactive={route:ROUTE,provider:ROUTE,model:selected.id,endpoint:'',externalConsent:false},existingSettings=parse(localStorage.getItem(SETTINGS_KEY),{}),existingProfiles=parse(localStorage.getItem(PROFILES_KEY),{});const stored={...existingSettings,...interactive,consent:false,agenticEnabled:false,settingsOwner:VERSION,localOnly:true,updatedAt:at};const profiles={...existingProfiles,interactive,agentic:null,agenticEnabled:false,settingsOwner:VERSION,localOnly:true,updatedAt:at};try{localStorage.setItem(SETTINGS_KEY,JSON.stringify(stored));localStorage.setItem(PROFILES_KEY,JSON.stringify(profiles))}catch{}const detail={...localDetail(selected),savedAt:at};try{dispatchEvent(new CustomEvent('civweave:model-settings-saved',{detail}))}catch{}return detail}
function bind(form){if(!form||form.dataset.cwEmbeddedPrimaryV283==='true')return form;form.dataset.cwEmbeddedPrimaryV283='true';const route=ensureOption(form);ensurePanel(form);form.dataset.cwEmbeddedFallbackRoute=baseRoute(form);route?.addEventListener('change',()=>queueMicrotask(()=>apply(form)));form.addEventListener('submit',event=>{const chosen=field(form,'route')?.value;if(chosen!==ROUTE){const selected=selection();if(selected.active&&selected.id)manager()?.select?.(null);return}event.preventDefault();event.stopImmediatePropagation();const selected=selection(),status=form.querySelector('[data-status],[data-save-status]');if(!selected.active||!selected.id){if(status)status.textContent='Download a model below and tap Use locally before selecting Embedded local AI as primary.';apply(form);return}persistLocal(selected);if(status)status.textContent=`Saved local-only AI · ${label(selected)}. Guild and cloud fallback are disabled for this route.`;apply(form)},true);apply(form,{preferSelection:true});return form}
function enhance(){return bind(document.querySelector(FORM_SELECTOR))}
addEventListener('civweave:model-settings-opened',()=>queueMicrotask(enhance));
addEventListener('civweave:local-model-selection',event=>queueMicrotask(()=>{const form=document.querySelector(FORM_SELECTOR);const selected=event?.detail;if(selected?.active&&selected.id){persistLocal(selected);if(form){if(!form.dataset.cwEmbeddedFallbackRoute)form.dataset.cwEmbeddedFallbackRoute=baseRoute(form);const route=ensureOption(form);if(route)route.value=ROUTE;apply(form)}}else if(form){restoreBase(form);queueMicrotask(()=>apply(form))}}));
addEventListener('pageshow',()=>queueMicrotask(enhance));
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>queueMicrotask(enhance),{once:true});else queueMicrotask(enhance);
globalThis.CivweaveLocalAIPrimaryRouteV283=Object.freeze({version:VERSION,route:ROUTE,enhance,selection,label,persistLocal});
})();
