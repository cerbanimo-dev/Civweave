(()=>{
'use strict';
const VERSION='211.1-explicit-persistence-no-settings-ownership';
if(globalThis.CivweaveDeviceCredentialPersistenceV211?.version===VERSION)return;
const SETTINGS_KEY='civweave.universal-ai.v127';
const PROFILES_KEY='civweave-model-profiles-v1';
const SESSION_KEY='civweave-model-session';
const SECRET_KEY='civweave-model-secrets-v1';
const PERSIST_KEY='civweave-model-persistent-secrets-v191';
const LEGACY_PERSIST_KEY='civweave-model-persistent-secrets-v160';
const POLICY_KEY='civweave-model-credential-policy-v191';
const parse=(value,fallback={})=>{try{const parsed=JSON.parse(value);return parsed==null?fallback:parsed}catch{return fallback}};
const read=(storage,key,fallback='')=>{try{return storage?.getItem?.(key)??fallback}catch{return fallback}};
const write=(storage,key,value)=>{try{storage?.setItem?.(key,String(value));return true}catch{return false}};
const remove=(storage,key)=>{try{storage?.removeItem?.(key)}catch{}};
const now=()=>new Date().toISOString();
function providerName(value){const provider=String(value||'').trim().toLowerCase();if(provider==='gemini')return'gemini';if(provider==='ollama'||provider==='local-api')return'ollama';if(['openai','compatible','openai-compatible','hosted'].includes(provider))return'openai-compatible';return'deterministic'}
function currentConfig(){const settings=parse(read(localStorage,SETTINGS_KEY,'{}'),{}),profiles=parse(read(localStorage,PROFILES_KEY,'{}'),{}),interactive=profiles?.interactive&&typeof profiles.interactive==='object'?profiles.interactive:settings,provider=providerName(interactive?.provider||interactive?.route||settings?.provider||settings?.route);return{provider,route:provider,model:String(interactive?.model||settings?.model||(provider==='gemini'?'gemini-3.5-flash-lite':'local-model')).trim(),endpoint:String(interactive?.endpoint||settings?.endpoint||(provider==='gemini'?'https://generativelanguage.googleapis.com/v1beta':'')).trim(),externalConsent:Boolean(interactive?.externalConsent??interactive?.remoteConsent??settings?.externalConsent??settings?.consent)}}
function persistentRecord(){const current=parse(read(localStorage,PERSIST_KEY,''),null),legacy=parse(read(localStorage,LEGACY_PERSIST_KEY,''),null),source=current||legacy;if(!source||typeof source!=='object')return null;const nested=source.session&&typeof source.session==='object'?source.session:source,apiKey=String(nested.apiKey||source.apiKey||'').trim();if(!apiKey)return null;return{apiKey,provider:providerName(nested.provider||source.provider||'gemini'),model:String(nested.model||source.model||'').trim(),endpoint:String(nested.endpoint||source.endpoint||'').trim(),remoteConsent:Boolean(nested.remoteConsent??source.remoteConsent??nested.externalConsent??source.externalConsent),savedAt:String(source.savedAt||nested.savedAt||''),legacy:Boolean(!current&&legacy)}}
function mirrorRuntime(record){if(!record?.apiKey)return false;const config=currentConfig(),provider=providerName(config.provider||record.provider),model=config.model||record.model,endpoint=config.endpoint||record.endpoint,consent=typeof record.remoteConsent==='boolean'?record.remoteConsent:Boolean(config.externalConsent),savedAt=record.savedAt||now(),current=parse(read(sessionStorage,SESSION_KEY,'{}'),{});write(sessionStorage,SESSION_KEY,JSON.stringify({...current,apiKey:record.apiKey,provider,model,endpoint,remoteConsent:consent,savedAt,restoredAt:now(),credentialRevision:VERSION}));return true}
function restore(){const record=persistentRecord();if(!record)return false;write(localStorage,POLICY_KEY,'device');if(record.legacy){write(localStorage,PERSIST_KEY,JSON.stringify({schema:'civweave.device-model-secret.v211',apiKey:record.apiKey,provider:record.provider,model:record.model,endpoint:record.endpoint,remoteConsent:record.remoteConsent,savedAt:record.savedAt||now(),credentialRevision:VERSION,migratedFrom:'v160'}));remove(localStorage,LEGACY_PERSIST_KEY)}const restored=mirrorRuntime(record);if(restored)try{dispatchEvent(new CustomEvent('civweave:model-credential-restored',{detail:{version:VERSION,provider:record.provider,remembered:true,at:now()}}))}catch{}return restored}
function forget(){remove(localStorage,PERSIST_KEY);remove(localStorage,LEGACY_PERSIST_KEY);write(localStorage,POLICY_KEY,'session');remove(sessionStorage,SESSION_KEY);remove(sessionStorage,SECRET_KEY);try{dispatchEvent(new CustomEvent('civweave:model-secret-forgotten',{detail:{version:VERSION,at:now()}}))}catch{}return status()}
function status(){const remembered=Boolean(persistentRecord()),session=parse(read(sessionStorage,SESSION_KEY,'{}'),{});return{version:VERSION,remembered,session:Boolean(session.apiKey),mode:remembered?'device':'session',usable:Boolean(session.apiKey&&session.remoteConsent)}}
function persistFromSession(mode='session'){const selected=mode==='device'?'device':'session';if(selected!=='device'){remove(localStorage,PERSIST_KEY);remove(localStorage,LEGACY_PERSIST_KEY);write(localStorage,POLICY_KEY,'session');return false}const session=parse(read(sessionStorage,SESSION_KEY,'{}'),{}),apiKey=String(session.apiKey||'').trim();if(!apiKey)return false;const config=currentConfig(),record={schema:'civweave.device-model-secret.v211',apiKey,provider:providerName(session.provider||config.provider),model:String(session.model||config.model||'').trim(),endpoint:String(session.endpoint||config.endpoint||'').trim(),remoteConsent:Boolean(session.remoteConsent??config.externalConsent),savedAt:String(session.savedAt||now()),credentialRevision:VERSION};write(localStorage,PERSIST_KEY,JSON.stringify(record));remove(localStorage,LEGACY_PERSIST_KEY);write(localStorage,POLICY_KEY,'device');mirrorRuntime(record);return true}
function decorate(){return null}
function refreshForms(){return false}
function patchSettingsApi(){return globalThis.CivweaveModelSettingsControllerV173||null}
globalThis.CivweaveDeviceCredentialPersistenceV211=Object.freeze({version:VERSION,restore,forget,status,persistFromSession,decorate,refreshForms,patchSettingsApi,credentialPolicy:'explicit-session-or-device',storage:'browser-local-storage',automaticRestore:false,automaticListeners:false,inputOwnership:false,settingsApiPatching:false,reason:'Canonical Settings controller owns credential lifecycle. This API remains for explicit compatibility calls only.'});
})();
