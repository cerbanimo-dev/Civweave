(()=>{
'use strict';
const VERSION='1.0.115-local-chat-runtime-v302',SEL='civweave.local-ai.selection.v266',HEALTH='civweave.local-ai.health.v286',BOOT='/app/local-ai/bootstrap-v266.js?v=1.0.115-v302-session-handoff',BOOT_REVISION='1.0.115-local-ai-bootstrap-v302-session-handoff',BOOT_LOAD_TIMEOUT_MS=15000,BOOT_READY_TIMEOUT_MS=45000;
if(globalThis.CivweaveLocalChatRuntimeV295?.version===VERSION)return;
let bootstrapLoad=null,bootstrapFailed=false;
const parse=(v,d)=>{try{return JSON.parse(v)??d}catch{return d}},clamp=(n,a,b)=>Math.max(a,Math.min(b,Number(n)||0));
function selected(){const live=globalThis.CivweaveLocalModelDownloadV266?.selection?.();if(live?.active&&live.id)return live;const x=parse(localStorage.getItem(SEL),{});return x?.active&&x.id?x:null}
function health(id){const all=globalThis.CivweaveLocalModelTestPulseV269?.health?.()||parse(localStorage.getItem(HEALTH),{});return all?.[id]||{}}
function budget(id){const m=health(id).metrics||{},tps=Math.max(0,Number(m.tokensPerSecond||m.benchmarkTokensPerSecond||0)),maxNewTokens=tps?Math.round(clamp(tps*30,48,128)):64,cold=Math.max(0,Number(m.coldStartMs||0)),timeoutMs=Math.round(clamp(Math.max(90000,cold*1.5+60000),90000,180000));return{maxNewTokens,timeoutMs,tps,coldStartMs:cold}}
function bounded(value,ms,error){return new Promise((resolve,reject)=>{let settled=false;const timer=setTimeout(()=>{if(settled)return;settled=true;reject(error)},ms);Promise.resolve(value).then(result=>{if(settled)return;settled=true;clearTimeout(timer);resolve(result)},reason=>{if(settled)return;settled=true;clearTimeout(timer);reject(reason)})})}
function load(src,ready){
 if(ready?.())return Promise.resolve(true);if(bootstrapLoad)return bootstrapLoad;
 const path=new URL(src,location.href).pathname;
 bootstrapLoad=new Promise((resolve,reject)=>{const s=document.createElement('script'),fail=message=>reject(Object.assign(new Error(message),{code:'LOCAL_RUNTIME_BOOT_LOAD_FAILED',phase:'loading-runtime'})),timer=setTimeout(()=>fail(`The local AI bootstrap did not load within ${Math.round(BOOT_LOAD_TIMEOUT_MS/1000)} seconds.`),BOOT_LOAD_TIMEOUT_MS);s.src=src;s.async=false;s.dataset.civweaveLocalChatBootstrap='v302';s.onload=()=>{clearTimeout(timer);ready?.()?resolve(true):fail(`${path} loaded without becoming ready.`)};s.onerror=()=>{clearTimeout(timer);fail(`Could not load ${path}.`)};document.head.append(s)}).finally(()=>{bootstrapLoad=null});
 return bootstrapLoad
}
function runtimeReady(){const runtime=globalThis.CivweaveLocalModelRuntimeV266;return Boolean(runtime?.version==='1.0.115-local-ai-runtime-v302-session-handoff'&&runtime?.generate&&runtime?.freshWorkerFallback===true&&runtime?.phaseAwareErrors===true&&runtime?.promptBudgetEnforced===true&&runtime?.terminalCancellation===true&&runtime?.settingsTeardown===true&&globalThis.CivweaveLocalModelDownloadV266?.selection)}
async function ready(onProgress){
 if(runtimeReady())return true;if(bootstrapFailed){try{delete globalThis.CivweaveLocalAIBootstrapV266}catch{globalThis.CivweaveLocalAIBootstrapV266=undefined}bootstrapFailed=false}
 const started=performance.now();try{onProgress?.({phase:'loading-runtime',stage:'bootstrap',elapsedMs:0,timeoutMs:BOOT_READY_TIMEOUT_MS});await load(BOOT,()=>globalThis.CivweaveLocalAIBootstrapV266?.revision===BOOT_REVISION&&Boolean(globalThis.CivweaveLocalAIBootstrapV266?.ready));const boot=globalThis.CivweaveLocalAIBootstrapV266,ok=await bounded(boot?.ready,BOOT_READY_TIMEOUT_MS,Object.assign(new Error(`The local AI runtime did not become ready within ${Math.round(BOOT_READY_TIMEOUT_MS/1000)} seconds.`),{code:'LOCAL_RUNTIME_BOOT_TIMEOUT',phase:'loading-runtime'}));if(!ok||!runtimeReady())throw Object.assign(new Error('The local AI bootstrap finished without a compatible runtime.'),{code:'LOCAL_RUNTIME_BOOT_FAILED',phase:'loading-runtime'});onProgress?.({phase:'loading-runtime',stage:'ready',elapsedMs:performance.now()-started,progress:1});return true}catch(error){bootstrapFailed=true;throw error}
}
function idleLimit(phase){const p=String(phase||'');if(p==='loading-model')return 300000;if(p==='loading-tokenizer'||p==='warming-model'||p==='benchmarking-model')return 180000;if(p.includes('download')||p==='backend-fallback'||p==='backend-quarantined')return 360000;if(p==='generating'||p==='preparing-prompt')return 120000;return 180000}
async function generate({systemPrompt,messages,onToken,onProgress}){
 const pick=selected();if(!pick)throw Object.assign(new Error('No downloaded local model is selected.'),{code:'LOCAL_MODEL_NOT_SELECTED'});await ready(onProgress);
 const runtime=globalThis.CivweaveLocalModelRuntimeV266,spec=globalThis.CivweaveLocalModelRegistryV266?.byId?.(pick.id)||runtime.activeSpec?.()||{},b=budget(pick.id),absoluteMs=Math.round(clamp(Math.max(900000,b.timeoutMs,Number(spec.healthTimeoutMs||0)),300000,900000));
 let idleTimer=0,hardTimer=0,watchReject=null,lastPhase='starting',stalled=false;
 const arm=phase=>{lastPhase=String(phase||lastPhase||'working');clearTimeout(idleTimer);idleTimer=setTimeout(()=>{stalled=true;try{runtime.shutdown?.({reason:'chat-stage-stalled'})}catch{}watchReject?.(Object.assign(new Error(`Local model stopped because ${lastPhase} made no progress for ${Math.round(idleLimit(lastPhase)/1000)} seconds.`),{code:'LOCAL_CHAT_STAGE_STALLED',phase:lastPhase}))},idleLimit(lastPhase))};
 const watchdog=new Promise((_,reject)=>{watchReject=reject;arm('starting')});
 const progress=p=>{arm(p?.phase||lastPhase);try{onProgress?.({...p,watchdogMs:idleLimit(p?.phase||lastPhase),absoluteTimeoutMs:absoluteMs,runtimeFallbackOwned:Boolean(runtime?.stalledWebGPUFallback)})}catch{}};
 const token=t=>{arm('generating');try{onToken?.(t)}catch{}};
 const request=runtime.generate({messages:[{role:'system',content:systemPrompt},...messages],maxNewTokens:b.maxNewTokens,promptTokenBudget:Math.min(1024,Math.max(640,Number(spec.workingContextTokens||1024))),temperature:spec.generation?.nonThinkingTemperature??.7,thinking:false,timeoutMs:absoluteMs,stream:true,executionProfile:'interactive',onToken:token,onProgress:progress});
 const hardTimeout=new Promise((_,reject)=>{hardTimer=setTimeout(()=>{stalled=true;try{runtime.shutdown?.({reason:'chat-absolute-timeout'})}catch{}reject(Object.assign(new Error(`Local interactive generation exceeded its ${Math.round(absoluteMs/1000)} second overall recovery budget.`),{code:'LOCAL_CHAT_ABSOLUTE_TIMEOUT',phase:lastPhase}))},absoluteMs+2000)});
 try{return await Promise.race([request,watchdog,hardTimeout])}catch(error){if(stalled||error?.code==='LOCAL_MODEL_TIMEOUT'||error?.code==='LOCAL_CHAT_STAGE_STALLED'||error?.code==='LOCAL_CHAT_ABSOLUTE_TIMEOUT')try{runtime.shutdown?.({reason:error?.code||'chat-recovery'})}catch{}throw error}finally{clearTimeout(idleTimer);clearTimeout(hardTimer);watchReject=null}
}
globalThis.CivweaveLocalChatRuntimeV295=Object.freeze({version:VERSION,downloadedLocalDirect:true,thinkingDisabled:true,streaming:true,boundedRecovery:true,boundedStartup:true,startupProgress:true,singleBootstrapFlight:true,stageAwareWatchdog:true,runtimeOwnedWebGPUFallback:true,progressExtendsColdStart:true,coldStartBenchmarkOptOut:true,windowsWebGPUGrace:true,freshWorkerFallback:true,phaseAwareErrors:true,promptBudgetEnforced:true,terminalCancellation:true,settingsTeardown:true,selected,budget,ready,generate});
})();
