(()=>{
'use strict';
const VERSION='1.0.7-context-plan-composer-v198';
const ADAPTER_URL='/app/models/all-minilm-l6-v2/adapter.js';
const root=globalThis;
if(root.CommonweaveContextPlanComposerV198?.version===VERSION)return;
const clean=(value,max=12000)=>String(value??'').trim().slice(0,max);
const clone=value=>typeof structuredClone==='function'?structuredClone(value):JSON.parse(JSON.stringify(value));
const words=value=>clean(value).toLowerCase().match(/[a-z0-9][a-z0-9'’-]*/g)||[];
const STOP=new Set('the a an and or but if then this that these those to of in on for from with by as is are was were be been being it its they them their you your we our i me my can could should would may might will do does did have has had about into through across at not no yes'.split(/\s+/));
const meaningful=value=>[...new Set(words(value).filter(word=>word.length>2&&!STOP.has(word)))];
const overlap=(left,right)=>{const a=new Set(meaningful(left)),b=new Set(meaningful(right));if(!a.size||!b.size)return 0;let hit=0;a.forEach(item=>{if(b.has(item))hit+=1});return hit/Math.sqrt(a.size*b.size)};
function hash(value){let h=2166136261;for(const char of clean(value,24000)){h^=char.charCodeAt(0);h=Math.imul(h,16777619)}return(h>>>0).toString(36)}
const now=()=>new Date().toISOString();
const titleCase=value=>clean(value).replace(/\s+/g,' ').replace(/^./,char=>char.toUpperCase());
const REWARD_COPY='Generative models earn 50% more Buttons, Acorns, and Skill XP, have no daily task cap, and can earn review bonuses.';
const POOLS=Object.freeze({
  learning:[
    {id:'learn-outcome',role:'outcome',weight:5,title:'Define the capability and its real use',prompt:'Describe what the learner must be able to do, explain, decide, or make in the real setting.',deliverable:'A capability statement tied to a real situation.',evidence:['Capability statement','Use-case note'],criteria:['The result uses an observable verb.','The situation where the skill matters is named.'],tags:'learn outcome capability goal real use'},
    {id:'learn-diagnostic',role:'diagnostic',weight:4,title:'Map prior knowledge and likely gaps',prompt:'Complete a small diagnostic that separates existing knowledge, misconceptions, and unknowns.',deliverable:'A gap map with known, uncertain, and untested areas.',evidence:['Diagnostic response','Gap map'],criteria:['At least one existing strength is identified.','At least two uncertainties or misconceptions are recorded.'],tags:'diagnostic prior knowledge gaps misconception baseline'},
    {id:'learn-source-map',role:'foundation',weight:4,title:'Build a source-backed foundation',prompt:'Study a compact source set and extract the concepts, vocabulary, examples, and cautions needed for the capability.',deliverable:'A source map with claims linked to references.',evidence:['Source map','Annotated notes'],criteria:['Every major claim points to a source.','Conflicting or uncertain information is marked.'],tags:'research sources references concepts vocabulary foundation'},
    {id:'learn-worked-example',role:'guided-practice',weight:5,title:'Complete and explain a worked example',prompt:'Follow one complete example while explaining why each decision is made.',deliverable:'A worked example with decision notes.',evidence:['Worked artifact','Decision notes'],criteria:['The example reaches a checkable result.','The learner explains at least three decisions in their own words.'],tags:'guided practice worked example explain decisions'},
    {id:'learn-contrast',role:'guided-practice',weight:4,title:'Compare a strong and weak example',prompt:'Contrast two examples to identify quality signals, common failures, and repair strategies.',deliverable:'A comparison rubric with repair notes.',evidence:['Comparison table','Repair notes'],criteria:['At least three quality signals are named.','Each weak point has a plausible repair.'],tags:'compare examples quality mistakes rubric repair'},
    {id:'learn-independent',role:'independent-demo',weight:6,title:'Produce a fresh independent demonstration',prompt:'Complete a new example without copying the guided solution, using the intended tools and constraints.',deliverable:'An independent artifact in the target context.',evidence:['Fresh artifact','Process record'],criteria:['The artifact is materially different from the guided example.','The result satisfies the stated capability criteria.'],tags:'independent demonstration transfer fresh artifact practice'},
    {id:'learn-teachback',role:'independent-demo',weight:5,title:'Teach the idea back to another person',prompt:'Explain the capability, demonstrate it, and answer likely questions without reading a script.',deliverable:'A teach-back recording or written explainer.',evidence:['Teach-back artifact','Question log'],criteria:['The explanation uses original wording.','Questions or misunderstandings are answered accurately.'],tags:'teach explain communicate demonstrate questions'},
    {id:'learn-assessment',role:'assessment',weight:6,title:'Pass a criterion-based assessment',prompt:'Complete a mixed assessment that checks recall, application, judgment, and error correction.',deliverable:'A scored assessment with targeted corrections.',evidence:['Assessment responses','Correction record'],criteria:['Required criteria meet the defined threshold.','Missed items are corrected with reasons, not just replaced answers.'],tags:'assessment test rubric criteria corrections mastery'},
    {id:'learn-reflection',role:'reflection',weight:4,title:'Review evidence and choose the next learning edge',prompt:'Compare the starting state with current evidence and decide what to reinforce, apply, or investigate next.',deliverable:'An evidence-based reflection and next practice target.',evidence:['Before-and-after note','Next-practice decision'],criteria:['The reflection cites specific evidence.','The next target addresses a demonstrated gap or opportunity.'],tags:'reflection evidence review reinforce next practice'}
  ],
  project:[
    {id:'project-outcome',role:'outcome',weight:5,title:'Define the observable project outcome',prompt:'Describe the finished result, its users, the problem it changes, and what is outside the first release.',deliverable:'A one-page outcome and scope brief.',evidence:['Outcome brief','Out-of-scope list'],criteria:['A stranger can tell what will exist when the work is done.','The first release has explicit boundaries.'],tags:'project outcome users scope deliverable result'},
    {id:'project-constraints',role:'constraints',weight:4,title:'Map constraints, dependencies, and risks',prompt:'Record time, tools, skills, materials, access, safety, legal, and collaboration constraints before sequencing work.',deliverable:'A constraint and dependency map.',evidence:['Constraint map','Risk register'],criteria:['Blocking dependencies have owners or resolution paths.','High-impact risks have a test or mitigation.'],tags:'constraints dependencies risks tools time budget legal safety'},
    {id:'project-research',role:'discovery',weight:4,title:'Inspect references and existing solutions',prompt:'Study relevant examples, prior work, user needs, and technical or material options without copying blindly.',deliverable:'A reference brief with decisions and rejected options.',evidence:['Reference set','Decision notes'],criteria:['At least three relevant references are compared.','The brief explains what will and will not be borrowed.'],tags:'research references users examples options discovery'},
    {id:'project-architecture',role:'design',weight:5,title:'Design the working structure before building',prompt:'Map components, responsibilities, data or material flows, interfaces, and failure points.',deliverable:'A system, service, or build diagram.',evidence:['Architecture diagram','Interface notes'],criteria:['Every major component has a responsibility.','Critical handoffs and failure paths are visible.'],tags:'design architecture components flow interface system'},
    {id:'project-prototype',role:'prototype',weight:6,title:'Build a bounded proof of the core promise',prompt:'Create the smallest complete prototype that exercises the central behavior from beginning to end.',deliverable:'A runnable, viewable, or physically testable prototype.',evidence:['Prototype artifact','Run record'],criteria:['The core behavior can be exercised end to end.','The prototype tests a meaningful uncertainty, not merely setup.'],tags:'prototype vertical slice proof core behavior test'},
    {id:'project-build',role:'implementation',weight:6,title:'Complete the first integrated build',prompt:'Turn the validated structure into an integrated version with the required content, interfaces, and safeguards.',deliverable:'An integrated first release candidate.',evidence:['Build artifact','Change log'],criteria:['Required components work together.','Known limitations are recorded rather than hidden.'],tags:'implement build integrate release candidate artifact'},
    {id:'project-validate',role:'validation',weight:6,title:'Validate against users and acceptance criteria',prompt:'Run realistic tests, collect observations, and distinguish defects from preference changes.',deliverable:'A test report with prioritized revisions.',evidence:['Test results','User observations','Revision list'],criteria:['Every acceptance criterion has a result.','Revisions are prioritized by evidence and impact.'],tags:'validate test users acceptance criteria revise'},
    {id:'project-ship',role:'delivery',weight:5,title:'Deliver, document, and hand off the result',prompt:'Package the result, document operation and maintenance, and confirm the recipient can use it.',deliverable:'A delivered release with an operating guide.',evidence:['Release record','Operating guide','Handoff confirmation'],criteria:['The intended recipient can access and use the result.','Ownership, maintenance, and rollback are explicit.'],tags:'ship deliver release document handoff maintenance'},
    {id:'project-retro',role:'reflection',weight:4,title:'Run a post-project evidence review',prompt:'Compare outcome, effort, defects, and lessons, then decide what to preserve or change next time.',deliverable:'A retrospective with concrete process changes.',evidence:['Outcome comparison','Retrospective notes'],criteria:['Claims are tied to records or observations.','At least one practice is preserved and one is revised.'],tags:'retrospective review evidence lessons process'}
  ],
  market:[
    {id:'market-need',role:'need',weight:5,title:'Specify the exact need or offer',prompt:'Name the item, service, access, or exchange in concrete terms and explain the use it must support.',deliverable:'A precise need-or-offer statement.',evidence:['Specification','Use-case note'],criteria:['The request can be understood without private context.','Essential features are separated from preferences.'],tags:'market need offer item service specification use'},
    {id:'market-quantity',role:'constraints',weight:5,title:'Set quantity, timing, location, and condition',prompt:'Record how much is needed or offered, by when, where the handoff occurs, and acceptable condition.',deliverable:'A logistics and condition sheet.',evidence:['Quantity record','Timing and location note'],criteria:['Quantity and deadline are explicit.','Condition and handoff location are testable.'],tags:'quantity timing deadline location condition logistics'},
    {id:'market-terms',role:'terms',weight:6,title:'Define fair terms and a hard budget boundary',prompt:'State payment, Buttons, barter, lending, return, ownership, cancellation, and accessibility terms.',deliverable:'A terms sheet with maximum commitment.',evidence:['Terms sheet','Budget or exchange cap'],criteria:['Maximum cost or commitment is explicit.','Ownership, return, and cancellation conditions are stated.'],tags:'price budget buttons barter loan ownership return fair terms'},
    {id:'market-substitutes',role:'alternatives',weight:4,title:'List acceptable substitutes and repair paths',prompt:'Identify alternatives, borrowing, repair, reuse, local production, or partial fulfillment options.',deliverable:'A ranked substitution map.',evidence:['Substitute list','Trade-off notes'],criteria:['At least two alternatives are considered.','Trade-offs are visible rather than silently accepted.'],tags:'substitute borrow repair reuse alternatives partial fulfillment'},
    {id:'market-sources',role:'discovery',weight:5,title:'Compare candidate sources or recipients',prompt:'Evaluate availability, provenance, reliability, accessibility, total cost, and community impact.',deliverable:'A source comparison with a recommended path.',evidence:['Candidate list','Comparison table'],criteria:['At least three relevant candidates or channels are checked when available.','The recommendation includes total cost and reliability, not sticker price alone.'],tags:'sources vendors offers compare availability provenance reliability'},
    {id:'market-trust',role:'verification',weight:6,title:'Verify identity, provenance, and fulfillment conditions',prompt:'Confirm the parties, item or service condition, permissions, and proof required before commitment.',deliverable:'A verification checklist and consent record.',evidence:['Verification record','Consent or permission note'],criteria:['Material claims have inspectable support.','No consequential commitment occurs without explicit consent.'],tags:'verify trust provenance identity consent condition'},
    {id:'market-handoff',role:'handoff',weight:6,title:'Complete the exchange and record the handoff',prompt:'Confirm pickup, delivery, transfer, payment, return obligations, and receipt in one bounded event.',deliverable:'A completed handoff record.',evidence:['Receipt','Delivery or pickup confirmation'],criteria:['Both sides can tell what changed hands and when.','Outstanding obligations are recorded.'],tags:'exchange handoff delivery pickup receipt transfer'},
    {id:'market-review',role:'reflection',weight:4,title:'Review value, fairness, and remaining gaps',prompt:'Assess whether the exchange met the need, created hidden costs, or should change future requests.',deliverable:'A post-exchange review and updated preference note.',evidence:['Outcome review','Updated request guidance'],criteria:['The review compares the result with the original need.','Hidden labor, waste, or access costs are considered.'],tags:'review fairness value hidden costs outcome'}
  ]
});
const REQUIRED_ROLES=Object.freeze({learning:['outcome','diagnostic','foundation','guided-practice','independent-demo','assessment'],project:['outcome','constraints','design','prototype','validation','delivery'],market:['need','constraints','terms','discovery','verification','handoff']});
let adapterPromise=null;
async function semanticAdapter(){if(!adapterPromise)adapterPromise=import(ADAPTER_URL).catch(error=>{adapterPromise=null;throw error});return adapterPromise}
function lexicalRank(query,candidates){return candidates.map(item=>({...item,lexical:overlap(query,`${item.title} ${item.prompt} ${item.tags}`)})).sort((a,b)=>b.lexical-a.lexical||b.weight-a.weight||a.id.localeCompare(b.id))}
async function rankCandidates(query,candidates,{semantic=true,limit=12}={}){
  const lexical=lexicalRank(query,candidates),diagnostics=['Deterministic lexical ranking available.'];
  if(!semantic||meaningful(query).length<3||typeof document==='undefined')return{rows:lexical.slice(0,limit),device:'deterministic-lexical',semantic:false,diagnostics};
  try{
    const adapter=await semanticAdapter(),status=await adapter.status();
    if(!status.available){diagnostics.push('MiniLM package is not downloaded; lexical fallback used.');return{rows:lexical.slice(0,limit),device:'deterministic-lexical',semantic:false,diagnostics}}
    if(!status.ready)await adapter.prewarm({explicit:true,installIfMissing:false,timeoutMs:45000});
    const result=await adapter.rank(query,candidates.map(item=>({id:item.id,text:`${item.title}. ${item.prompt}. ${item.tags}`})),{limit:candidates.length,cacheKey:`cw198:${hash(query)}:${candidates.length}`,timeoutMs:20000});
    const semanticScores=new Map((result.matches||[]).map(item=>[item.id,Number(item.score||0)]));
    const rows=lexical.map(item=>({...item,semantic:Number(semanticScores.get(item.id)||0),score:Number(semanticScores.get(item.id)||0)*.7+item.lexical*.3})).sort((a,b)=>b.score-a.score||b.weight-a.weight||a.id.localeCompare(b.id));
    diagnostics.push(`MiniLM ranked ${semanticScores.size} template pieces on ${result.device||'local runtime'}.`);
    return{rows:rows.slice(0,limit),device:result.device||'minilm',semantic:true,diagnostics};
  }catch(error){diagnostics.push(`MiniLM remained advisory and fell back safely: ${clean(error.message,240)}`);return{rows:lexical.slice(0,limit),device:'deterministic-lexical',semantic:false,diagnostics}}
}
function planKind(value,realm='commonweave'){
  const text=clean(value).toLowerCase();
  if(realm==='living-school')return'learning';
  if(realm==='fellowfare')return'market';
  if(realm==='cerbanimo')return'project';
  if(/\b(learn|study|lesson|curriculum|course|teach|understand|practice|master|research)\b/.test(text))return'learning';
  if(/\b(need|offer|buy|borrow|trade|market|materials|supplies|resource|vendor|request|inventory)\b/.test(text))return'market';
  return'project';
}
function goalText(value){return clean(value,1600).replace(/^\s*(i\s+(want|wish|need|hope)\s+(to\s+)?|we\s+(want|need)\s+(to\s+)?|please\s+|help\s+me\s+)/i,'').replace(/[.!?]+$/,'').trim()||'move the intention forward'}
function chooseByRoles(rows,kind,count=6){
  const roles=REQUIRED_ROLES[kind],chosen=[];
  for(const role of roles){const item=rows.find(row=>row.role===role&&!chosen.some(value=>value.id===row.id));if(item)chosen.push(item)}
  for(const item of rows){if(chosen.length>=count)break;if(!chosen.some(value=>value.id===item.id))chosen.push(item)}
  return chosen.slice(0,Math.max(5,Math.min(8,count)));
}
function taskFrom(piece,goal,kind,index,mode){
  const objective=`For “${goal},” ${piece.prompt.charAt(0).toLowerCase()}${piece.prompt.slice(1)}`;
  const task={id:`${kind}-${hash(`${goal}|${piece.id}`)}`,templateId:piece.id,role:piece.role,title:piece.title,objective,deliverable:piece.deliverable,evidence:clone(piece.evidence),acceptanceCriteria:clone(piece.criteria),complexityPoints:Math.max(4,Number(piece.weight||4)),sequence:index+1,status:'ready',microsteps:[`Open the materials and context needed for ${piece.title.toLowerCase()}.`,`Produce the stated deliverable.`,`Check every acceptance criterion and attach evidence.`]};
  return root.CommonweaveRewardPolicyV198?.decorateTask?.(task,mode)||task;
}
async function composePath({text='',kind=planKind(text),realm,mode='deterministic',semantic=true,count=6}={}){
  const goal=goalText(text),resolved=POOLS[kind]?kind:'project',targetRealm=realm||(resolved==='learning'?'living-school':resolved==='market'?'fellowfare':'cerbanimo'),ranked=await rankCandidates(`${goal} ${resolved} ${targetRealm}`,POOLS[resolved],{semantic,limit:POOLS[resolved].length}),selected=chooseByRoles(ranked.rows,resolved,count),tasks=selected.map((piece,index)=>taskFrom(piece,goal,resolved,index,mode));
  const title=resolved==='learning'?`Learning path: ${titleCase(goal)}`:resolved==='market'?`Market request: ${titleCase(goal)}`:`Project plan: ${titleCase(goal)}`;
  return{id:`path-${resolved}-${hash(goal)}`,type:resolved==='learning'?'learning':resolved==='market'?'material-acquirement':'skilled-labor',kind:resolved,realm:targetRealm,title,purpose:resolved==='learning'?`Build usable capability for ${goal} through sources, practice, transfer, and assessment.`:resolved==='market'?`Turn ${goal} into a fair, specific, verifiable request or offer with a recorded handoff.`:`Turn ${goal} into an observable result through bounded design, build, validation, and delivery.`,steps:tasks.map(task=>task.title),tasks,completionCriteria:resolved==='learning'?`The capability is independently demonstrated in its intended context and assessed against explicit criteria.`:resolved==='market'?`The need or offer has explicit terms, verified parties or sources, a completed handoff record, and a fairness review.`:`The promised result is delivered, tested against every acceptance criterion, and handed off with inspectable evidence.`,evidence:[...new Set(tasks.flatMap(task=>task.evidence))].slice(0,12),status:'draft',planning:{engine:ranked.semantic?'deterministic+minilm':'deterministic-lexical',semantic:ranked.semantic,device:ranked.device,templateIds:selected.map(item=>item.id),diagnostics:ranked.diagnostics},rewardPolicy:{mode,dailyEligibleTaskLimit:mode==='deterministic'?3:null,promotion:root.CommonweaveRewardPolicyV198?.PROMOTION||{headline:'Generative models earn 50% more rewards',detail:REWARD_COPY}}};
}
function transcript({text='',history=[]}={}){return[...(Array.isArray(history)?history:[]).filter(item=>item?.role==='user').map(item=>clean(item.text||item.content,1800)),clean(text,1800)].filter(Boolean).slice(-12).join('\n')}
async function enhancePlan(base,{text='',history=[],context={},mode='deterministic'}={}){
  const plan=clone(base||{}),query=transcript({text,history})||clean(plan.wish||plan.title),paths=Array.isArray(plan.paths)?plan.paths:[],enhanced=[];
  for(const source of paths.slice(0,3)){
    const kind=planKind(`${query} ${source.title||''} ${source.purpose||''}`,source.realm),composed=await composePath({text:query,kind,realm:source.realm,mode,semantic:true,count:kind==='market'?7:6});
    enhanced.push({...source,...composed,id:source.id||composed.id,title:source.title&&source.title.length>12?source.title:composed.title,purpose:source.purpose&&source.purpose.length>20?source.purpose:composed.purpose});
  }
  if(!enhanced.length)enhanced.push(await composePath({text:query,kind:planKind(query,context?.routingAnswer?.system),mode,semantic:true,count:6}));
  plan.paths=enhanced;plan.updatedAt=now();plan.planning={schema:'commonweave.context-template-planning.v1',version:VERSION,authority:'advisory-structure-only',engine:enhanced.some(path=>path.planning?.semantic)?'deterministic+minilm':'deterministic-lexical',model:enhanced.some(path=>path.planning?.semantic)?'Xenova/all-MiniLM-L6-v2':'none',semanticUsed:enhanced.some(path=>path.planning?.semantic),templatePieces:enhanced.flatMap(path=>path.planning?.templateIds||[]),rewardPolicy:{deterministicDailyEligibleTasks:3,generativeRewardPromotion:REWARD_COPY},diagnostics:[...new Set(enhanced.flatMap(path=>path.planning?.diagnostics||[])),'MiniLM ranks template pieces only; deterministic evidence rules control reward eligibility and completion.']};
  plan.assumptions=[...(plan.assumptions||[]).filter(Boolean),'Template selection is advisory; no task is completed and no reward is minted by semantic similarity.'];
  return plan;
}
function format(plan){const paths=(plan.paths||[]).map((path,index)=>{const tasks=(path.tasks||[]).map((task,i)=>`  ${i+1}. ${task.title}\n     Deliverable: ${task.deliverable}\n     Evidence: ${(task.evidence||[]).join(', ')}`).join('\n');return`${index+1}. ${path.title} · ${path.realm}\n${path.purpose}\n${tasks}\nCompletion: ${path.completionCriteria}`}).join('\n\n');return[`I built and saved a reviewable weave for “${plan.title}.”`,'','Governing outcome',plan.outcome||plan.wish,'',paths,'',REWARD_COPY,'','The weave is in REVIEW. Inspect, revise, or explicitly activate it before any consequential action.'].join('\n').trim()}
async function maybeCreate({text='',history=[],context={},force=false,mode='deterministic'}={}){
  const planner=root.CommonweaveIntentionPlanner;if(!planner?.buildPlan||!planner?.persist)return null;if(!planner.shouldCreate?.({text,history,context,force}))return null;
  const built=planner.buildPlan({text,history,context}),plan=await enhancePlan(built,{text,history,context,mode}),item=planner.persist(plan);item.plan=plan;
  const approvalGate={kind:'intention-activation',planId:item.id,state:item.state||'review',required:true,actions:['review','revise','activate']};
  return{item,plan,response:{answer:format(plan),choice:{mode:'Plan',system:'commonweave',room:plan.routing?.room||'commonweave.quad',nextAction:'Review the deliverables, evidence, acceptance criteria, and reward eligibility before activation.'},assumptions:plan.assumptions,requiresConsent:true,confidence:.96,approvalGate}};
}
async function enrichAction(action,text,system,context={},mode='deterministic'){
  if(!action||typeof action!=='object')return action;const kind=planKind(text,system),path=await composePath({text,kind,realm:system,mode,semantic:true,count:kind==='market'?7:6});
  action.templatePlan=path;action.checkpoints=path.tasks.map(task=>task.title);action.acceptanceCriteria=[...new Set([...(action.acceptanceCriteria||[]),...path.tasks.flatMap(task=>task.acceptanceCriteria||[])])].slice(0,16);action.evidence=[...new Set([...(action.evidence||[]),...path.tasks.flatMap(task=>task.evidence||[])])].slice(0,16);action.rewardPolicy=path.rewardPolicy;action.updatedAt=now();return action;
}
function status(){return{version:VERSION,templateCounts:Object.fromEntries(Object.entries(POOLS).map(([key,value])=>[key,value.length])),rewardCopy:REWARD_COPY,semanticAuthority:'ranking-only'}}
root.CommonweaveContextPlanComposerV198=Object.freeze({version:VERSION,POOLS,REQUIRED_ROLES,REWARD_COPY,planKind,lexicalRank,rankCandidates,composePath,enhancePlan,maybeCreate,enrichAction,format,status});
try{root.dispatchEvent?.(new CustomEvent('commonweave:context-plan-composer-ready',{detail:status()}))}catch{}
})();
