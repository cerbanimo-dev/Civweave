import * as rubric from '../../services/living-school/modules/rubric-engine.mjs';
import * as gate from '../../services/living-school/modules/project-gate.mjs';
import * as bridge from '../../services/living-school/modules/cerbanimo-bridge.mjs';
import{state,replaceState,persist,toast,award,activeModule,progressFor,availablePaths,fields,field,formFor,clean,uid,now,copy,readJson,writeJson,STATE_KEY,OUTBOX_KEY,freshState,normalizeState,generateSchool}from'./living-school-cleanroom-core-v218.mjs';

export const actions={
  'apply-path':async target=>{const id=clean(field('pathId',formFor(target)),180),path=availablePaths().find(item=>item.id===id);if(!path)throw new Error('That path is unavailable.');const s=state();s.activePathId=id;s.pathContext=path;persist('learning-path-selected',{pathId:id});toast(`Using ${path.title}`)},
  'generate-curriculum':async target=>{const data=fields(target,['title','capability','level','count','mode','modelRoute','proof']);if(!clean(data.capability))throw new Error('Name an observable capability.');const s=state(),school=await generateSchool(data),old=s.school?.modules||[],nextProgress={};school.modules.forEach((item,index)=>nextProgress[item.id]=s.progress[old[index]?.id]||progressFor(item.id));s.school=school;s.activeModuleId=school.modules[0].id;s.progress=nextProgress;s.settings={...s.settings,modelRoute:data.modelRoute,mode:data.mode};persist('curriculum-generated',{schoolId:school.id});toast(school.generation.fallback?'Moss built a local fallback.':'Moss generated the curriculum.')},
  'select-module':async target=>{const id=clean(target.dataset.moduleId,180),s=state();if(s.school?.modules?.some(item=>item.id===id)){s.activeModuleId=id;persist('module-selected',{moduleId:id})}},
  'save-lesson':async target=>{const module=activeModule(),note=clean(field('lessonNote',formFor(target)),8000);if(!module||!note)throw new Error('Add a working note.');const s=state(),progress=progressFor(module.id),first=!progress.lessonComplete;s.progress[module.id]={...progress,lessonComplete:true,note,evidence:[...(progress.evidence||[]),{id:uid('evidence'),type:'lesson-note',detail:{note},at:now()}].slice(-50)};if(first)award(5,'Lesson evidence recorded',module.id);persist('lesson-completed',{moduleId:module.id});toast('Lesson evidence saved.')},
  'evaluate-assessment':async target=>{const module=activeModule(),scope=formFor(target),answer=clean(field('assessmentAnswer',scope),12000),choice=scope.querySelector('[name="evidenceChoice"]:checked')?.value;if(!module||!answer)throw new Error('Write an answer.');const evaluation=rubric.evaluateShortAnswer?.({prompt:module.question,response:answer,lessonExcerpt:module.lesson,criteria:[{id:'principle',label:'Explain the principle',points:3,role:'principle',required:true},{id:'application',label:'Apply it',points:3,role:'application',required:true},{id:'evidence',label:'Name evidence',points:4,role:'evidence',required:true}],minWords:18,maxWords:280})||{ok:answer.split(/\s+/).length>=18,uncertain:false,score:75,feedback:'Local review.'};const mc=choice==='b',passed=mc&&evaluation.ok&&!evaluation.uncertain,s=state(),progress=progressFor(module.id),first=passed&&!progress.assessmentPassed,attempt={id:uid('attempt'),answer,mc,score:Math.round(Number(evaluation.score||0)*.8+(mc?20:0)),passed,feedback:mc?evaluation.feedback:`Choose inspectable evidence. ${evaluation.feedback}`,at:now()};s.progress[module.id]={...progress,assessmentPassed:passed,attempts:[...(progress.attempts||[]),attempt].slice(-12),evidence:[...(progress.evidence||[]),{id:uid('evidence'),type:'assessment',detail:attempt,at:now()}].slice(-50)};if(first)award(15,'Module assessment passed',module.id);persist('assessment-evaluated',{moduleId:module.id,passed});toast(passed?'Assessment passed.':'Saved for revision.')},
  'add-source':async target=>{const data=fields(target,['sourceTitle','sourceUrl','sourceNotes']);if(!clean(data.sourceTitle))throw new Error('Add a source title.');const s=state();s.sources=[...s.sources,{id:uid('source'),title:clean(data.sourceTitle,300),url:clean(data.sourceUrl,1000),notes:clean(data.sourceNotes,3000),at:now()}].slice(-100);persist('source-added');toast('Source attached.')},
  'remove-source':async target=>{const s=state();s.sources=s.sources.filter(item=>item.id!==target.dataset.sourceId);persist('source-removed')},
  'save-practicum':async target=>{const data=fields(target,['practicumTitle','practicumArtifact','practicumProof']);if(!clean(data.practicumTitle)||!clean(data.practicumArtifact))throw new Error('Add a title and artifact.');const s=state();s.practicum={id:s.practicum?.id||uid('practicum'),title:clean(data.practicumTitle,300),artifact:clean(data.practicumArtifact,5000),proof:clean(data.practicumProof,5000),updatedAt:now()};s.projectGate=gate.normalizeProjectGate?.({...s.projectGate,status:'ready-to-submit',projectRef:s.practicum.id,brief:s.practicum,updatedAt:now()})||s.projectGate;persist('practicum-saved');toast('Practicum saved.')},
  'create-handoff':async()=>{const s=state();if(!s.practicum)throw new Error('Save a practicum first.');const request={schema:'commonweave.cerbanimo.project-request.v1',requestId:uid('request'),sourceSystem:'living-school',targetSystem:'cerbanimo',capability:s.school?.capability||'',practicum:copy(s.practicum),createdAt:now(),status:'ready'},outbox=readJson(OUTBOX_KEY,[]);writeJson(OUTBOX_KEY,[...(Array.isArray(outbox)?outbox:[]),request].slice(-100));s.projectGate=gate.normalizeProjectGate?.({...s.projectGate,status:'submitted',requestId:request.requestId,updatedAt:now()})||s.projectGate;persist('cerbanimo-handoff-created');toast('Cerbanimo request created.')},
  'import-receipt':async target=>{const text=clean(field('receiptJson',formFor(target)),30000);if(!text)throw new Error('Paste a receipt.');const raw=JSON.parse(text),receipt=bridge.normalizeProjectReceipt?.(raw)||raw,s=state(),result=gate.applyReceipt?.(s.projectGate,receipt)||{changed:true,gate:{...s.projectGate,status:receipt.status||'accepted',lastReceipt:receipt}};if(!result.changed)throw new Error(`Receipt rejected: ${result.reason||'invalid'}`);s.projectGate=result.gate;s.receipts=[...s.receipts,receipt].slice(-100);persist('cerbanimo-receipt-applied');toast(`Project gate: ${result.gate.status}`)},
  'open-cerbanimo':async()=>location.assign('/app/cabinet-only-v144.html?system=cerbanimo&from=living-school&capability=cerbanimo.create-quest'),
  'evaluate-final':async target=>{const s=state();if(!(gate.canUnlockFinalTest?.(s.projectGate)||Boolean(s.projectGate?.status==='accepted'&&s.projectGate?.lastReceipt)))throw new Error('An accepted Cerbanimo receipt is required.');const answer=clean(field('finalAnswer',formFor(target)),16000);if(!answer)throw new Error('Write the final explanation.');const evaluation=rubric.evaluateShortAnswer?.({prompt:'Explain capability, evidence, uncertainty, and revision.',response:answer,lessonExcerpt:`Capability: ${s.school?.capability||''}. Artifact: ${s.practicum?.artifact||''}.`,criteria:[{id:'capability',label:'Capability',points:3,role:'principle',required:true},{id:'evidence',label:'Evidence',points:4,role:'evidence',required:true},{id:'limits',label:'Limits',points:3,role:'action',required:true}],minWords:30,maxWords:420})||{ok:answer.split(/\s+/).length>=30,uncertain:false,score:80,feedback:'Local final review.'};const passed=evaluation.ok&&!evaluation.uncertain,first=passed&&!s.final?.passed;s.final={id:uid('final'),answer,score:Number(evaluation.score||0),passed,feedback:evaluation.feedback,receiptId:s.projectGate.lastReceipt?.receiptId,at:now()};if(first)award(25,'Final competency passed',s.final.id);persist('final-assessment-evaluated',{passed});toast(passed?'Final competency passed.':'Saved for revision.')},
  'issue-credential':async()=>{const s=state();if(!s.final?.passed)throw new Error('Pass the final competency check first.');s.credential=s.credential||{schema:'commonweave.learning-credential.v1',credentialId:uid('credential'),title:s.school?.title||'Living School capability',capability:s.school?.capability||'',learner:s.passport.displayName,schoolId:s.school?.id||'',receiptId:s.projectGate.lastReceipt?.receiptId||'',issuedAt:now()};persist('credential-issued',{credentialId:s.credential.credentialId});toast('Portable credential issued.')},
  'save-profile':async target=>{const s=state();s.passport={...s.passport,displayName:clean(field('displayName',formFor(target)),100)||'Local learner'};persist('passport-updated')},
  'copy-record':async()=>{if(!navigator.clipboard?.writeText)throw new Error('Clipboard unavailable.');await navigator.clipboard.writeText(JSON.stringify({...state(),exportedAt:now()},null,2));toast('Record copied.')},
  'restore-record':async target=>{const restored=JSON.parse(clean(field('restoreJson',formFor(target)),500000));writeJson(`${STATE_KEY}.backup.${Date.now()}`,state());replaceState(normalizeState(restored));persist('cleanroom-record-restored');toast('Record restored without legacy navigation state.')},
  'reset-record':async()=>{writeJson(`${STATE_KEY}.backup.${Date.now()}`,state());replaceState(freshState());persist('cleanroom-record-reset');toast('Reset complete. Backup preserved.')},
  'open-ai-settings':async()=>{if(globalThis.CommonweaveFamilyAILoaderV105?.openSettings)return globalThis.CommonweaveFamilyAILoaderV105.openSettings();throw new Error('AI settings are still loading.')}
};
