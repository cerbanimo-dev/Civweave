(()=>{
'use strict';
const SETTINGS_KEY='commonweave.universal-ai.v127';
const SESSION_KEY='commonweave-model-session';
const MODEL_ID='Xenova/all-MiniLM-L6-v2';
const MANIFEST='/app/models/all-minilm-l6-v2/model-manifest.json';
const GEMINI_ENDPOINT='https://generativelanguage.googleapis.com/v1beta';
const GEMINI_MODEL='gemini-3.5-flash-lite';
const OLLAMA_ENDPOINT='http://127.0.0.1:11434/api/chat';
const parse=(value,fallback)=>{try{const parsed=JSON.parse(value);return parsed==null?fallback:parsed}catch{return fallback}};
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const runtime=()=>globalThis.CommonweaveModelRuntime||null;
function providerName(value){const p=String(value||'bundled').toLowerCase();if(['bundled','packaged','reflex','minilm','local-reflex','smollm2','deterministic','browser'].includes(p))return'bundled';if(['openai','compatible','openai-compatible'].includes(p))return'openai-compatible';if(p==='local-api')return'ollama';return['gemini','ollama','openai-compatible','hosted'].includes(p)?p:'bundled'}
function existingKey(){try{const key=runtime()?.readSharedConfig?.('interactive')?.apiKey;if(key)return key}catch{}return parse(sessionStorage.getItem(SESSION_KEY),{}).apiKey||''}
function readState(){
  const legacy=parse(localStorage.getItem(SETTINGS_KEY),{route:'bundled',model:MODEL_ID,endpoint:MANIFEST,consent:false});let profiles={interactive:null,agentic:null,agenticEnabled:false},shared=null;
  try{profiles=runtime()?.readModelProfiles?.()||profiles;shared=runtime()?.readSharedConfig?.('interactive')||null}catch{}
  const interactive=profiles.interactive||shared||legacy;const route=providerName(interactive?.provider||interactive?.route);
  return {route,consent:Boolean(interactive?.externalConsent??legacy.consent),hasKey:Boolean(existingKey()),agenticEnabled:Boolean(profiles.agenticEnabled&&profiles.agentic),geminiModel:route==='gemini'?(interactive.model||GEMINI_MODEL):GEMINI_MODEL,geminiEndpoint:route==='gemini'?(interactive.endpoint||GEMINI_ENDPOINT):GEMINI_ENDPOINT,ollamaModel:route==='ollama'?(interactive.model||'llama3.2'):'llama3.2',ollamaEndpoint:route==='ollama'?(interactive.endpoint||OLLAMA_ENDPOINT):OLLAMA_ENDPOINT,compatibleModel:route==='openai-compatible'?(interactive.model||'local-model'):'local-model',compatibleEndpoint:route==='openai-compatible'?(interactive.endpoint||''):''};
}
function markup({inline=false}={}){return `<form class="cw-ai-settings-form${inline?' cw-ai-inline-form':''}" data-reflex-settings-form>
${inline?'':`<header><div><small>UNIVERSAL AI SETTINGS</small><h2>Choose the Compass mind</h2></div><button class="cw127-close" type="button" data-close aria-label="Close">×</button></header>`}
${inline?'<small class="kicker">UNIVERSAL AI SETTINGS</small><h2>Choose the Compass mind</h2>':''}
<label>Route<select name="route"><option value="bundled">Onboard Semantic Reflex</option><option value="gemini">Google Gemini</option><option value="ollama">Ollama or local API</option><option value="openai-compatible">OpenAI-compatible endpoint</option></select></label>
<section class="cw-ai-route-panel" data-route-panel="bundled">
<p><b>MiniLM Semantic Reflex</b> routes and retrieves locally without generating tokens. Commonweave composes the answer from canonical state, consent rules, and matched response patterns.</p>
<div class="cw-ai-package-state" data-package-state>Checking the local semantic package…</div>
<label>Semantic model<input value="${MODEL_ID}" readonly></label><label>Package manifest<input value="${MANIFEST}" readonly></label>
<div class="cw-ai-fallback-contract"><b>Immediate fallback</b><span>Every call has a lexical reflex available in milliseconds. MiniLM improves semantic matching after it warms; it never blocks the chat waiting to generate prose.</span></div>
<div class="cw-ai-actions"><button type="button" data-check-package>Check local package</button><button type="button" data-benchmark>Run reflex speed trial</button></div>
<output class="cw-ai-test-status" data-test-status role="status">The semantic package has not been checked yet.</output><div class="cw-ai-benchmark" data-benchmark-output hidden></div>
</section>
<section class="cw-ai-route-panel" data-route-panel="gemini" hidden><label>Gemini API key<input name="apiKey" type="password" autocomplete="off" spellcheck="false"></label><label>Model<input name="geminiModel" value="${GEMINI_MODEL}"></label><label>Google endpoint<input name="geminiEndpoint" value="${GEMINI_ENDPOINT}"></label><label class="cw-ai-consent"><input name="geminiConsent" type="checkbox"><span>Allow prompts to leave this device for Gemini.</span></label><label class="cw-ai-agent-toggle"><input name="agenticEnabled" type="checkbox"><span><b>Use Antigravity for agentic work</b><small>The local semantic reflex remains the immediate fallback.</small></span></label><div class="cw-ai-actions"><button type="button" data-test-provider>Test Gemini</button></div><output class="cw-ai-test-status" data-test-status role="status"></output></section>
<section class="cw-ai-route-panel" data-route-panel="ollama" hidden><label>Model<input name="ollamaModel" value="llama3.2"></label><label>Endpoint<input name="ollamaEndpoint" value="${OLLAMA_ENDPOINT}"></label><div class="cw-ai-actions"><button type="button" data-test-provider>Test local API</button></div><output class="cw-ai-test-status" data-test-status role="status"></output></section>
<section class="cw-ai-route-panel" data-route-panel="openai-compatible" hidden><label>Model<input name="compatibleModel" value="local-model"></label><label>Endpoint<input name="compatibleEndpoint"></label><label>API key<input name="compatibleApiKey" type="password" autocomplete="off"></label><label class="cw-ai-consent"><input name="compatibleConsent" type="checkbox"><span>Allow prompts to leave this device when the endpoint is remote.</span></label><div class="cw-ai-actions"><button type="button" data-test-provider>Test endpoint</button></div><output class="cw-ai-test-status" data-test-status role="status"></output></section>
<p>Provider preferences stay local. Credentials remain session-only.</p><menu class="cw-ai-actions"><button type="submit">Save settings</button></menu><output data-save-status role="status"></output></form>`}
function sync(form){const route=providerName(form.route.value);form.querySelectorAll('[data-route-panel]').forEach(panel=>panel.hidden=panel.dataset.routePanel!==route)}
function fill(form){const s=readState();form.route.value=s.route;form.geminiModel.value=s.geminiModel;form.geminiEndpoint.value=s.geminiEndpoint;form.geminiConsent.checked=s.consent;form.agenticEnabled.checked=s.agenticEnabled;form.ollamaModel.value=s.ollamaModel;form.ollamaEndpoint.value=s.ollamaEndpoint;form.compatibleModel.value=s.compatibleModel;form.compatibleEndpoint.value=s.compatibleEndpoint;form.compatibleConsent.checked=s.consent;sync(form);checkPackage(form)}
function setupFrom(form){const route=providerName(form.route.value),prior=existingKey();if(route==='bundled')return{route,key:'',consent:false,agentic:false,interactive:{route:'bundled',provider:'bundled',model:MODEL_ID,endpoint:MANIFEST,externalConsent:false},agenticConfig:null};if(route==='gemini'){const key=form.apiKey.value.trim()||prior,consent=form.geminiConsent.checked,agentic=form.agenticEnabled.checked,interactive={route:'gemini',provider:'gemini',model:form.geminiModel.value.trim()||GEMINI_MODEL,endpoint:form.geminiEndpoint.value.trim()||GEMINI_ENDPOINT,externalConsent:consent};return{route,key,consent,agentic,interactive,agenticConfig:agentic?{route:'gemini',provider:'gemini',model:'antigravity',endpoint:interactive.endpoint,externalConsent:consent}:null}}if(route==='ollama')return{route,key:'',consent:false,agentic:false,interactive:{route:'ollama',provider:'ollama',model:form.ollamaModel.value.trim()||'llama3.2',endpoint:form.ollamaEndpoint.value.trim()||OLLAMA_ENDPOINT,externalConsent:false},agenticConfig:null};const key=form.compatibleApiKey.value.trim()||prior,consent=form.compatibleConsent.checked;return{route,key,consent,agentic:false,interactive:{route:'openai-compatible',provider:'openai-compatible',model:form.compatibleModel.value.trim()||'local-model',endpoint:form.compatibleEndpoint.value.trim(),externalConsent:consent},agenticConfig:null}}
function persist(setup){localStorage.setItem(SETTINGS_KEY,JSON.stringify({route:setup.route,provider:setup.interactive.provider,model:setup.interactive.model,endpoint:setup.interactive.endpoint,consent:setup.consent,agenticEnabled:setup.agentic}));if(setup.key||setup.consent)sessionStorage.setItem(SESSION_KEY,JSON.stringify({apiKey:setup.key,remoteConsent:setup.consent,provider:setup.interactive.provider,savedAt:new Date().toISOString()}));else sessionStorage.removeItem(SESSION_KEY);const r=runtime();if(!r)return;r.saveSharedConfig(setup.interactive,{profile:'interactive'});if(setup.key||setup.consent)r.saveSessionSecret(setup.interactive,{apiKey:setup.key,externalConsent:setup.consent});if(setup.agenticConfig){r.saveSharedConfig(setup.agenticConfig,{profile:'agentic',enabled:true});r.saveSessionSecret(setup.agenticConfig,{apiKey:setup.key,externalConsent:setup.consent})}else r.saveModelProfiles({agenticEnabled:false})}
function missingSummary(result){return(result?.missing||[]).map(item=>`${item.url.split('/').at(-1)} (${item.status||'network'}, ${Number(item.length||0)} bytes)`).join(', ')}
async function checkPackage(form){const state=form.querySelector('[data-package-state]');if(!state)return;state.textContent='Checking MiniLM, tokenizer, and local runtimes…';try{const result=await globalThis.CommonweaveReflexRuntime?.status?.();if(!result)throw new Error('Reflex runtime has not loaded.');state.textContent=result.available?'MiniLM semantic retrieval is ready. A lexical reflex remains available even before model warmup.':`Semantic package is incomplete: ${missingSummary(result)}`;state.className=`cw-ai-package-state ${result.available?'is-ready':'is-missing'}`}catch(error){state.textContent=`Semantic package check failed: ${error.message}`;state.className='cw-ai-package-state is-missing'}}
async function benchmark(form,button){const output=form.querySelector('[data-benchmark-output]'),status=form.querySelector('[data-route-panel="bundled"] [data-test-status]');button.disabled=true;output.hidden=false;status.textContent='Prewarming MiniLM and measuring five semantic matches…';const cases=[['mutual-aid','I wish I could get my friends to work together to form mutual aid networks and local food sources','mutual-aid-food-network'],['learning','I want to understand watershed testing and practice reading the results','learning-path'],['build','Repair a greenhouse vent and prove the work is complete','build-repair-project'],['exchange','Find reclaimed boards and borrow a trailer fairly','resource-exchange'],['governance','Draft how the neighborhood approves shared tool purchases','governance-proposal']].map(([id,text,expected])=>({id,text,expected}));try{const packageState=await globalThis.CommonweaveReflexRuntime.status();if(!packageState?.available)throw new Error(`Semantic package incomplete: ${missingSummary(packageState)}`);const warmStart=performance.now();const warm=await globalThis.CommonweaveReflexRuntime.prewarm();if(warm?.ready===false)throw new Error(warm.error||'MiniLM failed to initialize.');const warmMs=Math.round(performance.now()-warmStart);const trial=await globalThis.CommonweaveReflexRuntime.benchmark(cases);let correct=0;const rows=trial.results.map((result,index)=>{const expected=cases[index].expected,actual=result.matches?.[0]?.id||'';if(actual===expected)correct+=1;return`<li class="${actual===expected?'is-pass':'is-fail'}"><b>${esc(result.id)}</b>: ${esc(actual||result.error||'no match')} · ${result.elapsedMs} ms · ${esc(result.device||'lexical')}</li>`}).join('');output.innerHTML=`<b>${correct}/5 semantic matches correct</b><p>Cold prewarm: ${warmMs} ms. Five warm queries: ${trial.elapsedMs} ms. Chat never waits for this model; the lexical reflex answers immediately.</p><ol>${rows}</ol>`;status.textContent=`Reflex trial complete: ${correct}/5 matches. Warm queries averaged ${Math.round(trial.elapsedMs/5)} ms.`;status.className=`cw-ai-test-status ${correct>=4?'is-ok':'is-error'}`}catch(error){output.innerHTML=`<b>Trial failed.</b><p>${esc(error.message)}</p>`;status.textContent=`Reflex trial failed: ${error.message}`;status.className='cw-ai-test-status is-error'}finally{button.disabled=false}}
async function testProvider(form,button){const setup=setupFrom(form),status=button.closest('[data-route-panel]').querySelector('[data-test-status]');button.disabled=true;try{if(setup.route==='gemini'&&!setup.key)throw new Error('Enter a Gemini API key first.');if(setup.route==='gemini'&&!setup.consent)throw new Error('Enable remote consent first.');const capability=await runtime().detectCapabilities({...setup.interactive,apiKey:setup.key},{probe:true});if(capability.available===false)throw new Error('The provider did not answer.');status.textContent=`${capability.provider} is reachable. MiniLM Reflex remains the instant fallback.`;status.className='cw-ai-test-status is-ok'}catch(error){status.textContent=`Connection test failed: ${error.message}`;status.className='cw-ai-test-status is-error'}finally{button.disabled=false}}
function bind(form){if(!form||form.dataset.reflexBound)return;form.dataset.reflexBound='true';fill(form);form.route.addEventListener('change',()=>sync(form));form.querySelector('[data-check-package]')?.addEventListener('click',()=>checkPackage(form));form.querySelector('[data-benchmark]')?.addEventListener('click',event=>benchmark(form,event.currentTarget));form.querySelectorAll('[data-test-provider]').forEach(button=>button.addEventListener('click',()=>testProvider(form,button)));form.addEventListener('submit',event=>{event.preventDefault();const setup=setupFrom(form);persist(setup);form.querySelector('[data-save-status]').textContent=setup.route==='bundled'?'Saved the instant onboard semantic reflex.':'Saved the selected provider. MiniLM Reflex remains the immediate fallback.'})}
function build(){let dialog=document.getElementById('cw-ai-settings-v138');if(dialog)return dialog;dialog=document.createElement('dialog');dialog.id='cw-ai-settings-v138';dialog.className='cw127-dialog cw-ai-settings-dialog';dialog.innerHTML=markup();document.body.append(dialog);bind(dialog.querySelector('form'));dialog.querySelector('[data-close]').onclick=()=>dialog.close();dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()});return dialog}
function open(){const dialog=build();fill(dialog.querySelector('form'));if(!dialog.open)dialog.showModal()}
document.addEventListener('click',event=>{const target=event.target.closest?.('[data-action="settings"],#lite-settings');if(!target||target.closest('[data-reflex-settings-form]'))return;event.preventDefault();event.stopImmediatePropagation();open()},true);
const observer=new MutationObserver(()=>document.querySelectorAll('[data-reflex-settings-form]').forEach(bind));observer.observe(document.documentElement,{childList:true,subtree:true});
globalThis.CommonweaveModelSettingsV133={open,inlineMarkup:()=>`<section class="cw-ai-inline-card">${markup({inline:true})}</section>`,model:MODEL_ID,manifest:MANIFEST};
})();
