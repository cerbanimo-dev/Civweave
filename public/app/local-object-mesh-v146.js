(()=>{
'use strict';
const DB_NAME='civweave-community-objects-v146';
const DB_VERSION=1;
const OBJECT_SCHEMA='civweave.community-object.v1';
const DELIVERY_SCHEMA='civweave.community-delivery.v1';
const RECEIPT_SCHEMA='civweave.community-receipt.v1';
const CREDENTIAL_KEY='device-credential';
const sessions=new Map();
const encoder=new TextEncoder();
const decoder=new TextDecoder();
const listeners=new Set();
const now=()=>new Date().toISOString();
const uid=prefix=>`${prefix}:${crypto.randomUUID?.()||`${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`}`;
const clone=value=>structuredClone(value);
const allowedConsent=new Set(['private','direct','group','public','federated']);
function emit(type,detail={}){const event={type,detail,at:now()};for(const listener of listeners)try{listener(event)}catch{};dispatchEvent(new CustomEvent('civweave:mesh',{detail:event}))}
function base64url(bytes){let binary='';for(const byte of new Uint8Array(bytes))binary+=String.fromCharCode(byte);return btoa(binary).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}
function fromBase64url(value){const raw=String(value).replace(/-/g,'+').replace(/_/g,'/');const padded=raw+'='.repeat((4-raw.length%4)%4);const binary=atob(padded);return Uint8Array.from(binary,char=>char.charCodeAt(0))}
function normalized(value){if(Array.isArray(value))return value.map(normalized);if(value&&typeof value==='object'){const out={};for(const key of Object.keys(value).sort())if(value[key]!==undefined)out[key]=normalized(value[key]);return out}return value}
function canonical(value){return JSON.stringify(normalized(value))}
async function sha256(value){const bytes=typeof value==='string'?encoder.encode(value):value;return base64url(await crypto.subtle.digest('SHA-256',bytes))}
function openDb(){return new Promise((resolve,reject)=>{const request=indexedDB.open(DB_NAME,DB_VERSION);request.onupgradeneeded=()=>{const db=request.result;if(!db.objectStoreNames.contains('objects')){const store=db.createObjectStore('objects',{keyPath:'id'});store.createIndex('updatedAt','updatedAt');store.createIndex('kind','kind');store.createIndex('consent','consent')}if(!db.objectStoreNames.contains('outbox')){const store=db.createObjectStore('outbox',{keyPath:'id'});store.createIndex('status','status');store.createIndex('createdAt','createdAt')}if(!db.objectStoreNames.contains('receipts'))db.createObjectStore('receipts',{keyPath:'id'});if(!db.objectStoreNames.contains('peers'))db.createObjectStore('peers',{keyPath:'id'});if(!db.objectStoreNames.contains('meta'))db.createObjectStore('meta',{keyPath:'key'})};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}
async function tx(storeNames,mode,work){const db=await openDb();return new Promise((resolve,reject)=>{const transaction=db.transaction(storeNames,mode);const stores=Object.fromEntries(storeNames.map(name=>[name,transaction.objectStore(name)]));let result;try{result=work(stores,transaction)}catch(error){transaction.abort();reject(error);return}transaction.oncomplete=()=>resolve(result);transaction.onerror=()=>reject(transaction.error);transaction.onabort=()=>reject(transaction.error||new Error('Local object transaction aborted'))})}
function req(request){return new Promise((resolve,reject)=>{request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}
async function credential(){let record=await tx(['meta'],'readonly',stores=>req(stores.meta.get(CREDENTIAL_KEY)));if(record?.privateKey&&record?.publicKey)return record;const pair=await crypto.subtle.generateKey({name:'ECDSA',namedCurve:'P-256'},false,['sign','verify']);const publicKey=await crypto.subtle.exportKey('jwk',pair.publicKey);const fingerprint=(await sha256(canonical(publicKey))).slice(0,24);record={key:CREDENTIAL_KEY,id:`device:${fingerprint}`,fingerprint,publicKey,privateKey:pair.privateKey,createdAt:now()};await tx(['meta'],'readwrite',stores=>stores.meta.put(record));return record}
function signableObject(object){return{schema:object.schema,id:object.id,revision:object.revision,kind:object.kind,purpose:object.purpose,audience:object.audience,consent:object.consent,payload:object.payload,payloadHash:object.payloadHash,parentIds:object.parentIds,createdAt:object.createdAt,updatedAt:object.updatedAt,expiresAt:object.expiresAt,origin:object.origin,hopLimit:object.hopLimit}}
async function sign(privateKey,value){return base64url(await crypto.subtle.sign({name:'ECDSA',hash:'SHA-256'},privateKey,encoder.encode(canonical(value))))}
async function verify(publicKey,value,signature){try{const key=await crypto.subtle.importKey('jwk',publicKey,{name:'ECDSA',namedCurve:'P-256'},false,['verify']);return crypto.subtle.verify({name:'ECDSA',hash:'SHA-256'},key,fromBase64url(signature),encoder.encode(canonical(value)))}catch{return false}}
async function createObject(input={}){const owner=await credential();const consent=allowedConsent.has(input.consent)?input.consent:'private';const createdAt=now();const payload=clone(input.payload??{});const object={schema:OBJECT_SCHEMA,id:input.id||uid('object'),revision:Number(input.revision||1),kind:String(input.kind||'civweave.record').slice(0,160),purpose:String(input.purpose||'local work').slice(0,500),audience:Array.isArray(input.audience)?[...new Set(input.audience.map(String))].slice(0,100):[],consent,payload,payloadHash:await sha256(canonical(payload)),parentIds:Array.isArray(input.parentIds)?[...new Set(input.parentIds.map(String))].slice(0,50):[],createdAt,updatedAt:createdAt,expiresAt:input.expiresAt||null,origin:{nodeId:owner.id,credential:owner.publicKey,fingerprint:owner.fingerprint},hopLimit:Math.max(0,Math.min(16,Number(input.hopLimit??4))),visited:[owner.id]};object.revisionHash=await sha256(canonical(signableObject(object)));object.signature=await sign(owner.privateKey,signableObject(object));await tx(['objects'],'readwrite',stores=>stores.objects.put(object));if(input.publish!==false&&consent!=='private')await queue(object,{destinations:input.destinations});emit('object-created',{id:object.id,kind:object.kind,consent});return object}
async function queue(object,options={}){const delivery={schema:DELIVERY_SCHEMA,id:`delivery:${object.id}:${object.revisionHash}`,objectId:object.id,revisionHash:object.revisionHash,destinations:Array.isArray(options.destinations)?options.destinations.map(String):object.audience,status:'pending',attempts:0,createdAt:now(),updatedAt:now(),nextAttemptAt:now(),lastError:null};await tx(['outbox'],'readwrite',stores=>stores.outbox.put(delivery));emit('queued',{deliveryId:delivery.id,objectId:object.id});await requestBackgroundSync();return delivery}
async function getObject(id){return tx(['objects'],'readonly',stores=>req(stores.objects.get(id)))}
async function listObjects(){return tx(['objects'],'readonly',stores=>req(stores.objects.getAll()))}
async function listOutbox(status='pending'){return tx(['outbox'],'readonly',stores=>status?req(stores.outbox.index('status').getAll(status)):req(stores.outbox.getAll()))}
async function markDelivery(id,status,error=null){return tx(['outbox'],'readwrite',stores=>{const request=stores.outbox.get(id);request.onsuccess=()=>{const row=request.result;if(!row)return;row.status=status;row.updatedAt=now();row.attempts=Number(row.attempts||0)+1;row.lastError=error?String(error).slice(0,1000):null;row.nextAttemptAt=status==='pending'?new Date(Date.now()+Math.min(3600000,1000*2**Math.min(row.attempts,10))).toISOString():null;stores.outbox.put(row)}})}
function intendedFor(object,peerId,groups=[]){if(object.consent==='public'||object.consent==='federated')return true;if(object.consent==='direct')return object.audience.includes(peerId);if(object.consent==='group')return object.audience.some(id=>groups.includes(id));return false}
async function validateObject(object){if(object?.schema!==OBJECT_SCHEMA||!object.id||!object.revisionHash||!object.signature)return{ok:false,error:'invalid object envelope'};if(await sha256(canonical(object.payload))!==object.payloadHash)return{ok:false,error:'payload hash mismatch'};if(await sha256(canonical(signableObject(object)))!==object.revisionHash)return{ok:false,error:'revision hash mismatch'};if(!await verify(object.origin?.credential,signableObject(object),object.signature))return{ok:false,error:'signature rejected'};if(object.expiresAt&&Date.parse(object.expiresAt)<=Date.now())return{ok:false,error:'object expired'};return{ok:true}}
async function ingest(object,context={}){const validation=await validateObject(object);if(!validation.ok)throw new Error(validation.error);const local=await getObject(object.id);if(local?.revisionHash===object.revisionHash)return{status:'duplicate',object:local};if(local&&Number(local.revision)>Number(object.revision))return{status:'older',object:local};const next={...clone(object),visited:[...new Set([...(object.visited||[]),context.fromPeer].filter(Boolean))],receivedAt:now()};await tx(['objects'],'readwrite',stores=>stores.objects.put(next));const receipt={schema:RECEIPT_SCHEMA,id:`receipt:${object.id}:${object.revisionHash}:${context.localNodeId||'local'}`,objectId:object.id,revisionHash:object.revisionHash,fromPeer:context.fromPeer||null,status:'accepted',createdAt:now()};await tx(['receipts'],'readwrite',stores=>stores.receipts.put(receipt));emit('object-received',{id:object.id,kind:object.kind,fromPeer:context.fromPeer||null});return{status:'accepted',object:next,receipt}}
async function deviceId(){return(await credential()).id}
function waitIce(connection){if(connection.iceGatheringState==='complete')return Promise.resolve();return new Promise(resolve=>{const done=()=>{if(connection.iceGatheringState==='complete'){connection.removeEventListener('icegatheringstatechange',done);resolve()}};connection.addEventListener('icegatheringstatechange',done);setTimeout(resolve,5000)})}
function encodeSignal(description){return base64url(encoder.encode(JSON.stringify(description)))}
function decodeSignal(value){return JSON.parse(decoder.decode(fromBase64url(value)))}
function bindChannel(session,channel){session.channel=channel;channel.binaryType='arraybuffer';channel.addEventListener('open',async()=>{channel.send(JSON.stringify({type:'hello',nodeId:await deviceId(),schemas:[OBJECT_SCHEMA]}));emit('peer-open',{sessionId:session.id});flushSession(session).catch(error=>emit('peer-error',{sessionId:session.id,error:error.message}))});channel.addEventListener('message',event=>handleMessage(session,event.data));channel.addEventListener('close',()=>emit('peer-close',{sessionId:session.id}));channel.addEventListener('error',()=>emit('peer-error',{sessionId:session.id,error:'data channel error'}))}
async function handleMessage(session,data){let message;try{message=JSON.parse(typeof data==='string'?data:decoder.decode(data))}catch{return}if(message.type==='hello'){session.peerId=String(message.nodeId||session.id);await tx(['peers'],'readwrite',stores=>stores.peers.put({id:session.peerId,lastSeenAt:now(),transport:'webrtc',status:'connected'}));emit('peer-identified',{sessionId:session.id,peerId:session.peerId});return}if(message.type==='objects'){for(const object of message.items||[]){try{const result=await ingest(object,{fromPeer:session.peerId,localNodeId:await deviceId()});if(result.receipt)session.channel?.send(JSON.stringify({type:'receipt',receipt:result.receipt}))}catch(error){session.channel?.send(JSON.stringify({type:'rejection',objectId:object?.id,error:error.message}))}}return}if(message.type==='receipt'){const receipt=message.receipt;if(receipt?.id)await tx(['receipts'],'readwrite',stores=>stores.receipts.put(receipt));const deliveryId=`delivery:${receipt?.objectId}:${receipt?.revisionHash}`;if(receipt?.status==='accepted')await markDelivery(deliveryId,'delivered');emit('receipt',{receipt});return}if(message.type==='rejection')emit('rejection',message)}
function newConnection(){return new RTCPeerConnection({iceServers:[]})}
async function createOffer(){const connection=newConnection(),session={id:uid('session'),connection,channel:null,peerId:null,role:'offerer'};sessions.set(session.id,session);bindChannel(session,connection.createDataChannel('civweave-objects',{ordered:true}));await connection.setLocalDescription(await connection.createOffer());await waitIce(connection);return{sessionId:session.id,offer:encodeSignal(connection.localDescription)}}
async function acceptOffer(encoded){const connection=newConnection(),session={id:uid('session'),connection,channel:null,peerId:null,role:'answerer'};sessions.set(session.id,session);connection.addEventListener('datachannel',event=>bindChannel(session,event.channel));await connection.setRemoteDescription(decodeSignal(encoded));await connection.setLocalDescription(await connection.createAnswer());await waitIce(connection);return{sessionId:session.id,answer:encodeSignal(connection.localDescription)}}
async function acceptAnswer(sessionId,encoded){const session=sessions.get(sessionId);if(!session)throw new Error('Pairing session not found');await session.connection.setRemoteDescription(decodeSignal(encoded));return{sessionId,status:'connecting'}}
async function flushSession(session){if(!session?.channel||session.channel.readyState!=='open'||!session.peerId)return{sent:0};const pending=await listOutbox('pending'),items=[];for(const delivery of pending){const object=await getObject(delivery.objectId);if(!object||!intendedFor(object,session.peerId,[]))continue;if((object.visited||[]).includes(session.peerId)||object.hopLimit<=0)continue;items.push({...object,hopLimit:object.hopLimit-1,visited:[...new Set([...(object.visited||[]),await deviceId()])]})}if(items.length)session.channel.send(JSON.stringify({type:'objects',items}));emit('peer-flush',{peerId:session.peerId,count:items.length});return{sent:items.length}}
async function syncGateway(baseUrl){const base=new URL(baseUrl||location.origin),pending=await listOutbox('pending');let sent=0,received=0;for(const delivery of pending){const object=await getObject(delivery.objectId);if(!object)continue;try{const response=await fetch(new URL('/api/envelopes',base),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({schema:'civweave.community-object-envelope.v1',from:await deviceId(),to:object.audience.length===1?object.audience[0]:'*',kind:'community-object',subject:object.kind,payload:object,correlationId:object.id})});if(!response.ok)throw new Error(`gateway returned ${response.status}`);await markDelivery(delivery.id,'delivered');sent++}catch(error){await markDelivery(delivery.id,'pending',error.message)}}try{const endpoint=new URL('/api/envelopes',base);endpoint.searchParams.set('limit','200');const response=await fetch(endpoint,{cache:'no-store'});if(response.ok){const payload=await response.json();for(const envelope of payload.envelopes||[]){if(envelope.kind!=='community-object'||!envelope.payload)continue;try{const object=envelope.payload;if(!intendedFor(object,await deviceId(),[]))continue;const result=await ingest(object,{fromPeer:envelope.from,localNodeId:await deviceId()});if(result.status==='accepted')received++}catch{}}}}catch{}emit('gateway-sync',{base:base.origin,sent,received});return{sent,received}}
async function requestBackgroundSync(){try{const registration=await navigator.serviceWorker?.ready;if(registration?.sync)await registration.sync.register('civweave-community-outbox')}catch{}}
async function exportBundle(){const owner=await credential();return{schema:'civweave.community-bundle.v1',createdAt:now(),node:{id:owner.id,publicKey:owner.publicKey},objects:await listObjects(),receipts:await tx(['receipts'],'readonly',stores=>req(stores.receipts.getAll()))}}
async function importBundle(bundle){if(bundle?.schema!=='civweave.community-bundle.v1'||!Array.isArray(bundle.objects))throw new Error('Unsupported community bundle');const results=[];for(const object of bundle.objects)try{results.push(await ingest(object,{fromPeer:bundle.node?.id,localNodeId:await deviceId()}))}catch(error){results.push({status:'rejected',id:object?.id,error:error.message})}return results}
function subscribe(listener){listeners.add(listener);return()=>listeners.delete(listener)}
globalThis.CivweaveLocalMeshV146={DB_NAME,OBJECT_SCHEMA,deviceId,credential,createObject,queue,getObject,listObjects,listOutbox,ingest,validateObject,createOffer,acceptOffer,acceptAnswer,flushSession,syncGateway,exportBundle,importBundle,requestBackgroundSync,subscribe,sessions};
})();
