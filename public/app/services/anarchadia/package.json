{
  "name": "anarchadia-charter-forge",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "description": "Local-first living amendment hall with precisely mapped interactive room artwork, existing Charter Forge governance, Civweave sync, Rook advisory tools, selective exchange, and human-controlled records.",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/domain.js && node --check src/store.js && node --check src/ai.js && node --check src/export.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "AGPL-3.0-or-later"
}
