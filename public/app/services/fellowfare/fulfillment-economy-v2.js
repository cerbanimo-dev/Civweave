(()=>{
'use strict';
if(globalThis.CivweaveFulfillmentEconomyV2)return;

const VERSION='2.0.0';
const SCHEMA='civweave.fulfillment-economy.v2';
const STATE_KEY='civweave.fulfillment-economy.v1';
const MARKET_KEY='fellowfare.marketplace.v2';
const MERCHANT_KEY='fellowfare.stripe-merchant.v1';
const MONEY_EDGE=cleanOrigin(globalThis.CIVWEAVE_MONEY_EDGE_ORIGIN||'https://civweave-core.cerbanimo.workers.dev');
const DIRECT_API=`${MONEY_EDGE}/api/fellowfare/direct-commerce`;
const REWARD_PER_QUEST=5;
const MILESTONE_SIZE=100;
const MILESTONE_BONUS=10;
const GOODS_KINDS=new Set(['product','resource']);
const TOKEN_KINDS=new Set(['service','learning','tutoring']);
const ASSETS={acorns:{ledger:'acorn',symbol:'🌰',label:'Acorns'},buttons:{ledger:'button',symbol:'🔘',label:'Buttons'}};
const now=()=>new Date().toISOString();
const clean=(value,max=4000)=>String(value??'').replace(/\s+/g,' ').trim().slice(0,max);
const parse=(value,fallback)=>{try{return JSON.parse(value)??fallback}catch{return fallback}};
const copy=value=>JSON.parse(JSON.stringify(value));
const integer=(value,label='amount')=>{const n=Number(value);if(!Number.isSafeInteger(n)||n<0)throw new RangeError(`${label} must be a whole number.`);return n};
function cleanOrigin(value){try{return new URL(String(value)).origin}catch{return'https://civweave-core.cerbanimo.workers.dev'}}
const localDay=()=>{const d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return`${y}-${m}-${day}`};
const hash=value=>{let h=2166136261;for(const c of String(value)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0};
const money=minor=>Number(minor)>0?`$${(Number(minor)/100).toFixed(2)}`:'';

const QUEST_POOL=Object.freeze([
  {id:'finish-learning-module',bucket:'progress',event:'learning_complete',target:1,title:'Finish a learning module',rewardAsset:'acorns',noBalance:true},
  {id:'finish-cerbanimo-quest',bucket:'progress',event:'cerbanimo_quest_complete',target:1,title:'Finish a Cerbanimo quest',rewardAsset:'buttons',noBalance:true},
  {id:'help-someone-learn',bucket:'progress',event:'teaching_complete',target:1,title:'Help someone finish a learning activity',rewardAsset:'acorns',noBalance:true},
  {id:'fulfill-20-acorns',bucket:'fulfillment',event:'fulfill_acorns',target:20,title:'Fulfill 20 🌰 Acorns',rewardAsset:'acorns'},
  {id:'fulfill-20-buttons',bucket:'fulfillment',event:'fulfill_buttons',target:20,title:'Fulfill 20 🔘 Buttons',rewardAsset:'buttons'},
  {id:'complete-service-arrangement',bucket:'fulfillment',event:'service_complete',target:1,title:'Complete a service arrangement',rewardAsset:'buttons',noBalance:true},
  {id:'post-need',bucket:'community',event:'post_need',target:1,title:'Post a need',rewardAsset:'acorns',noBalance:true},
  {id:'post-offering',bucket:'community',event:'post_offering',target:1,title:'Post an offering',rewardAsset:'buttons',noBalance:true},
  {id:'answer-need',bucket:'community',event:'answer_need',target:1,title:'Respond to a community need',rewardAsset:'buttons',noBalance:true},
  {id:'validate-contribution',bucket:'community',event:'validate_contribution',target:1,title:'Validate a contribution',rewardAsset:'buttons',noBalance:true},
  {id:'contribute-project-resource',bucket:'community',event:'project_resource',target:1,title:'Contribute a useful project resource',rewardAsset:'acorns',noBalance:true}
]);

function emptyState(){return{schema:'civweave.fulfillment-economy.v1',version:2,lifetimeFulfilled:{acorns:0,buttons:0},days:{},activities:[],seenActivityKeys:[],seenListingIds:[],seenLearningSourceIds:[],contributions:[],migration:{},updatedAt:now()}}
function readState(){const raw=parse(localStorage.getItem(STATE_KEY),null);if(raw?.schema!=='civweave.fulfillment-economy.v1')return emptyState();const state={...emptyState(),...raw};state.lifetimeFulfilled={...emptyState().lifetimeFulfilled,...(raw.lifetimeFulfilled||{})};state.days=raw.days&&typeof raw.days==='object'?raw.days:{};state.activities=Array.isArray(raw.activities)?raw.activities:[];state.seenActivityKeys=Array.isArray(raw.seenActivityKeys)?raw.seenActivityKeys:[];state.seenListingIds=Array.isArray(raw.seenListingIds)?raw.seenListingIds:[];state.seenLearningSourceIds=Array.isArray(raw.seenLearningSourceIds)?raw.seenLearningSourceIds:[];state.contributions=Array.isArray(raw.contributions)?raw.contributions:[];return state}
let state=readState();
function writeState(reason='update'){state.updatedAt=now();state.activities=state.activities.slice(-1000);state.seenActivityKeys=state.seenActivityKeys.slice(-2500);state.seenListingIds=state.seenListingIds.slice(-2500);state.seenLearningSourceIds=state.seenLearningSourceIds.slice(-2500);state.contributions=state.contributions.slice(-1000);localStorage.setItem(STATE_KEY,JSON.stringify(state));try{dispatchEvent(new CustomEvent('civweave:fulfillment-economy-changed',{detail:{reason,state:copy(state)}}))}catch{}return state}
function ledger(){const api=globalThis.CivweaveCanonicalRewardsV2;if(!api?.appendEntry||!api?.project)throw new Error('Canonical Acorn/Button ledger is unavailable.');return api}
function balances(){const p=ledger().project();return{acorns:Number(p.acorns||0),buttons:Number(p.buttons||0)}}
function questForDay(day=localDay()){const buckets=['progress','fulfillment','community'];return buckets.map((bucket,index)=>{const rows=QUEST_POOL.filter(q=>q.bucket===bucket);return rows[hash(`${day}:${bucket}:${index}`)%rows.length]})}
function ensureDay(day=localDay()){const selected=questForDay(day),prior=state.days[day]&&typeof state.days[day]==='object'?state.days[day]:{},progress=prior.progress&&typeof prior.progress==='object'?prior.progress:{};for(const quest of selected)progress[quest.id]={count:Number(progress[quest.id]?.count||0),completed:Boolean(progress[quest.id]?.completed),rewardedAt:progress[quest.id]?.rewardedAt||null};state.days[day]={date:day,questIds:selected.map(q=>q.id),progress,createdAt:prior.createdAt||now(),updatedAt:now()};return state.days[day]}
async function awardQuest(day,quest){const record=ensureDay(day).progress[quest.id];if(record.rewardedAt)return false;const asset=ASSETS[quest.rewardAsset];await ledger().appendEntry({accountId:'passport:local',assetType:asset.ledger,amount:REWARD_PER_QUEST,sourceSystem:'fellowfare',sourceKind:'doing',sourceId:`daily-quest:${day}:${quest.id}`,sourceKey:`fellowfare:daily-quest:${day}:${quest.id}:${asset.ledger}`,metadata:{operation:'daily-quest-reward',questId:quest.id,day,fixedReward:true,amount:REWARD_PER_QUEST}});record.rewardedAt=now();record.completed=true;writeState('daily-quest-reward');return true}
async function recordActivity(type,context={}){const event=clean(type,80);if(!event)return false;const day=clean(context.day||localDay(),20),sourceId=clean(context.sourceId||context.id||context.listingId||context.orderId||context.moduleId||'',220),amount=Math.max(1,Number(context.amount||1)),activityKey=clean(context.activityKey||`${day}:${event}:${sourceId||clean(context.note||'',120)}`,500);if(sourceId&&state.seenActivityKeys.includes(activityKey))return false;if(sourceId)state.seenActivityKeys.push(activityKey);state.activities.push({event,sourceId:sourceId||null,amount,day,at:now(),metadata:context.metadata&&typeof context.metadata==='object'?copy(context.metadata):{}});const daily=ensureDay(day),selected=QUEST_POOL.filter(q=>daily.questIds.includes(q.id)&&q.event===event);for(const quest of selected){const row=daily.progress[quest.id];if(row.completed)continue;row.count=Math.min(quest.target,Number(row.count||0)+amount);if(row.count>=quest.target){row.completed=true;await awardQuest(day,quest)}}writeState('activity');return true}
function normalizeAsset(asset){const key=clean(asset,30).toLowerCase();if(['acorn','acorns','🌰'].includes(key))return'acorns';if(['button','buttons','🔘'].includes(key))return'buttons';throw new TypeError('Fulfillment asset must be Acorns or Buttons.')}
async function awardMilestones(assetKey,before,after){const first=Math.floor(before/MILESTONE_SIZE)+1,last=Math.floor(after/MILESTONE_SIZE);if(last<first)return[];const results=[];for(let step=first;step<=last;step++){const threshold=step*MILESTONE_SIZE,asset=ASSETS[assetKey];results.push(await ledger().appendEntry({accountId:'passport:local',assetType:asset.ledger,amount:MILESTONE_BONUS,sourceSystem:'fellowfare',sourceKind:'doing',sourceId:`fulfillment-milestone:${assetKey}:${threshold}`,sourceKey:`fellowfare:fulfillment-milestone:${assetKey}:${threshold}`,metadata:{operation:'fulfillment-milestone-bonus',asset:assetKey,threshold,fixedReward:true,amount:MILESTONE_BONUS}}))}return results}
async function fulfill(assetInput,amountInput,context={}){const assetKey=normalizeAsset(assetInput),amount=integer(amountInput,'Fulfillment amount');if(amount<1)throw new RangeError('Fulfillment amount must be at least 1.');const available=balances()[assetKey];if(available<amount)throw new RangeError(`Not enough ${ASSETS[assetKey].label} to fulfill ${amount}.`);const sourceId=clean(context.sourceId||context.orderId||context.listingId||`fulfillment:${crypto.randomUUID?.()||Date.now()}`,220);await ledger().appendEntry({accountId:'passport:local',assetType:ASSETS[assetKey].ledger,amount:-amount,sourceSystem:'fellowfare',sourceKind:'doing',sourceId,sourceKey:clean(context.sourceKey||`fellowfare:fulfillment:${sourceId}:${assetKey}`,500),metadata:{operation:'fulfillment-burn',nonTransferable:true,recipientCredited:false,asset:assetKey,listingId:clean(context.listingId,220)||null,orderId:clean(context.orderId,220)||null,kind:clean(context.kind,40)||null,purpose:clean(context.purpose,240)||null}});const before=Number(state.lifetimeFulfilled[assetKey]||0),after=before+amount;state.lifetimeFulfilled[assetKey]=after;await recordActivity(`fulfill_${assetKey}`,{sourceId:`${sourceId}:${assetKey}`,amount,metadata:{listingId:context.listingId||null,orderId:context.orderId||null}});await awardMilestones(assetKey,before,after);writeState('fulfillment');return{asset:assetKey,amount,before,after,balance:balances()[assetKey]}}
function milestoneStatus(assetInput){const assetKey=normalizeAsset(assetInput),fulfilled=Number(state.lifetimeFulfilled[assetKey]||0),next=(Math.floor(fulfilled/MILESTONE_SIZE)+1)*MILESTONE_SIZE;return{asset:assetKey,fulfilled,next,remaining:next-fulfilled,bonus:MILESTONE_BONUS}}

function marketState(){const raw=parse(localStorage.getItem(MARKET_KEY),{});return raw&&typeof raw==='object'?raw:{}}
let sanitizing=false;
function sellerPriceText(pricing={}){if(clean(pricing?.sellerPayment?.priceText,120))return clean(pricing.sellerPayment.priceText,120);const usd=Number(pricing?.usdMinor||0);if(usd>0)return money(usd);if(clean(pricing?.legacyLabel,120))return clean(pricing.legacyLabel,120);return''}
function sanitizeMarketplaceState(reason='policy-sanitize'){
  if(sanitizing)return false;
  const market=marketState(),rows=Array.isArray(market.listings)?market.listings:[];let changed=false;
  for(const listing of rows){if(!listing||typeof listing!=='object')continue;const kind=clean(listing.kind,40).toLowerCase(),pricing=listing.pricing&&typeof listing.pricing==='object'?listing.pricing:{};
    if(GOODS_KINDS.has(kind)){
      const preserved=listing.sellerPayment&&typeof listing.sellerPayment==='object'?listing.sellerPayment:{},priceText=clean(preserved.priceText||sellerPriceText(pricing),120);
      listing.sellerPayment={mode:'seller-direct',priceText,methods:Array.isArray(preserved.methods)?preserved.methods.map(x=>clean(x,80)).filter(Boolean).slice(0,12):[],instructions:clean(preserved.instructions,600),platformCollectsPayment:false,platformRoutesPayment:false};
      if(Number(pricing.usdMinor||0)||Number(pricing.buttons||0)||Number(pricing.acorns||0)||listing.commerce){listing.pricing={...pricing,usdMinor:0,buttons:0,acorns:0};listing.commerce=null;listing.settlement={mode:'seller-direct',platformCollectsGross:false,platformRoutesSellerProceeds:false};listing.updatedAt=now();changed=true}
    }else if(TOKEN_KINDS.has(kind)){
      if(listing.commerce){listing.commerce=null;changed=true}
      const cash=Number(pricing.usdMinor||0)>0;
      const next={tokenMode:'fulfillment-burn',cashMode:cash?'stripe-connect-direct-charge':'none',recipientTokenTransfer:false,platformIssuedRewards:true,merchantOfRecord:cash?'connected-provider':null,platformCollectsGross:false,platformRoutesSellerProceeds:false,platformFeeMode:cash?'application-fee':'none'};
      if(JSON.stringify(listing.settlement||{})!==JSON.stringify(next)){listing.settlement=next;listing.updatedAt=now();changed=true}
      if(!cash&&listing.sellerPayment?.mode==='stripe-direct'){listing.sellerPayment=null;changed=true}
    }else if(listing.commerce){listing.commerce=null;changed=true}
  }
  if(!changed)return false;sanitizing=true;market.updatedAt=now();localStorage.setItem(MARKET_KEY,JSON.stringify(market));try{dispatchEvent(new CustomEvent('fellowfare:marketplace-changed',{detail:{reason,updatedAt:market.updatedAt}}))}catch{}sanitizing=false;return true;
}

function merchantIdentity(){let value=parse(localStorage.getItem(MERCHANT_KEY),null);if(!value||typeof value!=='object'){value={schema:'fellowfare.stripe-merchant.v1',userId:`ff-merchant:${crypto.randomUUID?.()||`${Date.now()}-${Math.random().toString(16).slice(2)}`}`,email:'',accountId:null,status:'unconnected',updatedAt:now()};localStorage.setItem(MERCHANT_KEY,JSON.stringify(value))}return value}
function saveMerchant(patch){const next={...merchantIdentity(),...patch,schema:'fellowfare.stripe-merchant.v1',updatedAt:now()};localStorage.setItem(MERCHANT_KEY,JSON.stringify(next));return next}
async function directApi(path,init={}){const response=await fetch(`${DIRECT_API}${path}`,{cache:'no-store',headers:{accept:'application/json',...(init.body?{'content-type':'application/json'}:{}),...(init.headers||{})},...init});const body=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(clean(body.error||body.message||`Direct commerce returned ${response.status}`,600)),{status:response.status});return body}
let merchantStatus={state:'idle',data:null,error:''};
async function refreshMerchantStatus(){const identity=merchantIdentity();merchantStatus={state:'loading',data:null,error:''};renderMerchantPanel();try{const data=await directApi(`/accounts/${encodeURIComponent(identity.userId)}`);saveMerchant({accountId:data.accountId,status:data.readyToProcessPayments?'ready':'onboarding'});merchantStatus={state:'ready',data,error:''};if(data.readyToProcessPayments)await syncOwnCashListings()}catch(error){if(error.status===404){merchantStatus={state:'unconnected',data:null,error:''};saveMerchant({accountId:null,status:'unconnected'})}else merchantStatus={state:'error',data:null,error:clean(error.message,400)}}renderMerchantPanel();enhanceListingCards()}
async function beginMerchantOnboarding(email){const identity=saveMerchant({email:clean(email,320)}),market=marketState(),profile=market.profile||{},displayName=clean(profile.name||'FellowFare provider',180);const account=await directApi('/accounts',{method:'POST',body:JSON.stringify({userId:identity.userId,displayName,contactEmail:identity.email,country:'us'})});saveMerchant({accountId:account.accountId,status:account.readyToProcessPayments?'ready':'onboarding'});if(account.readyToProcessPayments){merchantStatus={state:'ready',data:account,error:''};await syncOwnCashListings();renderMerchantPanel();return}const link=await directApi(`/accounts/${encodeURIComponent(identity.userId)}/onboard`,{method:'POST',body:'{}'});location.href=link.url}
async function continueMerchantOnboarding(){const identity=merchantIdentity(),link=await directApi(`/accounts/${encodeURIComponent(identity.userId)}/onboard`,{method:'POST',body:'{}'});location.href=link.url}
function ownCashListings(){const market=marketState();return(market.listings||[]).filter(row=>row?.ownerId==='me'&&TOKEN_KINDS.has(clean(row.kind,40).toLowerCase())&&Number(row?.pricing?.usdMinor||0)>0)}
let syncingCash=false;
async function syncOwnCashListings(){if(syncingCash)return;const identity=merchantIdentity();if(merchantStatus.data&&!merchantStatus.data.readyToProcessPayments)return;syncingCash=true;try{const market=marketState();let changed=false;for(const listing of(market.listings||[])){const kind=clean(listing?.kind,40).toLowerCase(),amount=Number(listing?.pricing?.usdMinor||0);if(listing?.ownerId!=='me'||!TOKEN_KINDS.has(kind)||!(amount>0))continue;if(listing.sellerPayment?.mode==='stripe-direct'&&listing.sellerPayment.priceId&&Number(listing.sellerPayment.priceAmountMinor)===amount)continue;try{const price=await directApi('/prices',{method:'POST',body:JSON.stringify({userId:identity.userId,listingId:listing.id,kind,name:listing.title,description:listing.description,amountMinor:amount,currency:'usd'})});listing.sellerPayment={mode:'stripe-direct',accountId:price.accountId,productId:price.productId,priceId:price.priceId,priceAmountMinor:price.amountMinor,currency:price.currency,platformApplicationFeeBps:price.platformFeeBps,merchantOfRecord:'connected-provider',platformCollectsGross:false,platformRoutesSellerProceeds:false,updatedAt:now()};listing.updatedAt=now();changed=true}catch(error){listing.sellerPayment={...(listing.sellerPayment||{}),mode:'stripe-direct-pending',error:clean(error.message,300),updatedAt:now()};changed=true}}
    if(changed){market.updatedAt=now();localStorage.setItem(MARKET_KEY,JSON.stringify(market));try{dispatchEvent(new CustomEvent('fellowfare:marketplace-changed',{detail:{reason:'stripe-direct-listing-sync',updatedAt:market.updatedAt}}))}catch{}}
  }finally{syncingCash=false}}
async function beginDirectCheckout(listing){const payment=listing?.sellerPayment||{};if(payment.mode!=='stripe-direct'||!payment.accountId||!payment.priceId)throw new Error('This seller has not finished Stripe setup for this listing yet.');const result=await directApi('/checkout',{method:'POST',body:JSON.stringify({accountId:payment.accountId,priceId:payment.priceId,listingId:listing.id,kind:listing.kind,quantity:1})});if(!result.checkoutUrl)throw new Error('Stripe did not return a Checkout URL.');location.href=result.checkoutUrl}

let pendingSellerTerms=null;
function priceLabel(form,name){return form.elements?.[name]?.closest?.('label')||null}
function ensureSellerFields(form){if(form.querySelector('[data-ff-seller-direct-fields]'))return;const anchor=form.querySelector('#ffv2ComposerAdvice')||form.querySelector('button[type="submit"]'),section=document.createElement('section');section.dataset.ffSellerDirectFields='true';section.className='ff-fulfillment-seller-fields';section.innerHTML=`<div class="ffv2-advice"><strong>Seller-direct goods payment.</strong> FellowFare lists the item but never collects, routes, splits, escrows, or settles the seller's payment.</div><label><span>Seller price / terms</span><input name="sellerPriceText" maxlength="120" placeholder="$25, free, make an offer…"></label><label><span>Private payment methods</span><input name="sellerPaymentMethods" maxlength="300" placeholder="Cash, PayPal, Venmo, seller checkout link…"></label><label><span>Payment / pickup instructions</span><textarea name="sellerPaymentInstructions" rows="3" maxlength="600"></textarea></label>`;anchor?.parentNode?.insertBefore(section,anchor)}
function configureComposer(){const form=document.querySelector('#ffv2ComposerForm');if(!form)return;ensureSellerFields(form);const kind=clean(form.elements?.kind?.value,40).toLowerCase(),goods=GOODS_KINDS.has(kind),tokens=TOKEN_KINDS.has(kind),seller=form.querySelector('[data-ff-seller-direct-fields]');if(seller)seller.hidden=!goods;const usd=priceLabel(form,'usd'),buttons=priceLabel(form,'buttons'),acorns=priceLabel(form,'acorns');if(usd)usd.hidden=!tokens;if(buttons)buttons.hidden=!tokens;if(acorns)acorns.hidden=!tokens;if(form.elements?.usd){form.elements.usd.disabled=!tokens;if(!tokens)form.elements.usd.value=''}if(form.elements?.buttons){form.elements.buttons.disabled=!tokens;if(!tokens)form.elements.buttons.value=''}if(form.elements?.acorns){form.elements.acorns.disabled=!tokens;if(!tokens)form.elements.acorns.value=''}const advice=document.querySelector('#ffv2ComposerAdvice');if(advice){if(goods)advice.textContent='Goods use the seller’s own payment method. No Acorn/Button price and no FellowFare checkout.';else if(tokens)advice.textContent='Offer USD, Acorn/Button fulfillment, or both. USD is a direct charge on your connected Stripe merchant account; Acorns/Buttons are burned, never transferred.'}}
function captureComposerSubmit(event){const form=event.target;if(form?.id!=='ffv2ComposerForm')return;const kind=clean(form.elements?.kind?.value,40).toLowerCase();if(GOODS_KINDS.has(kind)){if(form.elements?.usd){form.elements.usd.disabled=false;form.elements.usd.value=''}if(form.elements?.buttons){form.elements.buttons.disabled=false;form.elements.buttons.value=''}if(form.elements?.acorns){form.elements.acorns.disabled=false;form.elements.acorns.value=''}const methods=clean(form.elements?.sellerPaymentMethods?.value,300).split(',').map(x=>clean(x,80)).filter(Boolean);pendingSellerTerms={kind,title:clean(form.elements?.title?.value,180),sellerPayment:{mode:'seller-direct',priceText:clean(form.elements?.sellerPriceText?.value,120),methods,instructions:clean(form.elements?.sellerPaymentInstructions?.value,600),platformCollectsPayment:false,platformRoutesPayment:false},at:Date.now()}}else pendingSellerTerms=null}
function attachPendingSellerTerms(){if(!pendingSellerTerms)return false;const market=marketState(),rows=Array.isArray(market.listings)?market.listings:[],listing=rows.find(row=>row?.ownerId==='me'&&row?.kind===pendingSellerTerms.kind&&clean(row?.title,180)===pendingSellerTerms.title&&!row?.sellerPayment);if(!listing)return false;listing.sellerPayment=pendingSellerTerms.sellerPayment;listing.pricing={...(listing.pricing||{}),usdMinor:0,buttons:0,acorns:0};listing.commerce=null;listing.updatedAt=now();market.updatedAt=now();localStorage.setItem(MARKET_KEY,JSON.stringify(market));pendingSellerTerms=null;try{dispatchEvent(new CustomEvent('fellowfare:marketplace-changed',{detail:{reason:'seller-direct-terms-attached',updatedAt:market.updatedAt}}))}catch{}return true}
function formatTerms(listing){const p=listing?.pricing||{},parts=[];if(Number(p.usdMinor)>0)parts.push(money(p.usdMinor));if(Number(p.acorns)>0)parts.push(`Fulfill ${Number(p.acorns)} 🌰`);if(Number(p.buttons)>0)parts.push(`Fulfill ${Number(p.buttons)} 🔘`);return parts.length?parts.join(' · '):(p.gift?'Gift / free':'No fulfillment amount set')}
function listingMap(){return new Map((globalThis.CivweaveFellowFareMarketplaceV2?.listings?.()||[]).map(x=>[x.id,x]))}
function enhanceListingCards(){const listings=listingMap();for(const card of document.querySelectorAll('.ffv2-listing[data-listing-id]')){const listing=listings.get(card.dataset.listingId);if(!listing)continue;const price=card.querySelector('.ffv2-price'),actions=card.querySelector('.ffv2-card-actions');if(GOODS_KINDS.has(listing.kind)){const sp=listing.sellerPayment||{};if(price)price.textContent=[clean(sp.priceText,120)||'Seller sets payment terms','seller-direct'].join(' · ');if(!card.querySelector('[data-ff-payment-boundary]')){const note=document.createElement('small');note.dataset.ffPaymentBoundary='true';note.className='ff-fulfillment-boundary';note.textContent='Payment goes directly between buyer and seller. FellowFare never touches the goods payment.';price?.after(note)}}else if(TOKEN_KINDS.has(listing.kind)){if(price)price.textContent=formatTerms(listing);if(!card.querySelector('[data-ff-payment-boundary]')){const note=document.createElement('small');note.dataset.ffPaymentBoundary='true';note.className='ff-fulfillment-boundary';note.textContent='Acorns/Buttons burn on fulfillment. USD uses a provider-owned Stripe direct charge; FellowFare receives only its application fee.';price?.after(note)}if(listing.ownerId!=='me'&&Number(listing?.pricing?.usdMinor||0)>0&&actions&&!actions.querySelector('[data-ff-cash-checkout]')){const ready=listing.sellerPayment?.mode==='stripe-direct'&&listing.sellerPayment?.accountId&&listing.sellerPayment?.priceId,button=document.createElement('button');button.type='button';button.dataset.ffCashCheckout=listing.id;button.disabled=!ready;button.textContent=ready?`Pay ${money(listing.pricing.usdMinor)} with Stripe`:'USD checkout awaiting seller setup';actions.prepend(button)}}}}

function questView(day=localDay()){const daily=ensureDay(day);return daily.questIds.map(id=>{const quest=QUEST_POOL.find(q=>q.id===id),progress=daily.progress[id]||{count:0};return{...quest,count:Number(progress.count||0),completed:Boolean(progress.completed),rewardedAt:progress.rewardedAt||null}})}
function renderQuestPanel(){const route=document.body.dataset.ffRoute||location.hash.slice(1);if(route!=='inbox')return;const host=document.querySelector('.ffv2-balance-grid')?.parentElement||document.querySelector('#main');if(!host)return;let panel=document.querySelector('#ffFulfillmentDaily');if(!panel){panel=document.createElement('section');panel.id='ffFulfillmentDaily';panel.className='ff-fulfillment-daily';host.insertBefore(panel,host.children[1]||null)}const quests=questView(),a=milestoneStatus('acorns'),b=milestoneStatus('buttons');panel.innerHTML=`<div class="ffv2-section-head"><div><p class="ffv2-eyebrow">DAILY FULFILLMENTS</p><h2>Today's 3 quests</h2></div><span>${quests.filter(q=>q.completed).length}/3</span></div><div class="ff-fulfillment-quest-grid">${quests.map(q=>`<article><strong>${q.completed?'✓ ':''}${q.title}</strong><span>${Math.min(q.count,q.target)} / ${q.target}</span><small>Reward: +${REWARD_PER_QUEST} ${ASSETS[q.rewardAsset].symbol}</small></article>`).join('')}</div><div class="ff-fulfillment-milestones"><article><strong>🌰 ${a.fulfilled} fulfilled</strong><span>${a.remaining} until +${a.bonus} 🌰</span></article><article><strong>🔘 ${b.fulfilled} fulfilled</strong><span>${b.remaining} until +${b.bonus} 🔘</span></article></div>`}
function renderMerchantPanel(){const route=document.body.dataset.ffRoute||location.hash.slice(1);if(route!=='inbox')return;const host=document.querySelector('#main');if(!host)return;let panel=document.querySelector('#ffDirectMerchant');if(!panel){panel=document.createElement('section');panel.id='ffDirectMerchant';panel.className='ff-fulfillment-merchant';const anchor=document.querySelector('.ffv2-money-panel')||host.children[2]||null;anchor?.parentNode?.insertBefore(panel,anchor)}const identity=merchantIdentity(),status=merchantStatus.state,data=merchantStatus.data;let body='';if(status==='loading')body='<p>Checking your connected Stripe merchant…</p>';else if(status==='error')body=`<p><strong>Stripe status unavailable.</strong> ${clean(merchantStatus.error,300)}</p><button type="button" data-ff-merchant-refresh>Try again</button>`;else if(status==='ready'&&data?.readyToProcessPayments)body=`<p><strong>USD service checkout is ready.</strong> Charges are created on your connected Stripe account. You are the merchant of record; FellowFare receives its application fee and never routes your sale proceeds.</p><p><small>${ownCashListings().length} USD service/learning listing${ownCashListings().length===1?'':'s'} on this device.</small></p><button type="button" data-ff-sync-cash>Sync USD listings</button>`;else if(status==='ready'||status==='onboarding')body=`<p><strong>Stripe onboarding still needs attention.</strong> Complete Stripe’s merchant requirements before buyers can use USD checkout.</p><button type="button" data-ff-merchant-onboard>Continue Stripe onboarding</button><button type="button" data-ff-merchant-refresh>Refresh status</button>`;else body=`<p>Connect your own Stripe merchant account if you want to accept USD for services, tutoring, or paid learning. Goods do not use this rail.</p><label><span>Stripe contact email</span><input type="email" data-ff-merchant-email value="${clean(identity.email,320)}" placeholder="you@example.com"></label><button type="button" data-ff-merchant-connect>Connect Stripe for services</button>`;panel.innerHTML=`<div class="ffv2-section-head"><div><p class="ffv2-eyebrow">USD SERVICES + LEARNING</p><h2>Provider-owned Stripe checkout</h2></div></div>${body}`;const moneyPanel=document.querySelector('.ffv2-money-panel');if(moneyPanel&&!moneyPanel.dataset.ffDirectBoundary){moneyPanel.dataset.ffDirectBoundary='true';moneyPanel.innerHTML='<div><p class="ffv2-eyebrow">PAYMENT BOUNDARY</p><h2>Three rails, kept separate</h2><div class="ffv2-money-status"><strong>Goods: seller-direct. Services/learning USD: connected-provider direct charge. Acorns/Buttons: fulfillment burn.</strong><span>FellowFare never receives a goods purchase or a service provider’s gross sale and then sends the provider a payout.</span></div></div>'}const policy=document.querySelector('.ffv2-policy-note');if(policy)policy.innerHTML='<h2>Current commerce rules</h2><p><strong>Goods:</strong> payment is private between buyer and seller. No FellowFare percentage, Acorn price, or Button price.</p><p><strong>Services / learning / tutoring:</strong> sellers may offer Acorn/Button fulfillment, USD direct checkout, or both.</p><p><strong>USD:</strong> the charge belongs to the connected provider. FellowFare receives only an application fee. There is no destination charge or separate seller transfer.</p><p><strong>Tokens:</strong> fulfilled units burn from the requester. The platform independently issues contribution rewards.</p>'}

function installStyles(){if(document.querySelector('#ffFulfillmentEconomyStylesV2'))return;const style=document.createElement('style');style.id='ffFulfillmentEconomyStylesV2';style.textContent=`.ff-fulfillment-boundary{display:block;margin-top:.35rem;opacity:.78}.ff-fulfillment-daily,.ff-fulfillment-merchant{margin:1rem 0;padding:1rem;border:1px solid color-mix(in srgb,currentColor 18%,transparent);border-radius:16px}.ff-fulfillment-quest-grid,.ff-fulfillment-milestones{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:.75rem}.ff-fulfillment-quest-grid article,.ff-fulfillment-milestones article{display:grid;gap:.25rem;padding:.8rem;border:1px solid color-mix(in srgb,currentColor 15%,transparent);border-radius:12px}.ff-fulfillment-milestones{grid-template-columns:repeat(2,minmax(0,1fr));margin-top:.75rem}.ff-fulfillment-seller-fields{display:grid;gap:.75rem;margin:.75rem 0}.ff-fulfillment-merchant{display:grid;gap:.75rem}.ff-fulfillment-merchant label{display:grid;gap:.35rem;max-width:32rem}@media(max-width:640px){.ff-fulfillment-quest-grid,.ff-fulfillment-milestones{grid-template-columns:1fr}}`;document.head.append(style)}
function captureNewListingActivity(detail={}){if(clean(detail.reason,80)!=='listing-published')return;const market=marketState(),rows=Array.isArray(market.listings)?market.listings:[],listing=rows.find(row=>row?.ownerId==='me'&&!state.seenListingIds.includes(row.id));if(!listing)return;state.seenListingIds.push(listing.id);recordActivity(listing.mode==='need'?'post_need':'post_offering',{sourceId:listing.id,listingId:listing.id}).catch(()=>{});if(TOKEN_KINDS.has(listing.kind)&&Number(listing?.pricing?.usdMinor||0)>0&&merchantStatus.data?.readyToProcessPayments)syncOwnCashListings().catch(()=>{})}
function scanLearningRewards(){let entries=[];try{entries=globalThis.CivweaveCanonicalRewardsV2?.readLedger?.()?.entries||[]}catch{return}for(const entry of entries){if(entry?.assetType!=='skill-xp'||entry?.sourceKind!=='learning'||Number(entry.amount)<=0)continue;const id=clean(entry.sourceId,220);if(!id||state.seenLearningSourceIds.includes(id))continue;state.seenLearningSourceIds.push(id);recordActivity('learning_complete',{sourceId:id,moduleId:id}).catch(()=>{})}writeState('learning-scan')}
function orderAndListing(orderId){const market=marketState(),order=(market.orders||[]).find(x=>x?.id===orderId),listing=(globalThis.CivweaveFellowFareMarketplaceV2?.listings?.()||[]).find(x=>x?.id===order?.listingId);return{order,listing}}
async function completeOrderWithFulfillment(orderId){const {order,listing}=orderAndListing(orderId);if(!order||!listing)throw new Error('Arrangement or listing is unavailable.');const kind=clean(listing.kind,40).toLowerCase(),own=listing.ownerId==='me';if(TOKEN_KINDS.has(kind)&&!own){const p=listing.pricing||{},needed={acorns:integer(Math.max(0,Number(p.acorns||0)),'Acorns'),buttons:integer(Math.max(0,Number(p.buttons||0)),'Buttons')},have=balances();if(have.acorns<needed.acorns||have.buttons<needed.buttons)throw new RangeError('You do not have enough Acorns/Buttons to complete this fulfillment.');if(needed.acorns)await fulfill('acorns',needed.acorns,{sourceId:`order:${orderId}`,sourceKey:`fellowfare:order-fulfill:${orderId}:acorns`,orderId,listingId:listing.id,kind,purpose:listing.title});if(needed.buttons)await fulfill('buttons',needed.buttons,{sourceId:`order:${orderId}`,sourceKey:`fellowfare:order-fulfill:${orderId}:buttons`,orderId,listingId:listing.id,kind,purpose:listing.title})}if(kind==='service'||kind==='tutoring')await recordActivity(own?'teaching_complete':'service_complete',{sourceId:`order:${orderId}:complete`,orderId,listingId:listing.id});else if(kind==='learning')await recordActivity(own?'teaching_complete':'learning_complete',{sourceId:`order:${orderId}:complete`,orderId,listingId:listing.id});state.contributions.push({orderId,listingId:listing.id,kind,providerId:listing.ownerId||null,localRole:own?'provider':'requester',completedAt:now(),directTokenTransfer:false,cashSettlementHandledBy:'connected-provider-if-used'});writeState('arrangement-complete');globalThis.CivweaveFellowFareMarketplaceCapabilities?.updateOrder?.(orderId,'completed')}
function showError(error){try{globalThis.CivweaveFellowFareMarketplaceCapabilities?.notify?.(error.message)}catch{}let node=document.querySelector('#ffv2CapabilityToast');if(!node){node=document.createElement('div');node.id='ffv2CapabilityToast';node.className='ffv2-toast';document.body.append(node)}node.textContent=clean(error.message,300);node.hidden=false;setTimeout(()=>node.hidden=true,3600)}
function captureOrderCompletion(event){const button=event.target.closest?.('[data-ff-order-action="completed"]');if(!button)return;event.preventDefault();event.stopImmediatePropagation();completeOrderWithFulfillment(button.dataset.ffOrderId).catch(showError)}
async function onPolicyClick(event){const cash=event.target.closest?.('[data-ff-cash-checkout]');if(cash){event.preventDefault();event.stopImmediatePropagation();const listing=listingMap().get(cash.dataset.ffCashCheckout);if(listing)beginDirectCheckout(listing).catch(showError);return}if(event.target.closest?.('[data-ff-merchant-connect]')){event.preventDefault();const email=document.querySelector('[data-ff-merchant-email]')?.value||'';if(!clean(email,320))return showError(new Error('Enter the email you want Stripe to use for merchant onboarding.'));beginMerchantOnboarding(email).catch(showError);return}if(event.target.closest?.('[data-ff-merchant-onboard]')){event.preventDefault();continueMerchantOnboarding().catch(showError);return}if(event.target.closest?.('[data-ff-merchant-refresh]')){event.preventDefault();refreshMerchantStatus().catch(showError);return}if(event.target.closest?.('[data-ff-sync-cash]')){event.preventDefault();syncOwnCashListings().then(()=>enhance()).catch(showError)}}
function enhance(){installStyles();configureComposer();sanitizeMarketplaceState();enhanceListingCards();renderQuestPanel();renderMerchantPanel()}
function bindExternalActivityEvents(){const bindings=[['civweave:learning-module-completed','learning_complete'],['living-school:module-completed','learning_complete'],['civweave:cerbanimo-quest-completed','cerbanimo_quest_complete'],['cerbanimo:quest-completed','cerbanimo_quest_complete'],['civweave:contribution-validated','validate_contribution'],['civweave:project-resource-contributed','project_resource'],['fellowfare:need-answered','answer_need']];for(const[eventName,eventType]of bindings)addEventListener(eventName,event=>{const d=event.detail||{};recordActivity(eventType,{sourceId:d.sourceId||d.id||d.moduleId||d.questId||d.contributionId||`${eventName}:${Date.now()}`,metadata:d}).catch(()=>{})})}
const SELF_MUTATION_SELECTOR='#ffFulfillmentDaily,#ffDirectMerchant,.ffv2-money-panel,.ffv2-policy-note';
let enhanceFrame=0;
function scheduleEnhance(){cancelAnimationFrame(enhanceFrame);enhanceFrame=requestAnimationFrame(enhance)}
const observer=new MutationObserver(records=>{if(records.length&&records.every(record=>record.target?.closest?.(SELF_MUTATION_SELECTOR)))return;scheduleEnhance()});
function start(){ensureDay();sanitizeMarketplaceState('boot-policy-sanitize');const market=marketState();if(!state.migration.initialListingsSeen){state.seenListingIds=(market.listings||[]).map(x=>x?.id).filter(Boolean).slice(-2500);state.migration.initialListingsSeen=now();writeState('initial-listing-baseline')}document.addEventListener('submit',captureComposerSubmit,true);document.addEventListener('click',captureOrderCompletion,true);document.addEventListener('click',onPolicyClick,true);document.addEventListener('click',event=>{if(event.target.closest?.('[data-open-composer],[data-use-draft],[data-ff-need-shortcut]'))requestAnimationFrame(configureComposer)},true);document.addEventListener('change',event=>{if(event.target?.closest?.('#ffv2ComposerForm')&&event.target.name==='kind')requestAnimationFrame(configureComposer)},true);addEventListener('fellowfare:marketplace-changed',event=>{attachPendingSellerTerms();sanitizeMarketplaceState();captureNewListingActivity(event.detail||{});requestAnimationFrame(enhance)});addEventListener('civweave:canonical-rewards-changed',()=>{scanLearningRewards();requestAnimationFrame(renderQuestPanel)});addEventListener('hashchange',()=>requestAnimationFrame(enhance));bindExternalActivityEvents();observer.observe(document.querySelector('#main')||document.body,{childList:true,subtree:true});scanLearningRewards();enhance();refreshMerchantStatus().catch(()=>{})}
const api=Object.freeze({version:VERSION,schema:SCHEMA,stateKey:STATE_KEY,rewardPerQuest:REWARD_PER_QUEST,milestoneSize:MILESTONE_SIZE,milestoneBonus:MILESTONE_BONUS,questPool:()=>copy(QUEST_POOL),today:()=>copy(questView()),balances,read:()=>copy(state),fulfill,recordActivity,milestoneStatus,sanitizeMarketplaceState,completeOrderWithFulfillment,merchantStatus:()=>copy(merchantStatus),refreshMerchantStatus,syncOwnCashListings,directCommerceOrigin:MONEY_EDGE});
globalThis.CivweaveFulfillmentEconomyV2=api;
// Compatibility alias prevents the retired v1 script from booting while preserving
// callers that still use the original global name.
globalThis.CivweaveFulfillmentEconomyV1=api;
if(document.readyState==='loading')addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
