import {newSovereigntyState,normalizeSovereigntyState,createPreferenceProfile,applyPreferenceRails,adoptPreferenceProfile,disablePreferenceProfile,restoreFederationRails,forkStatus,createLocalFork,evaluateLocalForkRails,createLocalForkPacket} from './anarchadia-sovereignty-kernel-v146.js';
const STATE_KEY='commonweave.anarchadia.local-sovereignty.v146';
const PREVIEW_KEY='commonweave.anarchadia.local-preview.v146';
const CHANNEL='commonweave-anarchadia-sovereignty-v146';
const app=document.querySelector('#app'),previewDialog=document.querySelector('#preview-dialog'),previewFrame=document.querySelector('#preview-frame'),previewTitle=document.querySelector('#preview-title');
let state=loadState(),view='overview',draft=null,forkDraft=null;
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const targetUrls={all:'/loom/',commonweave:'/loom/','living-school':'/lite/?system=living-school',cerbanimo:'/lite/?system=cerbanimo',fellowfare:'/app/fellowfare-cabinet-v144.html',anarchadia:'/app/anarchadia-console-v139.html'};
function loadState(){try{return normalizeSovereigntyState(JSON.parse(localStorage.getItem(STATE_KEY)||'null'))}catch{return newSovereigntyState()}}
function saveState(action='state.changed'){localStorage.setItem(STATE_KEY,JSON.stringify(state));try{const channel=new BroadcastChannel(CHANNEL);channel.postMessage({type:'sovereignty-state-changed',action,at:new Date().toISOString()});channel.close()}catch{}}
function download(name,value){const blob=new Blob([typeof value==='string'?value:JSON.stringify(value,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=name;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
function railMarkup(rails){if(!rails)return'<div class="empty">Build a draft to run local compatibility rails.</div>';return `<div class="rail-list">${rails.results.map(row=>`<article class="rail ${row.status}"><b>${row.status==='pass'?'✓':row.status==='review'?'?':'!'}</b><div><strong>${esc(row.label)}</strong><p>${esc(row.note)}</p></div></article>`).join('')}</div>`}
function activeCount(){return state.profiles.filter(profile=>profile.state==='active').length}
function divergentCount(){return state.profiles.filter(profile=>profile.state==='active'&&profile.compatibility?.status==='divergent').length}
function render(){document.querySelectorAll('.as-nav [data-view]').forEach(button=>button.classList.toggle('primary',button.dataset.view===view));if(view==='overview')renderOverview();else if(view==='preferences')renderPreferences();else if(view==='profiles')renderProfiles();else if(view==='source')renderSource();else renderReceipts()}
function renderOverview(){const status=forkStatus(state),diverged=!status.compatible;app.innerHTML=`<section class="hero"><div><span class="eyebrow">Your device, your branch</span><h2>${diverged?'Local fork active':'Locally free. Federatively legible.'}</h2><p>Presentation, accessibility, workflow, labels, routes, local settings, and installed source may be changed here. Compatible changes stay connected. Protocol-changing choices remain possible, but they become an explicit local fork rather than impersonating the shared rails.</p><div class="actions"><button class="primary" data-view="preferences">Make a local preference</button><button data-view="source">Change installed source</button>${diverged?'<button class="danger" data-action="restore-rails">Restore shared rails</button>':''}</div></div><div class="status-orb ${diverged?'diverged':''}"><strong>${diverged?`DIVERGED<br>${esc(status.networkMode)}`:'COMPATIBLE<br>LOCAL'}</strong></div></section>${diverged?`<section class="fork-banner"><span class="eyebrow">Break-glass fork</span><h2>${esc(status.forkId)}</h2><p>${esc(status.reason)}</p><p><strong>Outbound posture:</strong> ${esc(status.networkMode)}. ${status.networkMode==='quarantine'?'Federation, trade, and validation writes are blocked locally.':'Protected writes carry the fork ID and a diverged compatibility label.'}</p></section>`:''}<section class="grid four" style="margin-top:16px"><article class="card metric"><small>Active profiles</small><strong>${activeCount()}</strong><span class="tag pass">local overlays</span></article><article class="card metric"><small>Divergent profiles</small><strong>${divergentCount()}</strong><span class="tag ${divergentCount()?'incompatible':'pass'}">rail posture</span></article><article class="card metric"><small>Targets</small><strong>5</strong><span class="tag">all realms</span></article><article class="card metric"><small>Audit receipts</small><strong>${state.audit.length}</strong><span class="tag">local only</span></article></section><section class="section-head"><div><span class="eyebrow">Three lanes</span><h2>How changes move</h2></div></section><section class="grid three"><article class="card"><h3>Preference lane</h3><p>Declarative visual, accessibility, text, selector, asset, and local-setting changes preview and adopt immediately.</p><span class="tag pass">no vote required</span></article><article class="card"><h3>Compatible source lane</h3><p>Installed files may be changed on a local branch while preserving the shared protocol surfaces and normal test suite.</p><span class="tag pass">connected fork</span></article><article class="card"><h3>Break-glass lane</h3><p>Shared rails may be changed deliberately. The device becomes a labelled local fork and cannot silently emit canonical trade or validation records.</p><span class="tag incompatible">explicit divergence</span></article></section>`}
function renderPreferences(){const rails=draft?.railResult||null,incompatible=rails?.incompatible?.length>0;app.innerHTML=`<section class="section-head"><div><span class="eyebrow">Preference forge</span><h2>Preview and adopt a local behavior</h2></div><div class="actions"><button data-action="import-profile">Import profile</button></div></section><section class="grid two"><form class="card form-grid" id="preference-form"><label>Profile name<input name="name" required value="${esc(draft?.name||'')}"></label><label>Target<select name="target">${['all','commonweave','living-school','cerbanimo','fellowfare','anarchadia'].map(target=>`<option ${draft?.operations?.[0]?.target===target?'selected':''}>${target}</option>`).join('')}</select></label><label class="full">Purpose<textarea name="description" placeholder="What should feel or behave differently on this device?">${esc(draft?.description||'')}</textarea></label><label>Route pattern<input name="route" value="*" placeholder="* or regular expression"></label><label>Accent color<input name="accent" type="color" value="#ff3f91"></label><label>Font scale<input name="scale" type="number" min="0.7" max="2" step="0.05" value="1"></label><label class="check"><input name="reduceMotion" type="checkbox"> Reduce animation locally</label><label>Hide selector<input name="hideSelector" placeholder=".legacy-panel"></label><label>Replace text selector<input name="textSelector" placeholder="[data-label=welcome]"></label><label class="full">Replacement text<textarea name="textValue"></textarea></label><label class="full">Custom CSS<textarea name="css" placeholder="Use installed assets only. Remote @import and remote url() are blocked."></textarea></label><label>Local setting key<input name="settingKey" placeholder="commonweave.preference.example"></label><label>Setting value<input name="settingValue" placeholder="text or JSON"></label><label class="full">Advanced operations JSON<textarea name="advanced" placeholder='[{"type":"attribute","target":"cerbanimo","selector":"...","name":"aria-label","value":"..."}]'></textarea></label><div class="actions full"><button class="primary" type="submit">Build draft & run rails</button>${draft?'<button type="button" data-action="preview-profile">Preview</button><button type="button" class="acid" data-action="adopt-profile">Adopt locally</button>':''}</div></form><section class="card"><span class="eyebrow">Compatibility rails</span><h2>${rails?rails.compatible?'Compatible local preference':rails.blocking.length?'Blocked draft':'Local fork required':'No draft yet'}</h2>${railMarkup(rails)}${draft?`<pre>${esc(JSON.stringify(draft,null,2))}</pre>`:''}</section></section>${incompatible?breakGlassMarkup('profile'):''}`}
function breakGlassMarkup(kind){return `<section class="card fork-banner" id="break-glass"><span class="eyebrow">Break glass</span><h2>This choice changes the shared compatibility story.</h2><p>The change may still be adopted locally. It will create a fork ID and stop the device from presenting incompatible trade, validation, ledger, or federation writes as canonical.</p><div class="form-grid"><label class="full">Why is divergence worth it?<textarea id="fork-reason" placeholder="At least 20 characters"></textarea></label><label>Outbound posture<select id="fork-network"><option value="quarantine">Quarantine protected writes</option><option value="label-and-send">Label incompatible writes and send</option></select></label><label>Type the phrase<input id="fork-phrase" placeholder="FORK MY LOCAL COMMONWEAVE"></label><label class="check full"><input type="checkbox" data-ack="local-fork"> I understand this creates a local fork.</label><label class="check full"><input type="checkbox" data-ack="shared-rejection"> I understand other nodes may reject its records.</label><label class="check full"><input type="checkbox" data-ack="restore-responsibility"> I accept responsibility for restoring compatibility or remaining forked.</label><div class="actions full"><button class="danger" data-action="confirm-${kind}-fork">Adopt after failsafes</button></div></div></section>`}
function compilePreference(form){const data=new FormData(form),target=String(data.get('target')),route=String(data.get('route')||'*'),operations=[];const accent=String(data.get('accent')||'');if(accent)operations.push({type:'css-variable',target,route,selector:':root',name:'--cw-local-accent',value:accent});const scale=Number(data.get('scale')||1);if(scale!==1)operations.push({type:'custom-css',target,route,css:`html{font-size:${Math.round(scale*100)}%!important}`});if(data.get('reduceMotion'))operations.push({type:'custom-css',target,route,css:'*,*::before,*::after{animation-duration:.001ms!important;animation-iteration-count:1!important;transition-duration:.001ms!important;scroll-behavior:auto!important}'});const hideSelector=String(data.get('hideSelector')||'').trim();if(hideSelector)operations.push({type:'hide',target,route,selector:hideSelector});const textSelector=String(data.get('textSelector')||'').trim(),textValue=String(data.get('textValue')||'');if(textSelector)operations.push({type:'text',target,route,selector:textSelector,value:textValue});const css=String(data.get('css')||'').trim();if(css)operations.push({type:'custom-css',target,route,css});const settingKey=String(data.get('settingKey')||'').trim(),raw=String(data.get('settingValue')||'');if(settingKey){let value=raw;try{value=JSON.parse(raw)}catch{}operations.push({type:'setting',target,route,key:settingKey,value})}const advanced=String(data.get('advanced')||'').trim();if(advanced){const parsed=JSON.parse(advanced);if(!Array.isArray(parsed))throw new Error('Advanced operations must be a JSON array.');operations.push(...parsed.map(operation=>({...operation,target:operation.target||target,route:operation.route||route})))}return{name:String(data.get('name')||'Local preference'),description:String(data.get('description')||''),operations}}
function breakGlassValues(){return{phrase:document.querySelector('#fork-phrase')?.value||'',reason:document.querySelector('#fork-reason')?.value||'',networkMode:document.querySelector('#fork-network')?.value||'quarantine',acknowledgements:[...document.querySelectorAll('[data-ack]:checked')].map(input=>input.dataset.ack)}}
function renderProfiles(){app.innerHTML=`<section class="section-head"><div><span class="eyebrow">Adopted profiles</span><h2>${state.profiles.length} local preference record${state.profiles.length===1?'':'s'}</h2></div><div class="actions"><button data-action="export-state">Export sovereignty state</button>${divergentCount()?'<button class="danger" data-action="restore-rails">Restore shared rails</button>':''}</div></section><section class="list">${state.profiles.map(profile=>`<article class="item"><div class="item-head"><div><span class="tag ${profile.compatibility?.status==='compatible'?'pass':'incompatible'}">${esc(profile.compatibility?.status||'unknown')}</span><h3>${esc(profile.name)}</h3><p>${esc(profile.description)}</p></div><code>${esc(profile.profileHash?.slice(0,18)||'')}</code></div><p>${profile.operations?.length||0} operations · ${esc(profile.state)} · ${profile.operations?.map(operation=>operation.target).filter((value,index,array)=>array.indexOf(value)===index).join(', ')}</p><div class="actions"><button data-action="preview-saved" data-id="${esc(profile.id)}">Preview</button>${profile.state==='active'?`<button data-action="disable-profile" data-id="${esc(profile.id)}">Disable</button>`:`<button data-action="readopt-profile" data-id="${esc(profile.id)}">Re-adopt</button>`}<button data-action="export-profile" data-id="${esc(profile.id)}">Export</button><button class="danger" data-action="delete-profile" data-id="${esc(profile.id)}">Delete record</button></div></article>`).join('')||'<div class="empty">No local profiles have been created.</div>'}</section>`}
function renderSource(){const rails=forkDraft?.rails||null,incompatible=rails?.incompatible?.length>0;app.innerHTML=`<section class="section-head"><div><span class="eyebrow">Installed source fork</span><h2>Change any Commonweave platform on a local branch</h2></div></section><section class="notice warn file-help">Browser preferences enact instantly. Source changes produce a tamper-evident local packet. Apply it with <code>npm run anarchadia:fork -- --packet FILE --adopt</code>. The executor never pushes or merges.</section><section class="grid two"><form class="card form-grid" id="source-form"><label>Title<input name="title" required></label><label>Exact installed commit<input name="baseCommit" required pattern="[a-fA-F0-9]{40}"></label><label class="full">Local branch<input name="branch" value="local/anarchadia-fork-my-device" required></label><label class="full">Reason<textarea name="reason"></textarea></label><label class="full">Acceptance checks<textarea name="acceptance" required placeholder="One check per line"></textarea></label><label class="full">Rollback plan<textarea name="rollback" required></textarea></label><label class="full">Files JSON<textarea name="files" required placeholder='[{"path":"public/app/...","content":"..."}]'></textarea></label><div class="actions full"><button class="primary" type="submit">Build source packet</button>${forkDraft?'<button type="button" class="acid" data-action="export-fork-packet">Export executable packet</button>':''}</div></form><section class="card"><span class="eyebrow">Source rails</span><h2>${rails?rails.compatible?'Compatible local branch':rails.blocking.length?'Hard blocked':'Protocol fork required':'No packet yet'}</h2>${railMarkup(rails)}${forkDraft?`<pre>${esc(JSON.stringify(forkDraft,null,2))}</pre>`:''}</section></section>${incompatible?breakGlassMarkup('source'):''}`}
function renderReceipts(){app.innerHTML=`<section class="section-head"><div><span class="eyebrow">Local receipts</span><h2>Inspectable acts, no authority costume</h2></div><button data-action="clear-audit">Clear local audit</button></section><section class="list">${state.audit.map(item=>`<article class="item receipt"><div class="item-head"><strong>${esc(item.action)}</strong><time>${esc(new Date(item.at).toLocaleString())}</time></div><pre>${esc(JSON.stringify(item.detail,null,2))}</pre></article>`).join('')||'<div class="empty">No local sovereignty events have been recorded.</div>'}</section>`}
function openPreview(profile){sessionStorage.setItem(PREVIEW_KEY,JSON.stringify(profile));const target=profile.operations?.[0]?.target||'all',url=targetUrls[target]||'/loom/';previewTitle.textContent=profile.name||'Local preference preview';previewFrame.src=url+(url.includes('?')?'&':'?')+`cwSovereigntyPreview=${Date.now()}`;previewDialog.showModal()}
function closePreview(){previewDialog.close();previewFrame.src='about:blank';sessionStorage.removeItem(PREVIEW_KEY)}
async function buildForkPacket(breakGlass=null){if(!forkDraft)return;const packet=await createLocalForkPacket(forkDraft,breakGlass||{});download(`anarchadia-${forkDraft.targetBranch.replaceAll('/','-')}.json`,packet);state.audit.unshift({id:crypto.randomUUID(),at:new Date().toISOString(),action:'local-fork.packet-exported',detail:{forkId:forkDraft.id,hash:forkDraft.forkHash,compatibility:forkDraft.rails?.compatible?'compatible':'divergent'}});saveState('fork.packet-exported');render()}
document.addEventListener('submit',async event=>{if(event.target.id==='preference-form'){event.preventDefault();try{draft=await createPreferenceProfile(compilePreference(event.target));applyPreferenceRails(draft);render()}catch(error){alert(error.message)}}if(event.target.id==='source-form'){event.preventDefault();try{const data=new FormData(event.target),files=JSON.parse(String(data.get('files')||'[]'));if(!Array.isArray(files))throw new Error('Files must be a JSON array.');forkDraft=await createLocalFork({title:data.get('title'),baseCommit:data.get('baseCommit'),targetBranch:data.get('branch'),reason:data.get('reason'),acceptance:String(data.get('acceptance')||'').split('\n'),rollback:data.get('rollback'),files});forkDraft.rails=evaluateLocalForkRails(forkDraft);render()}catch(error){alert(error.message)}}});
document.addEventListener('click',async event=>{const target=event.target.closest('button,[data-view]');if(!target)return;if(target.dataset.view){view=target.dataset.view;render();return}const action=target.dataset.action;try{if(action==='console')location.assign('/app/anarchadia-console-v139.html');else if(action==='governance')location.assign('/app/anarchadia-governance-v145.html');else if(action==='preview-profile'&&draft)openPreview(draft);else if(action==='close-preview')closePreview();else if(action==='adopt-profile'&&draft){if(draft.railResult?.incompatible?.length){document.querySelector('#break-glass')?.scrollIntoView({behavior:'smooth'});return}const adopted=await adoptPreferenceProfile(state,draft);state=adopted.state;saveState('profile.adopted');draft=null;view='profiles';render()}else if(action==='confirm-profile-fork'&&draft){const adopted=await adoptPreferenceProfile(state,draft,breakGlassValues());state=adopted.state;saveState('profile.fork-adopted');draft=null;view='profiles';render()}else if(action==='preview-saved'){const profile=state.profiles.find(item=>item.id===target.dataset.id);if(profile)openPreview(profile)}else if(action==='disable-profile'){state=disablePreferenceProfile(state,target.dataset.id);saveState('profile.disabled');render()}else if(action==='readopt-profile'){const profile=state.profiles.find(item=>item.id===target.dataset.id);if(!profile)return;if(profile.compatibility?.status==='divergent'){draft=profile;view='preferences';render();return}const adopted=await adoptPreferenceProfile(state,profile);state=adopted.state;saveState('profile.readopted');render()}else if(action==='delete-profile'){state.profiles=state.profiles.filter(item=>item.id!==target.dataset.id);state.audit.unshift({id:crypto.randomUUID(),at:new Date().toISOString(),action:'profile.record-deleted',detail:{profileId:target.dataset.id}});saveState('profile.deleted');render()}else if(action==='export-profile'){const profile=state.profiles.find(item=>item.id===target.dataset.id);if(profile)download(`commonweave-local-profile-${profile.id}.json`,profile)}else if(action==='export-state')download('commonweave-local-sovereignty.json',state);else if(action==='restore-rails'){if(confirm('Disable every divergent profile and return protected network traffic to the shared rails?')){state=restoreFederationRails(state);saveState('rails.restored');render()}}else if(action==='export-fork-packet'&&forkDraft){if(forkDraft.rails?.incompatible?.length){document.querySelector('#break-glass')?.scrollIntoView({behavior:'smooth'});return}await buildForkPacket()}else if(action==='confirm-source-fork'&&forkDraft)await buildForkPacket(breakGlassValues());else if(action==='import-profile')document.querySelector('#import-profile').click();else if(action==='clear-audit'){state.audit=[];saveState('audit.cleared');render()}}catch(error){alert(error.message)}});
document.querySelector('#import-profile').addEventListener('change',async event=>{const file=event.target.files?.[0];if(!file)return;try{const raw=JSON.parse(await file.text());draft=await createPreferenceProfile(raw);applyPreferenceRails(draft);view='preferences';render()}catch(error){alert(error.message)}finally{event.target.value=''}});
previewDialog.addEventListener('close',()=>{previewFrame.src='about:blank';sessionStorage.removeItem(PREVIEW_KEY)});render();
