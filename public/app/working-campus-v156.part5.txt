lThis.CommonweaveModelRuntime)return Promise.resolve(globalThis.CommonweaveModelRuntime);return new Promise((resolve,reject)=>{const prior=[...document.scripts].find(x=>x.src&&new URL(x.src,location.href).pathname==='/app/shared/commonweave-model-runtime.js');if(prior){const timer=setInterval(()=>{if(globalThis.CommonweaveModelRuntime){clearInterval(timer);clearTimeout(timeout);resolve(globalThis.CommonweaveModelRuntime)}},50),timeout=setTimeout(()=>{clearInterval(timer);reject(new Error('The shared model runtime did not become ready.'))},8000);return}const script=document.createElement('script');script.src='/app/shared/commonweave-model-runtime.js?v=working-campus-v156';script.onload=()=>globalThis.CommonweaveModelRuntime?resolve(globalThis.CommonweaveModelRuntime):reject(new Error('The model runtime loaded without its API.'));script.onerror=()=>reject(new Error('The model runtime could not load.'));document.head.append(script)})}
async function testModelRoute(kind){const status=$('#model-test-status'),setup=currentModelSetup();if(setup.route!=='gemini')return;if(!setup.key){status.textContent='Enter a Gemini API key first.';return}if(!setup.consent){status.textContent='Enable remote consent before testing Google services.';return}status.textContent=kind==='agentic'?'Testing Antigravity permission and response…':'Testing Gemini connection…';try{const rt=await ensureModelRuntime();if(kind==='gemini'){const capability=await rt.detectCapabilities({...setup.interactive,apiKey:setup.key},{probe:true});if(capability.available===false)throw new Error('Gemini did not answer the capability probe.');status.textContent=`Gemini is reachable with ${capability.model||setup.interactive.model}.`}else{const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),25000);let result;try{result=await rt.generate({purpose:'working-campus-antigravity-test',executionProfile:'agentic',config:{...setup.agentic,apiKey:setup.key,timeoutMs:24000,maxTokens:64},signal:controller.signal,messages:[{role:'user',content:'Return the single word READY. Do not use tools.'}]})}finally{clearTimeout(timer)}if(result?.status!=='success'||String(result?.actual?.model||'').toLowerCase().indexOf('antigravity')<0)throw new Error(result?.fallback?.reason||result?.error?.message||'The key reached Gemini, but Antigravity was unavailable or fell back.');status.textContent='Antigravity answered directly and is available for agentic work.'}}catch(error){status.textContent=`Test failed: ${error.message}`}}
$$('.realm-node').forEach(b=>b.addEventListener('click',()=>openRealm(b.dataset.realm)));$$('[data-view]').forEach(b=>b.addEventListener('click',()=>{state.view=b.dataset.view;render()}));$('#brand-home').addEventListener('click',()=>{state.view='weave';render()});$('#guided-mode').addEventListener('click',()=>{state.mode='guided';render()});$('#roam-mode').addEventListener('click',()=>{state.mode='roam';render()});$('#settings-button').addEventListener('click',settings);$('#model-chip').addEventListener('click',settings);$('#model-route').addEventListener('change',syncModelFields);$('#test-gemini').addEventListener('click',()=>testModelRoute('gemini'));$('#test-antigravity').addEventListener('click',()=>testModelRoute('agentic'));$('#close-settings').addEventListener('click',()=>$('#settings').close());$('#save-settings').addEventListener('click',()=>{const setup=currentModelSetup();persistModel(setup);$('#model-chip').textContent=$('#model-route').selectedOptions[0].textContent;$('#settings').close();toast('Shared interactive and agentic model routes saved.')});$('#export-button').addEventListener('click',exportPlan);render()})();
