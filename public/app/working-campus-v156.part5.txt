function sharedModelSnapshot(detail){const profiles=parse(localStorage.getItem('commonweave-model-profiles-v1'),{}),legacy=parse(localStorage.getItem('commonweave.universal-ai.v127'),{}),interactive=detail?.interactive||profiles.interactive||legacy||model;return{...model,...interactive,route:interactive.route||interactive.provider||model.route,provider:interactive.provider||interactive.route||model.provider,consent:Boolean(detail?.consent??legacy.consent??model.consent),agenticEnabled:Boolean(detail?.agenticEnabled??profiles.agenticEnabled??legacy.agenticEnabled??model.agenticEnabled),agenticModel:detail?.agentic?.model||profiles.agentic?.model||model.agenticModel||'antigravity'}}
function syncModelChip(detail){model=sharedModelSnapshot(detail);const labels={bundled:'Onboard Reflex',gemini:'Gemini',ollama:'Ollama','openai-compatible':'Compatible API',hosted:'Hosted'};$('#model-chip').textContent=labels[model.provider]||labels[model.route]||'Model settings'}
async function openSharedSettings(){try{if(!globalThis.CommonweaveFamilyAILoaderV105?.openSettings)throw new Error('The shared settings loader is unavailable.');await globalThis.CommonweaveFamilyAILoaderV105.openSettings()}catch(error){toast(`AI settings could not open: ${error.message}`)}}
$$('.realm-node').forEach(b=>b.addEventListener('click',()=>openRealm(b.dataset.realm)));$$('[data-view]').forEach(b=>b.addEventListener('click',()=>{state.view=b.dataset.view;render()}));$('#brand-home').addEventListener('click',()=>{state.view='weave';render()});$('#guided-mode').addEventListener('click',()=>{state.mode='guided';render()});$('#roam-mode').addEventListener('click',()=>{state.mode='roam';render()});$('#settings-button').addEventListener('click',openSharedSettings);$('#model-chip').addEventListener('click',openSharedSettings);addEventListener('commonweave:model-settings-saved',event=>{syncModelChip(event.detail);toast('Shared AI settings saved for the Working Campus and settings bar.')});$('#export-button').addEventListener('click',exportPlan);syncModelChip();render()})();
