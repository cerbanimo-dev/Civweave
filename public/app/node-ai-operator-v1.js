(()=>{
'use strict';
const VERSION='1.0.70-node-ai-operator-v1';
const root=document.getElementById('status'),refreshButton=document.getElementById('refresh');
const clean=(value,max=500)=>String(value??'').trim().slice(0,max);
const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const money=value=>Number.isSafeInteger(Number(value))?`$${(Number(value)/100).toFixed(2)}`:'operator-defined';
function row(label,value,cls=''){return `<dt>${esc(label)}</dt><dd class="${cls}">${esc(value)}</dd>`}
function serviceCard(service){const billing=service.billing||{},backend=service.backend||{},disclosures=service.disclosures||{};return `<div class="service"><strong>${esc(service.label||service.id)}</strong><div class="chips">${(service.capabilities||[]).map(value=>`<span class="chip">${esc(value)}</span>`).join('')}</div><dl class="kv">${row('Service ID',service.id||'')}${row('Maximum retail/request',Number.isSafeInteger(Number(billing.maxRequestCents))?money(Number(billing.maxRequestCents)):'not declared')}${row('Minimum retail',Number.isSafeInteger(Number(billing.minimumChargeCents))?money(Number(billing.minimumChargeCents)):'none declared')}${row('Backend ownership',backend.ownership||'node operator')}${row('Model',backend.model||backend.example||'operator-selected')}${row('Model may change',disclosures.modelMayChange===false?'no':'yes')}</dl></div>`}
async function readJson(path){const response=await fetch(path,{headers:{accept:'application/json'},cache:'no-store'});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(clean(body?.error,1000)||`${path} returned ${response.status}`);return body}
async function render(){refreshButton.disabled=true;root.innerHTML='<section class="card wide"><h2>Reading node status</h2><p class="note">Refreshing public, non-secret node declarations…</p></section>';try{const [manifestEnvelope,inferenceEnvelope,finderEnvelope]=await Promise.all([readJson('/api/ai/node/manifest'),readJson('/api/ai/node/inference/status').catch(error=>({error:error.message})),readJson('/api/finder-status').catch(()=>null)]);const manifest=manifestEnvelope.manifest||{},market=manifestEnvelope.marketplace||{},inference=inferenceEnvelope?.inference||inferenceEnvelope||{},services=Array.isArray(manifest.services)?manifest.services:[],missing=Array.isArray(market.missing)?market.missing:[],baseUrls=manifest.metadata?.endpoints?.baseUrls||[];root.innerHTML=`<section class="card"><h2>Node</h2><dl class="kv">${row('Display name',manifest.displayName||finderEnvelope?.node?.label||manifest.nodeId||'Civweave Node')}${row('Node ID',manifest.nodeId||market.nodeId||'unconfigured')}${row('Operator ID',manifest.operatorId||'unconfigured')}${row('Protocol',manifest.protocol||market.protocol||'unknown')}${row('HTTP reachability',baseUrls.length?baseUrls.join(', '):'mesh-only')}${row('Observed peers',String(finderEnvelope?.peers?.length||0))}</dl></section><section class="card"><h2>Marketplace readiness</h2><dl class="kv">${row('Marketplace requested',market.requested?'yes':'no',market.requested?'ok':'warn')}${row('Marketplace enabled',market.enabled?'yes':'no',market.enabled?'ok':'off')}${row('Ledger storage',market.storage||'unavailable')}${row('Inference ready',inference.ready?'yes':'no',inference.ready?'ok':'warn')}${row('Signed receipts',market.signedSettlementReceipts?'ready':'not configured',market.signedSettlementReceipts?'ok':'warn')}${row('Missing requirements',missing.length?missing.join(', '):'none',missing.length?'warn':'ok')}</dl></section><section class="card wide"><h2>Advertised services</h2><div class="services">${services.length?services.map(serviceCard).join(''):'<p class="note">No node AI services are currently advertised.</p>'}</div></section><section class="card"><h2>Privacy + settlement</h2><dl class="kv">${row('Retention',manifest.privacy?.retention||'operator-declared')}${row('Third-party inference',manifest.privacy?.thirdPartyInference?'disclosed':'not declared')}${row('Settlement cadence',manifest.settlement?.cadence||'periodic')}${row('Mesh receipt publication',manifest.settlement?.meshPublication===false?'disabled':'enabled')}${row('Platform fee',Number.isSafeInteger(Number(manifest.platformFee?.basisPoints))?`${(Number(manifest.platformFee.basisPoints)/100).toFixed(2)}% of gross top-ups`:'not configured')}</dl></section><section class="card"><h2>Reference package</h2><p class="note">A node may attach the optional Fireworks reference service package or any compatible local operator package. The implementation remains behind the node service contract and is not distributed through mesh adverts.</p><dl class="kv">${row('Execution package',inference.ready?'attached and ready':'not attached / incomplete',inference.ready?'ok':'warn')}${row('Live payment processor','not enabled by this slice','warn')}</dl></section><section class="card wide"><h2>Security boundary</h2><p class="note">This status page deliberately omits API keys, capability/auth secrets, receipt private keys, node-internal credentials, user balances, ledger entries, and processor credentials. Wallet details remain session-authenticated and node-specific.</p></section>`}catch(error){root.innerHTML=`<section class="card wide"><h2>Node status unavailable</h2><p class="note">${esc(error.message)}</p></section>`}finally{refreshButton.disabled=false}}
refreshButton?.addEventListener('click',render);render();globalThis.CivweaveNodeAIOperatorV1=Object.freeze({version:VERSION,refresh:render});
})();
