const CACHE='commonweave-pocket-campus-v64-ai-uplift';
const CORE=['./','./index.html','./manifest.webmanifest','./logos/commonweave.webp','./commonweave-world.css','./commonweave-world.js','./commonweave-weaveling-steward.js','./commonweave-living-world.js','./commonweave-intention-orchestrator.js','./commonweave-actionable-quad.js','./commonweave-merlin-chat.css','./shared/commonweave-ai-personas.js','./shared/commonweave-merlin-chat.js','./shared/image-hotspot-calibrator.js','./assets/ai/weaveling-compass.png','./assets/world/town-square-home.webp','./assets/world/inside-quad.webp'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(CORE.map(url=>new Request(url,{cache:'reload'})))).then(()=>self.skipWaiting()))});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key.startsWith('commonweave-pocket-campus-')&&key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim()))});
async function networkFirst(request){const cache=await caches.open(CACHE);try{const response=await fetch(request);if(response.ok)cache.put(request,response.clone());return response}catch(error){const cached=await cache.match(request);if(cached)return cached;throw error}}
async function cacheFirst(request){const cache=await caches.open(CACHE),cached=await cache.match(request);if(cached)return cached;const response=await fetch(request);if(response.ok)cache.put(request,response.clone());return response}
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET')return;const url=new URL(request.url);if(url.origin!==location.origin)return;if(request.mode==='navigate'){event.respondWith(networkFirst(request));return}if(request.destination==='image'||request.destination==='font'){event.respondWith(cacheFirst(request));return}event.respondWith(networkFirst(request))});
