(()=>{
'use strict';
const VERSION='1.0.4-v156';
const CONFIG_KEY='commonweave.mesh-node.v156';
const PAIRING_KEY='commonweave.friend-pairings.v156';
const FRIEND_KEY='commonweave.friends.v156';
const INBOX_KEY='commonweave.mesh-inbox.v156';
const parse=(value,fallback)=>{try{return JSON.parse(value)??fallback}catch{return fallback}};
const read=(key,fallback)=>parse(localStorage.getItem(key),fallback);
const write=(key,value)=>(localStorage.setItem(key,JSON.stringify(value)),value);
const now=()=>new Date().toISOString();
const clean=(value,max=5000)=>String(value??'').trim().slice(0,max);
const list=value=>Array.isArray(value)?value:[];
function event(type,detail={}){try{dispatchEvent(new CustomEvent(type,{detail:{...detail,at:now(),version:VERSION}}))}catch{}}
function config(){const saved=read(CONFIG_KEY,{});return{url:clean(saved.url||location.origin,2000).replace(/\/$/,''),token:clean(sessionStorage.getItem('commonweave.mesh-node-token.v156')||'',500),label:clean(saved.label||'My Commonweave',160)}}
function configure(input={}){const next={url:clean(input.url||location.origin,2000).replace(/\/$/,''),label:clean(input.label||'My Commonweave',160)};write(CONFIG_KEY,next);if(input.token)sessionStorage.setItem('commonweave.mesh-node-token.v156',clean(input.token,500));else if(input.token==='')sessionStorage.removeItem('commonweave.mesh-node-token.v156');event('commonweave:mesh-configured',{config:{...next,tokenPresent:Boolean(input.token)}});return config()}
function headers(){const value={'content-type':'application/json'};const token=config().token;if(token)value.authorization=`Bearer ${token}`;return value}
async function ensureMesh(){if(globalThis.CommonweaveLocalMeshV146)return globalThis.CommonweaveLocalMeshV146;await new Promise((resolve,reject)=>{const script=document.createElement('script');script.src='/app/local-object-mesh-v146.js';script.onload=resolve;script.onerror=()=>reject(new Error('The local mesh runtime could not load.'));document.head.append(script)});if(!globalThis.CommonweaveLocalMeshV146)throw new Error('The local mesh runtime is unavailable.');return globalThis.CommonweaveLocalMeshV146}
function scrub(value,path='$'){if(Array.isArray(value))return value.map((item,index)=>scrub(item,`${path}[${index}]`));if(value&&typeof value==='object'){const out={};for(const [key,child] of Object.entries(value)){if(/api.?key|authorization|bearer|private.?key|passphrase|secret/i.test(key))throw new Error(`Mesh payload contains a protected secret field at ${path}.${key}.`);out[key]=scrub(child,`${path}.${key}`)}return out}if(typeof value==='string'&&/bearer\s+[a-z0-9._-]{12,}|AIza[0-9A-Za-z_-]{20,}/i.test(value))throw new Error(`Mesh payload appears to contain a secret at ${path}.`);return value}
async function postEnvelope(input,node=config()){const response=await fetch(`${node.url}/api/envelopes`,{method:'POST',headers:headers(),body:JSON.stringify(scrub(input))});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||`Node returned ${response.status}.`);return payload.envelope}
async function envelopes(node=config(),nodeId=''){const url=new URL('/api/envelopes',`${node.url}/`);url.searchParams.set('limit','200');if(nodeId)url.searchParams.set('nodeId',nodeId);const response=await fetch(url,{headers:headers(),cache:'no-store'});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||`Node returned ${response.status}.`);return list(payload.envelopes)}
function pairings(){return read(PAIRING_KEY,{pending:{},accepted:{}})}
function savePairings(value){value.pending=value.pending||{};value.accepted=value.accepted||{};return write(PAIRING_KEY,value)}
function friends(){return list(read(FRIEND_KEY,[]))}
function saveFriend(friend){const rows=friends(),index=rows.findIndex(item=>item.id===friend.id),next={...rows[index],...friend,updatedAt:now()};if(index<0)rows.unshift(next);else rows[index]=next;write(FRIEND_KEY,rows.slice(0,200));event('commonweave:friend-changed',{friend:next});return next}
function parseInvite(input){const raw=clean(input,4000);if(!raw)throw new Error('Friend invitation is empty.');try{const url=new URL(raw,location.href),token=url.searchParams.get('cwfriend');if(token)return{nodeUrl:url.origin,token}}catch{}try{const value=JSON.parse(raw);if(value?.schema==='commonweave.friend-code.v156'&&value.token&&value.nodeUrl)return{nodeUrl:value.nodeUrl,token:value.token}}catch{}throw new Error('That is not a Commonweave friend invitation.');}
async function createInvite(options={}){const mesh=await ensureMesh(),node={...config(),url:clean(options.nodeUrl||config().url,2000).replace(/\/$/,'')};configure({url:node.url,label:options.label||node.label,token:options.token??node.token});const owner=await mesh.credential(),offer=await mesh.createOffer(),token=crypto.randomUUID(),expiresAt=new Date(Date.now()+20*60*1000).toISOString();await postEnvelope({schema:'commonweave.friend-rendezvous.v156',from:owner.id,to:'*',kind:'friend-offer-v156',subject:'Commonweave friend invitation',correlationId:token,payload:{schema:'commonweave.friend-offer.v156',token,offer:offer.offer,inviterId:owner.id,inviterKey:owner.publicKey,label:options.label||node.label,expiresAt}},node);const state=pairings();state.pending[token]={token,sessionId:offer.sessionId,nodeUrl:node.url,createdAt:now(),expiresAt,role:'inviter'};savePairings(state);const code=`${node.url}/?cwfriend=${encodeURIComponent(token)}`;event('commonweave:friend-invite-created',{token,code,expiresAt});return{token,code,nodeUrl:node.url,expiresAt}}
async function acceptInvite(input,options={}){const parsed=parseInvite(input),node={...config(),url:parsed.nodeUrl};configure({url:node.url,label:options.label||config().label,token:options.token??node.token});const mesh=await ensureMesh(),owner=await mesh.credential(),rows=await envelopes(node,owner.id),offerEnvelope=rows.slice().reverse().find(item=>item.kind==='friend-offer-v156'&&item.correlationId===parsed.token);if(!offerEnvelope)throw new Error('The friend invitation was not found on that node.');const offer=offerEnvelope.payload;if(Date.parse(offer.expiresAt||0)<=Date.now())throw new Error('The friend invitation expired. Ask for a new one.');const answer=await mesh.acceptOffer(offer.offer);await postEnvelope({schema:'commonweave.friend-rendezvous.v156',from:owner.id,to:offer.inviterId,kind:'friend-answer-v156',subject:'Commonweave friend answer',correlationId:parsed.token,payload:{schema:'commonweave.friend-answer.v156',token:parsed.token,answer:answer.answer,answererId:owner.id,answererKey:owner.publicKey,label:options.label||config().label,createdAt:now()}},node);const state=pairings();state.accepted[parsed.token]={token:parsed.token,sessionId:answer.sessionId,nodeUrl:node.url,role:'answerer',peerId:offer.inviterId,createdAt:now()};savePairings(state);saveFriend({id:offer.inviterId,label:offer.label||'Commonweave friend',publicKey:offer.inviterKey,nodeUrl:node.url,status:'node-fallback-ready',pairingToken:parsed.token,addedAt:now()});event('commonweave:friend-invite-accepted',{token:parsed.token,peerId:offer.inviterId});return{token:parsed.token,peerId:offer.inviterId,status:'answer-posted'}}
async function finishInvite(token){const state=pairings(),pending=state.pending[token];if(!pending)throw new Error('No pending friend invitation matches that token.');const node={...config(),url:pending.nodeUrl},mesh=await ensureMesh(),owner=await mesh.credential(),rows=await envelopes(node,owner.id),answerEnvelope=rows.slice().reverse().find(item=>item.kind==='friend-answer-v156'&&item.correlationId===token);if(!answerEnvelope)return{pending:true,token};await mesh.acceptAnswer(pending.sessionId,answerEnvelope.payload.answer);saveFriend({id:answerEnvelope.payload.answererId,label:answerEnvelope.payload.label||'Commonweave friend',publicKey:answerEnvelope.payload.answererKey,nodeUrl:node.url,status:'node-fallback-ready',pairingToken:token,addedAt:now()});delete state.pending[token];state.accepted[token]={token,peerId:answerEnvelope.payload.answererId,nodeUrl:node.url,role:'inviter',connectedAt:now()};savePairings(state);event('commonweave:friend-paired',{token,peerId:answerEnvelope.payload.answererId});return{pending:false,token,peerId:answerEnvelope.payload.answererId}}
async function finishAll(){const results=[];for(const token of Object.keys(pairings().pending||{}))try{results.push(await finishInvite(token))}catch(error){results.push({token,error:error.message})}return results}
function intendedFor(object,localId){if(object.consent==='private')return false;if(['public','federated'].includes(object.consent))return true;if(object.consent==='direct')return list(object.audience).includes(localId);return false}
async function publishObject({kind,purpose,payload,consent='federated',audience=[]}){const mesh=await ensureMesh(),safePayload=scrub(payload),object=await mesh.createObject({kind,purpose,payload:safePayload,consent,audience,publish:false,hopLimit:4}),owner=await mesh.deviceId();await mesh.queue(object).catch(()=>{});await postEnvelope({schema:'commonweave.community-object-envelope.v1',from:owner,to:object.audience.length===1?object.audience[0]:'*',kind:'community-object',subject:object.kind,payload:object,correlationId:object.id});event('commonweave:mesh-object-published',{id:object.id,kind:object.kind,transport:['node-relay','webrtc-outbox']});return object}
const publishTrade=listing=>publishObject({kind:'fellowfare.trade.request.v156',purpose:'circulate a goods or services request',payload:listing,consent:listing.visibility==='direct'?'direct':'federated',audience:list(listing.audience)});
const requestValidation=request=>publishObject({kind:'cerbanimo.validation.request.v156',purpose:'request independent AI proof validation',payload:request,consent:request.validatorId?'direct':'federated',audience:request.validatorId?[request.validatorId]:[]});
const publishValidation=receipt=>publishObject({kind:'cerbanimo.validation.receipt.v156',purpose:'return a signed proof-validation receipt',payload:receipt,consent:'direct',audience:[receipt.requesterId]});
function saveInbox(object){const rows=list(read(INBOX_KEY,[]));if(!rows.some(item=>item.id===object.id))rows.unshift({id:object.id,kind:object.kind,payload:object.payload,origin:object.origin,receivedAt:object.receivedAt||now(),status:'unread'});write(INBOX_KEY,rows.slice(0,500));event('commonweave:mesh-inbox',{object,unread:rows.filter(item=>item.status==='unread').length})}
async function syncNode(){const mesh=await ensureMesh(),localId=await mesh.deviceId(),rows=await envelopes(config(),localId);let received=0,rejected=0;for(const envelope of rows){if(envelope.kind!=='community-object'||!envelope.payload)continue;const object=envelope.payload;if(object.origin?.nodeId===localId||!intendedFor(object,localId))continue;try{const result=await mesh.ingest(object,{fromPeer:envelope.from,localNodeId:localId});if(result.status==='accepted')received++}catch{rejected++}}event('commonweave:node-sync',{received,rejected});return{received,rejected}}
async function bindIncoming(){const mesh=await ensureMesh();mesh.subscribe(async message=>{if(message.type!=='object-received'||!message.detail?.id)return;try{const object=await mesh.getObject(message.detail.id);if(object&&['fellowfare.trade.request.v156','cerbanimo.validation.request.v156','cerbanimo.validation.receipt.v156'].includes(object.kind))saveInbox(object)}catch{}});await syncNode().catch(()=>{})}
function renderInvite(canvas,code){if(!globalThis.CommonweaveQRV156)throw new Error('The local QR renderer has not loaded.');return globalThis.CommonweaveQRV156.canvas(canvas,code,{scale:5,margin:4})}
async function scanInvite(video){if(!('BarcodeDetector'in globalThis))throw new Error('This browser cannot scan QR codes. Paste the friend invitation instead.');const detector=new BarcodeDetector({formats:['qr_code']}),stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});video.srcObject=stream;await video.play();return new Promise((resolve,reject)=>{const stop=value=>{stream.getTracks().forEach(track=>track.stop());value instanceof Error?reject(value):resolve(value)},deadline=Date.now()+30000;const tick=async()=>{try{const results=await detector.detect(video);if(results[0]?.rawValue)return stop(results[0].rawValue);if(Date.now()>deadline)return stop(new Error('No QR code was detected.'));requestAnimationFrame(tick)}catch(error){stop(error)}};tick()})}
bindIncoming().catch(()=>{});addEventListener('focus',()=>{finishAll().catch(()=>{});syncNode().catch(()=>{})});addEventListener('online',()=>syncNode().catch(()=>{}));setInterval(()=>{if(!document.hidden)syncNode().catch(()=>{})},60000);
globalThis.CommonweaveMeshToolsV156=Object.freeze({VERSION,configure,config,createInvite,acceptInvite,finishInvite,finishAll,parseInvite,friends,pairings,publishTrade,requestValidation,publishValidation,publishObject,renderInvite,scanInvite,envelopes,syncNode,bindIncoming});
})();
