(()=>{
'use strict';
const VERSION='1.2.0-weaveling-plan-json-v190-ai-quest-intent';
const INTENTIONS_KEY='civweave.intentions.v127';
const CAMPUS_KEY='civweave.working-campus.v1';
const HANDOFF_KEY='civweave.realm-inbox.v1';
const LOCAL_SELECTION_KEY='civweave.local-ai.selection.v266';
const PENDING_PROJECT_KEY='civweave.weaveling.quest.pending.v1';
const PENDING_PROJECT_TTL_MS=2*60*60*1000;
const TASK_DEEP_MODEL='gemma4-e4b-it-litert-web';
const PLAN_TRIGGER=/\b(plan|roadmap|routine|program|curriculum|pathway|steps|practice schedule|reviewable weave|reviewable quest|set (?:an )?intention|teach me|help me learn)\b|\b(i want|i wish|my wish|my goal|we want|we wish|let'?s|help me)\b/i;
const FOLLOWUP_PLAN_TRIGGER=/^\s*(?:yes[,! ]*)?(?:(?:can|could|would)\s+you\s+)?(?:please\s+)?(?:go\s+ahead\s+and\s+)?(?:draft|build|make|create|generate|structure|map|outline|write|do)\s+(?:it|that|this)(?:\s+for\s+(?:me|us))?\s*[.!?]*\s*$/i;
const PLAN_CONTEXT=/\b(plan|quest|weave|roadmap|curriculum|pathway|mapping phase|review(?:able)?|draft|steps?)\b/i;
const EXPLICIT_PLAN_REQUEST=/\b(plan|roadmap|reviewable weave|reviewable quest|quest|weave|outline|map out|step[- ]by[- ]step|steps)\b/i;
const PENDING_CANCEL=/^\s*(?:cancel|never\s*mind|forget it|stop|drop it)\b/i;
const QUESTION_REPLY=/^\s*(?:what|which|who|where|when|why|how|should|can|could|would|do|does|is|are|will)\b[^.!]*\?\s*$/i;
const LEARNING_GOAL=/\b(?:i\s+(?:want|need|would\s+like|['’]?d\s+like|hope|plan|intend|aim)\s+to\s+(?:learn|study|master|practice|understand)|my\s+(?:goal|objective)\s+is\s+to\s+(?:learn|study|master|practice|understand)|help\s+me\s+(?:learn|study|master|practice|understand)|(?:can|could|would)\s+you\s+help\s+me\s+(?:learn|study|master|practice|understand)|teach\s+me\s+(?:how\s+to\s+)?|(?:learn|study|master|practice)\s+(?:how\s+to\s+)?[a-z0-9])\b/i;
const PROJECT_ACTION='(?:make|build|create|start|organize|launch|open|set\\s*up|form|develop|design|run|establish|put\\s+together|write|learn|improve|change|grow|find)';
const DIRECT_PROJECT_RE=new RegExp(`^(?:(?:please|kindly)\\s+|(?:(?:can|could|would)\\s+you\\s+)|help\\s+(?:me|us)\\s+)?${PROJECT_ACTION}\\b`,'i');
const INTENT_PROJECT_RE=new RegExp(`\\b(?:i|we)\\s+(?:want|need|would\\s+like|wish|hope|plan|intend|aim)\\s+to\\s+${PROJECT_ACTION}\\b`,'i');
const COLLECTIVE_PROJECT_RE=new RegExp(`\\b(?:me\\s+and\\s+(?:my\\s+)?friends|my\\s+friends\\s+and\\s+(?:me|i)|friends?\\s+and\\s+i|our\\s+(?:friends|group|team|family|community)|we)\\b[^.!?]{0,90}\\b${PROJECT_ACTION}\\b`,'i');
const CONTROL={activate:/^(?:please\s+)?(?:activate|activate it|activate weave|activate the weave|activate quest|activate the quest|approve|approve it|yes,? activate)(?:\s+now)?[.!]?$/i,review:/^(?:please\s+)?(?:review|review it|review weave|review the weave|review quest|review the quest|show plan|show the plan|show weave|show quest)[.!]?$/i,revise:/^(?:please\s+)?(?:revise|revise it|revise weave|revise the weave|revise quest|revise the quest|change plan|change the plan)[.!]?$/i,pause:/^(?:please\s+)?(?:pause|pause it|return to review|deactivate weave|deactivate quest)[.!]?$/i};
const REALMS=new Set(['living-school','cerbanimo','fellowfare','anarchadia']);
const TYPES=new Set(['learning','skilled-labor','material-acquirement','civic-governance']);
const NON_AI_PROVIDERS=new Set(['','deterministic','bundled','packaged','reflex','minilm','local-reflex','manual']);
const LOCAL_AI_PROVIDERS=new Set(['downloaded-local','generative-local','smollm2','smollm3','qwen','local-ai','browser']);
const GEMMA4_LOCAL=/^gemma4-e(?:2|4)b-it-(?:litert-web|q4f16|q2f16-mobile)$/i;
const LIVING_PLAN_PURPOSE='living-school-learning-plan-review-v2';
const parse=(value,fallback)=>{try{const out=JSON.parse(value);return out==null?fallback:out}catch{return fallback}};
const clean=(value,max=12000)=>String(value??'').trim().slice(0,max);
const now=()=>new Date().toISOString();
const uid=prefix=>`${prefix}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;
const strings=(value,maxItems=8,maxLength=900)=>Array.isArray(value)?value.map(item=>clean(item,maxLength)).filter(Boolean).slice(0,maxItems):[];
const PLAN_SCHEMA={type:'object',required:['title','wish','outcome','assumptions','paths'],properties:{title:{type:'string'},wish:{type:'string'},outcome:{type:'string'},assumptions:{type:'array',minItems:1,maxItems:8,items:{type:'string'}},paths:{type:'array',minItems:1,maxItems:4,items:{type:'object',required:['type','realm','title','purpose','steps','completionCriteria','evidence'],properties:{type:{type:'string',enum:['learning','skilled-labor','material-acquirement','civic-governance']},realm:{type:'string',enum:['living-school','cerbanimo','fellowfare','anarchadia']},title:{type:'string'},purpose:{type:'string'},steps:{type:'array',minItems:1,maxItems:8,items:{type:'string'}},completionCriteria:{type:'string'},evidence:{type:'array',minItems:1,maxItems:8,items:{type:'string'}}}}},governance:{type:'object',required:['title','purpose','agreements','reviewQuestion'],properties:{included:{type:'boolean'},title:{type:'string'},purpose:{type:'string'},agreements:{type:'array',maxItems:8,items:{type:'string'}},reviewQuestion:{type:'string'}}},confidence:{type:'number'}}};
function stripGreeting(value=''){return clean(value,4000).replace(/^\s*(?:(?:hi|hello|hey|yo|good\s+(?:morning|afternoon|evening))[\s,!;:.-]*)+/i,'').trim()}
function learningJourneyIntent(text=''){const value=stripGreeting(text);return Boolean(value&&LEARNING_GOAL.test(value))}
function explicitProjectIntent(text=''){const value=stripGreeting(text);if(!value)return false;if(PLAN_TRIGGER.test(value)||DIRECT_PROJECT_RE.test(value)||INTENT_PROJECT_RE.test(value)||COLLECTIVE_PROJECT_RE.test(value))return true;if(/^we\s+[^.!?]{0,40}\b(?:make|build|create|start|organize|launch|open|set\s*up|form|develop|design|run|establish)\b/i.test(value))return true;return false}
function rows(history=[]){return(Array.isArray(history)?history:[]).slice(-24).map(item=>({role:clean(item?.role,40).toLowerCase(),text:clean(item?.text||item?.content,5000)})).filter(item=>item.text)}
function priorPlanningText(history=[]){for(const row of [...rows(history)].reverse())if(row.role==='user'&&explicitProjectIntent(row.text))return row.text;return''}
function contextualPlanIntent(text,history=[]){const value=clean(text,4000);if(!FOLLOWUP_PLAN_TRIGGER.test(value))return false;return rows(history).slice(-10).some(row=>(row.role==='user'&&explicitProjectIntent(row.text))||(row.role==='assistant'&&PLAN_CONTEXT.test(row.text)))}
function resolvedPlanningText(text,history=[]){return contextualPlanIntent(text,history)?priorPlanningText(history)||clean(text,4000):clean(text,4000)}
function hasResponseLayer(fn,flag){let current=fn,depth=0;while(typeof current==='function'&&depth<20){if(current[flag])return true;current=current.__prior;depth++}return false}
function compactPlan(plan){if(!plan)return null;return{id:plan.id,title:plan.title,wish:plan.wish,outcome:plan.outcome,state:plan.state,assumptions:strings(plan.assumptions,8,500),paths:(plan.paths||[]).slice(0,4).map(path=>({type:path.type,realm:path.realm,title:path.title,purpose:path.purpose,steps:strings(path.steps,8,600),completionCriteria:path.completionCriteria})),governance:plan.governance?{title:plan.governance.title,purpose:plan.governance.purpose,agreements:strings(plan.governance.agreements,8,500),reviewQuestion:plan.governance.reviewQuestion}:null}}
function selectedLocal(){try{const live=globalThis.CivweaveLocalModelDownloadV266?.selection?.();if(live?.active&&live.id)return live}catch{}try{const saved=parse(localStorage.getItem(LOCAL_SELECTION_KEY),{});return saved?.active&&saved.id?saved:null}catch{return null}}
function readPendingProject(){const pending=parse(localStorage.getItem(PENDING_PROJECT_KEY),null);if(!pending?.text)return null;const age=Date.now()-Number(pending.createdMs||0);if(!Number.isFinite(age)||age<0||age>PENDING_PROJECT_TTL_MS){clearPendingProject();return null}return pending}
function savePendingProject(text){const value={schema:'civweave.weaveling.pending-quest.v1',text:clean(text,5000),createdAt:now(),createdMs:Date.now()};try{localStorage.setItem(PENDING_PROJECT_KEY,JSON.stringify(value))}catch{}return value}
function clearPendingProject(){try{localStorage.removeItem(PENDING_PROJECT_KEY)}catch{}}
function pendingMatchesHistory(pending,history=[]){const target=clean(pending?.text,5000);return Boolean(target&&rows(history).slice(-12).some(row=>row.role==='user'&&clean(row.text,5000)===target))}
function shouldQualifyProject(text){const value=stripGreeting(text);if(learningJourneyIntent(value)||!explicitProjectIntent(value)||EXPLICIT_PLAN_REQUEST.test(value))return false;const words=value.split(/\s+/).filter(Boolean);return value.length<180&&words.length<30}
function substantiveQualifierReply(text){const value=clean(text,4000);if(!value||PENDING_CANCEL.test(value)||QUESTION_REPLY.test(value))return false;return value.length>=28||value.split(/\s+/).filter(Boolean).length>=6}
function combinedPendingText(pending,current){return `${clean(pending?.text,5000)}\n\nAdditional direction from the Hero:\n${clean(current,5000)}`.trim()}
function memory(history,text){const campus=parse(localStorage.getItem(CAMPUS_KEY),{}),intentions=parse(localStorage.getItem(INTENTIONS_KEY),[]),handoffs=parse(localStorage.getItem(HANDOFF_KEY),[]);return{schema:'civweave.weaveling-working-context.v190',currentRequest:clean(text,8000),resolvedPlanningRequest:resolvedPlanningText(text,history),currentWish:clean(campus.wish,4000),currentProfile:campus.profile&&typeof campus.profile==='object'?campus.profile:{},currentPlan:compactPlan(campus.plan),savedWeaves:(Array.isArray(intentions)?intentions:[]).filter(item=>item?.kind==='weave-plan').slice(0,8).map(item=>compactPlan(item.plan)),recentHandoffs:(Array.isArray(handoffs)?handoffs:[]).slice(0,12).map(item=>({target:item.target,kind:item.kind,title:item.title,status:item.status,weaveId:item.payload?.weaveId})),recentConversation:rows(history).map(item=>({role:item.role,text:clean(item.text,1600)}))}}
function prompt(mem){return[{role:'system',content:`You are Weaveling, Civweave's central mirror and Quest architect. You are the author of the user-specific Quest content. Turn the user's stated wish and supplied working context into a concrete, editable Quest. Preserve the user's actual premise and wording. Do not respond as though you personally need physical tools, money, land, permits, code, or the ability to perform the project. Your job is to design the user's Quest for them. Do not silently add an antagonist, genre, relationship, deadline, business model, location, budget, success condition, or other constraint the user did not state. Infer useful specifics from the request and conversation, but expose uncertain inferences as assumptions. Use Living School only for knowledge or practice, Cerbanimo only for skilled work and production, FellowFare only when materials, services, people, money, transport, or exchange are actually needed, and Anarchadia only for consent, roles, governance, automation, or a passport/review layer. Choose only the paths this particular request needs. Every title, purpose, step, completion criterion, evidence item, and governance agreement must be specific to this user's request rather than generic boilerplate. Prefer 2-3 focused paths with 3-5 concrete steps each unless the request clearly needs more. If the current request includes an earlier project request followed by “Additional direction from the Hero,” treat the additional direction as clarification of that same Quest. If the current request is a pronoun continuation such as “draft that,” resolve it from resolvedPlanningRequest and recentConversation rather than treating “that” as the wish. The Quest remains in REVIEW and nothing is activated. Return only JSON matching the supplied schema.`},{role:'user',content:`Generate the reviewable Quest from this working context:\n${JSON.stringify(mem)}`}];}
function latestItem(){const items=parse(localStorage.getItem(INTENTIONS_KEY),[]);return(Array.isArray(items)?items:[]).find(item=>item?.kind==='weave-plan'&&item?.state!=='completed')||null}
function control(text){for(const[action,pattern]of Object.entries(CONTROL))if(pattern.test(clean(text)))return action;return''}
function planIntent(text,history=[],context={}){const value=clean(text,4000);if(!value||learningJourneyIntent(value))return false;try{if(globalThis.CivweaveIntentionPlanner?.shouldCreate?.({text:value,history,context}))return true}catch{}return explicitProjectIntent(value)||contextualPlanIntent(value,history)}
function normalizePath(path,index){const realm=clean(path?.realm,80),type=clean(path?.type,80),title=clean(path?.title,240),purpose=clean(path?.purpose,1200),steps=strings(path?.steps,8,700),completionCriteria=clean(path?.completionCriteria,1200),evidence=strings(path?.evidence,8,500);if(!REALMS.has(realm)||!TYPES.has(type)||!title||!purpose||!steps.length||!completionCriteria||!evidence.length)throw new Error(`AI Quest path ${index+1} was incomplete after schema validation.`);return{id:clean(path?.id,120)||uid('path'),type,realm,title,purpose,steps,completionCriteria,evidence,progress:[],status:'ready'}}
function normalizeGovernance(value){if(value==null||value?.included===false)return null;if(typeof value!=='object')throw new Error('AI Quest governance was not an object.');const title=clean(value.title,240),purpose=clean(value.purpose,1200),agreements=strings(value.agreements,8,500),reviewQuestion=clean(value.reviewQuestion,700);if(!title||!purpose||!reviewQuestion)throw new Error('AI Quest governance was incomplete after schema validation.');return{realm:'anarchadia',title,purpose,agreements,reviewQuestion,status:'draft'}}
function normalizePlan(raw,{text,history,context,provider,model,latestRequest}={}){const planner=globalThis.CivweaveIntentionPlanner,source=raw&&typeof raw==='object'?raw:null;if(!source)throw new Error('AI Quest generation returned no structured object.');const title=clean(source.title,240),wish=clean(source.wish,4000),outcome=clean(source.outcome,2400),assumptions=strings(source.assumptions,8,700),rawPaths=Array.isArray(source.paths)?source.paths:[];if(!title||!wish||!outcome||!assumptions.length||!rawPaths.length)throw new Error('AI Quest generation returned an incomplete Quest.');const paths=rawPaths.slice(0,4).map(normalizePath),createdAt=now(),turns=planner?.activeIntentionTurns?.(history,text)||[clean(text)];return{schema:'civweave.intention-weave.v1',id:uid('weave'),title,wish,latestRequest:clean(latestRequest||text,4000),outcome,state:'review',createdAt,updatedAt:createdAt,sourceConversation:turns,signals:{aiGenerated:true},assumptions,paths,governance:normalizeGovernance(source.governance),requiresExplicitActivation:true,reviewOptions:['Revise the governing intention','Remove or reorder a path','Edit assumptions or completion criteria','Activate only after the Quest feels usable'],routing:{system:'civweave',room:context?.routingAnswer?.room||'civweave.quad',mode:'Plan'},authoring:{mode:'model-structured-json',aiGenerated:true,provider:provider||'unknown',model:model||'',revision:VERSION},confidence:Number(source.confidence)||null}}
function planResponse(item,provider,model){const plan=item.plan,format=globalThis.CivweaveIntentionPlanner?.format;return{response:{answer:format?format(plan):`I generated and saved a reviewable Quest for “${plan.title}.”`,choice:{mode:'Plan',system:'civweave',room:plan.routing?.room||'civweave.quad',nextAction:'Review, revise, or activate the saved Quest.'},assumptions:plan.assumptions||[],requiresConsent:true,confidence:.96,approvalGate:{kind:'intention-activation',planId:item.id,state:item.state||'review',required:true,actions:['review','revise','activate']}},provider,requestedProvider:provider,model,plan,planItemId:item.id,planControl:null,fallbackFrom:null,questAuthoring:{aiGenerated:true,provider,model,schema:VERSION}}}
function generationFailure(error,cfg={}){const message=clean(error?.message||error||'AI generation did not complete.',1400),provider=clean(cfg.provider||cfg.route||(selectedLocal()?.id?'downloaded-local':'unknown'),120),model=clean(cfg.model||selectedLocal()?.id||'',240);return{response:{answer:`I could not generate a Quest from that request because the selected AI did not return a valid complete Quest. Nothing was created or saved.\n\nGeneration detail: ${message}`,choice:{mode:'Plan',system:'civweave',room:'civweave.quad',nextAction:'Retry the Quest generation or choose another AI model.'},assumptions:[],requiresConsent:false,confidence:1},provider:'weaveling-ai-generation-failed',requestedProvider:provider,model,plan:null,planItemId:null,planControl:null,fallbackFrom:null,questAuthoring:{aiGenerated:false,required:true,questCreated:false,error:message}}}
function controlResponse(action){const item=latestItem();if(!item?.plan)return null;const ui=globalThis.CivweaveIntentionUI;let result={ok:true,item};if(action==='activate')result=ui?.activate?.(item.id)||{ok:false,error:'The intention controls are unavailable.'};else if(action==='pause')result=ui?.review?.(item.id)||{ok:false,error:'The intention controls are unavailable.'};else if(action==='review'){ui?.open?.(item.id)}else if(action==='revise'){ui?.open?.(item.id)}if(!result?.ok)return{response:{answer:result?.error||'The saved Quest could not be changed.',choice:{mode:'Plan',system:'civweave',room:'civweave.quad',nextAction:'Open the review panel and inspect the saved Quest.'},assumptions:[],requiresConsent:false,confidence:.99},provider:'civweave-control',model:VERSION,planControl:{action,ok:false},fallbackFrom:null};const refreshed=latestItem()||item,active=action==='activate',answer=action==='activate'?`The Quest “${refreshed.plan.title}” is active. Its routed paths may now move into the realms.`:action==='pause'?`The Quest “${refreshed.plan.title}” has returned to REVIEW.`:action==='revise'?`I opened “${refreshed.plan.title}” for revision. Change the intention, paths, assumptions, or completion criteria before activation.`:`I opened “${refreshed.plan.title}” for review.`;return{response:{answer,choice:{mode:'Plan',system:'civweave',room:'civweave.quad',nextAction:active?'Follow the next rail or revise the active Quest.':'Review, revise, or activate the saved Quest.'},assumptions:refreshed.plan.assumptions||[],requiresConsent:false,confidence:.99,approvalGate:{kind:'intention-activation',planId:refreshed.id,state:refreshed.state||refreshed.plan.state,required:!active,actions:['review','revise','activate']}},provider:'civweave-control',requestedProvider:'civweave-control',model:VERSION,plan:refreshed.plan,planItemId:refreshed.id,planControl:{action,ok:true},fallbackFrom:null}}
function localResultText(result){if(typeof result==='string')return clean(result,200000);if(result?.status&&!['success','fallback'].includes(result.status))throw new Error(result?.error?.message||`Local AI ended with ${result.status}.`);return clean(result?.outputText||result?.text||result?.output||result?.generatedText||result?.message,200000)}
async function preferredTaskModel(baseCfg={},selected=selectedLocal()){const current=clean(selected?.id||baseCfg.model,240);if(!selected?.active||!current||!GEMMA4_LOCAL.test(current))return current;try{const status=await globalThis.CivweaveLocalModelDownloadV266?.status?.(TASK_DEEP_MODEL);if(status?.available)return TASK_DEEP_MODEL}catch{}return current}
function localStructuredTransport(kind='quest'){return async({config,messages,signal,emit})=>{const runtime=globalThis.CivweaveLocalChatRuntimeV295,pick=selectedLocal(),modelId=clean(config?.model||pick?.id,240),isQuest=kind==='quest',flag=isQuest?'structuredQuest':'structuredLearningPlan';if(!runtime?.generate)throw new Error(`The downloaded-local generation runtime is not ready for structured ${isQuest?'Quest':'Learning Journey plan'} authoring.`);if(!pick?.active||!pick.id)throw new Error(`No downloaded local AI model is selected for ${isQuest?'Quest':'Learning Journey plan'} authoring.`);if(signal?.aborted)throw signal.reason||new DOMException('Generation cancelled','AbortError');const systemPrompt=messages.filter(row=>row?.role==='system').map(row=>clean(row.content,24000)).filter(Boolean).join('\n\n'),chatMessages=messages.filter(row=>row?.role!=='system').map(row=>({role:row?.role==='assistant'?'assistant':'user',content:clean(row?.content,24000)})).filter(row=>row.content),maxNewTokens=Math.max(900,Math.min(2800,Number(config?.maxTokens)||1800));let accumulated='';const onAbort=()=>{try{globalThis.CivweaveLocalModelRuntimeV266?.shutdown?.({reason:isQuest?'structured-quest-cancelled':'structured-learning-plan-cancelled'})}catch{}};signal?.addEventListener?.('abort',onAbort,{once:true});const generateArgs={systemPrompt,messages:chatMessages,maxNewTokens,onToken:token=>{const piece=clean(typeof token==='string'?token:token?.text,12000);if(!piece)return;accumulated+=piece;try{emit?.('partial',{text:piece,accumulatedText:accumulated,[flag]:true,provider:'downloaded-local',model:modelId||pick.id})}catch{}},onProgress:progress=>{try{emit?.(progress?.phase||'generating',{...progress,[flag]:true,provider:'downloaded-local',model:modelId||pick.id})}catch{}}};try{emit?.('generating',{[flag]:true,provider:'downloaded-local',model:modelId||pick.id,maxNewTokens});const fast=globalThis.CivweaveLiteRTGemma4FastRuntimeV1;let result;if(/^gemma4-e(?:2|4)b-it-litert-web$/i.test(modelId)&&typeof fast?.runFast==='function')result=await fast.runFast(generateArgs,modelId);else result=await runtime.generate(generateArgs);const text=localResultText(result);if(!text)throw new Error(`The selected local AI returned no structured ${isQuest?'Quest':'Learning Journey plan'} text.`);const actualModel=clean(result?.executionId||result?.model?.id||modelId||pick.id,240)||pick.id;return{text,payload:result,provider:'downloaded-local',model:actualModel,streamed:Boolean(accumulated),diagnostics:[`${isQuest?'Quest':'Learning Journey plan'} JSON was generated by the downloaded local AI model ${actualModel}.`]}}finally{signal?.removeEventListener?.('abort',onAbort)}}}
function installLocalStructuredPlanBridge(){const rt=globalThis.CivweaveModelRuntime;if(!rt?.generate)return false;if(hasResponseLayer(rt.generate,'__cwLocalStructuredPlanBridgeV1'))return true;const priorFn=rt.generate,prior=priorFn.bind(rt),generate=async request=>{const provider=clean(request?.config?.provider||request?.config?.route,120).toLowerCase();if(request?.purpose===LIVING_PLAN_PURPOSE&&LOCAL_AI_PROVIDERS.has(provider)&&!request?.transport){const selected=selectedLocal(),taskModel=await preferredTaskModel(request?.config||{},selected),config={...(request?.config||{}),provider:'downloaded-local',route:'downloaded-local',model:taskModel||selected?.id||request?.config?.model||TASK_DEEP_MODEL,timeoutMs:600000,maxTokens:Math.max(1800,Number(request?.config?.maxTokens)||0),stream:false};return prior({...request,config,transport:localStructuredTransport('learning-plan'),__civweaveSkipResponseRouter:true,__civweaveLocalStructuredLearningPlan:true,__civweaveDeepTaskModel:true,__civweaveStructuredTaskTimeoutMs:600000})}return prior(request)};generate.__cwLocalStructuredPlanBridgeV1=true;generate.__prior=priorFn;for(const key of Object.keys(priorFn))try{generate[key]=priorFn[key]}catch{}try{rt.generate=generate;if(rt.generate===generate)return true}catch{}try{globalThis.CivweaveModelRuntime={...rt,generate};return globalThis.CivweaveModelRuntime?.generate===generate}catch{return false}}
function isAIProvider(provider){return !NON_AI_PROVIDERS.has(clean(provider,100).toLowerCase())}
async function runLearningJourney(args={}){const unified=globalThis.CivweaveUnifiedChatSystemV1;if(typeof unified?.runLivingSchoolCurriculum!=='function')throw new Error('Moss Learning Journey planning is unavailable.');clearPendingProject();return unified.runLivingSchoolCurriculum({...args,systemId:'living-school',sourceSystemId:'civweave',sourceGuide:'Weaveling'})}
async function createModelPlan(args,api){const planner=globalThis.CivweaveIntentionPlanner,rt=globalThis.CivweaveModelRuntime,selected=selectedLocal(),baseCfg=api.selectedConfig?.()||rt?.readSharedConfig?.('interactive')||{},baseProvider=clean(baseCfg.provider||baseCfg.route,100).toLowerCase(),useLocal=Boolean(selected?.active&&selected.id)||LOCAL_AI_PROVIDERS.has(baseProvider);if(!planner?.persist||!rt?.generate)throw new Error('The structured Quest runtime is unavailable.');if(!useLocal&&!isAIProvider(baseProvider))throw new Error('Quest creation requires an AI model; the current route is deterministic.');const sourceText=resolvedPlanningText(args.text,args.history),context=await(api.context?.(sourceText,'civweave',args.history)||Promise.resolve({currentContext:{systemId:'civweave'},routingAnswer:{room:'civweave.quad'}})),mem=memory(args.history,args.text),taskModel=useLocal?await preferredTaskModel(baseCfg,selected):clean(baseCfg.model,240),config=useLocal?{...baseCfg,provider:'downloaded-local',route:'downloaded-local',model:taskModel||selected?.id||baseCfg.model||'downloaded-local',timeoutMs:600000,maxTokens:2200,stream:false}:{...baseCfg,maxTokens:Math.max(2200,Number(baseCfg.maxTokens)||0),stream:false},request={purpose:'civweave-weaveling-intention-json-v190',executionProfile:'interactive',config,context:{...context,workingMemory:mem},messages:prompt(mem),schema:PLAN_SCHEMA,maxRepairAttempts:1,...(useLocal?{transport:localStructuredTransport('quest'),__civweaveSkipResponseRouter:true,__civweaveLocalStructuredPlan:true}: {})},result=await rt.generate(request);if(result?.status!=='success'||result?.structured?.valid===false||!result?.outputJson||typeof result.outputJson!=='object')throw new Error(result?.error?.message||`AI Quest generation ended with ${result?.status||'an invalid response'}.`);const provider=clean(result.actual?.provider||config.provider||config.route,120).toLowerCase(),model=clean(result.actual?.model||config.model,240);if(!isAIProvider(provider))throw new Error(`Quest authoring returned non-AI provider '${provider||'unknown'}'.`);const plan=normalizePlan(result.outputJson,{text:sourceText,history:args.history,context,provider,model,latestRequest:args.latestRequest||args.text}),item=planner.persist(plan);try{globalThis.CivweaveWeavelingPlanMaterializationV265?.materialize?.(item.plan||plan,{source:'weaveling-ai-structured-model-v190'})}catch{}return planResponse(item,provider,model)}
function install(){installLocalStructuredPlanBridge();const api=globalThis.CivweaveAssistantV141;if(!api?.respond)return false;if(hasResponseLayer(api.respond,'__weavelingPlanJsonV190'))return true;const priorFn=api.respond,prior=priorFn.bind(api),wrapped=async args=>{const text=clean(args?.text),system=args?.systemId||'civweave';if(system==='civweave'){const action=control(text);if(action){const handled=controlResponse(action);if(handled)return handled}const history=args?.history||[];if(learningJourneyIntent(text)){try{return await runLearningJourney(args)}catch(error){return{response:{answer:`Moss could not start the Learning Journey plan: ${clean(error?.message||error,1000)} No learning content was generated or queued.`,choice:{mode:'Learn',system:'living-school',room:'',nextAction:'Retry after the Learning Journey runtime is ready.'},assumptions:[],requiresConsent:false,confidence:1},provider:'living-school-learning-plan-unavailable',model:TASK_DEEP_MODEL,plan:null}}}let pending=readPendingProject();if(pending&&!pendingMatchesHistory(pending,history)){clearPendingProject();pending=null}if(pending&&PENDING_CANCEL.test(text)){clearPendingProject();return prior(args)}if(pending&&substantiveQualifierReply(text)){const cfg=api.selectedConfig?.()||globalThis.CivweaveModelRuntime?.readSharedConfig?.('interactive')||{},combined=combinedPendingText(pending,text);try{const result=await createModelPlan({...args,text:combined,latestRequest:text},api);clearPendingProject();return result}catch(error){return generationFailure(error,{...cfg,model:clean(cfg.model||TASK_DEEP_MODEL,240)})}}const pre=await(api.context?.(text,system,history)||Promise.resolve({currentContext:{systemId:system},routingAnswer:{room:'civweave.quad'}}));if(shouldQualifyProject(text)){savePendingProject(text);return prior(args)}if(planIntent(text,history,pre)){const cfg=api.selectedConfig?.()||globalThis.CivweaveModelRuntime?.readSharedConfig?.('interactive')||{};try{return await createModelPlan({...args,latestRequest:text},api)}catch(error){return generationFailure(error,cfg)}}}return prior(args)};wrapped.__weavelingPlanJsonV190=true;wrapped.__weavelingAIQuestRequiredV1=true;wrapped.__cwMossLearningGoalPlannerV1=true;wrapped.__prior=priorFn;for(const key of ['__cwUnifiedChatSystemV1','__guideIdentityIntegrityV216','__deterministicModeV175'])if(priorFn[key])wrapped[key]=priorFn[key];api.respond=wrapped;api.weavelingPlanSchema=PLAN_SCHEMA;api.weavelingPlanRevision=VERSION;api.weavelingQuestAIRequired=true;return true}
function reassert(){installLocalStructuredPlanBridge();install()}
if(typeof addEventListener==='function')for(const name of ['civweave:assistant-runtime-ready','civweave:guide-loader-reset','civweave:local-model-runtime-ready','civweave:gemma4-litert-fast-runtime-ready','civweave:guide-chat-opened','pageshow'])addEventListener(name,()=>queueMicrotask(reassert));
globalThis.CivweaveWeavelingPlanJsonV190={version:VERSION,install,schema:PLAN_SCHEMA,memory,prompt,normalizePlan,control,latestItem,stripGreeting,learningJourneyIntent,runLearningJourney,explicitProjectIntent,planIntent,contextualPlanIntent,resolvedPlanningText,selectedLocal,readPendingProject,savePendingProject,clearPendingProject,shouldQualifyProject,substantiveQualifierReply,preferredTaskModel,taskDeepModel:TASK_DEEP_MODEL,localStructuredTransport,installLocalStructuredPlanBridge,createModelPlan,isAIProvider,aiQuestAuthoringRequired:true,deterministicQuestFallback:false,localStructuredResponseRouterBypass:true,localLearningPlanBridge:true,learningIntentPassover:true,qualificationHandoff:true,taskGenerationModelPolicy:'prefer-gemma4-e4b-when-installed',structuredTaskTimeoutMs:600000};
reassert();
})();