(()=>{
'use strict';
let session=null,saveTimer=null,lastText='';
const editor=()=>document.getElementById('cs-text-editor');
const provenance=()=>globalThis.CivweaveContentProvenanceV1;
const store=()=>globalThis.CivweaveCreatorStoreV1;
const journal=()=>globalThis.CivweaveCreatorCrashJournalV1;
async function digest(text){const bytes=new TextEncoder().encode(text),hash=await crypto.subtle.digest('SHA-256',bytes);return Array.from(new Uint8Array(hash),b=>b.toString(16).padStart(2,'0')).join('')}
function notify(){dispatchEvent(new CustomEvent('creator-suite:session-updated',{detail:{kind:'text',session}}))}
async function persist(){if(session)await store()?.putSession(session)}
async function journalText(text,contentDigest){if(session&&journal())await journal().checkpoint({session,kind:'text',content:text,contentDigest:`sha256:${contentDigest}`,recoverable:true})}
function newSession(){session=provenance().createSession({mediaType:'text',artifactType:'document',sourceSystem:'creator-suite'});lastText='';if(editor())editor().textContent='';persist();notify();return session}
async function openSession(next){if(!next||next.mediaType!=='text')return false;session=structuredClone(next);const recovered=await journal()?.restore(session.id);const text=recovered&&!recovered.cleared?String(recovered.content||''):'';editor().innerText=text;lastText=text;notify();return true}
async function sealSession(next){if(!next||next.mediaType!=='text'||!session||next.id!==session.id)throw new Error('Finalized text session does not match the active document.');session=structuredClone(next);await persist();notify();return session}
async function recordHumanSnapshot(type='text.replace'){
  if(!session)newSession();if(session.finalizedAt)throw new Error('This document has a finalized creation receipt. Start a new document to continue editing.');const text=editor()?.innerText||'',contentDigest=await digest(text);if(text===lastText)return session;session=await provenance().recordEvent(session,{type,actor:{kind:'human',id:'local-creator'},payload:{length:text.length,contentDigest:`sha256:${contentDigest}`}});lastText=text;await persist();await journalText(text,contentDigest);notify();return session;
}
function scheduleSnapshot(){clearTimeout(saveTimer);saveTimer=setTimeout(()=>recordHumanSnapshot().catch(console.error),180)}
async function execute(action,args,actor){if(!session)newSession();if(session.finalizedAt)throw new Error('This document has a finalized creation receipt. Start a new document before applying more AI edits.');if(action==='replaceAll'||action==='generate'){const text=String(args.text||args.content||''),contentDigest=await digest(text);editor().innerText=text;session=await provenance().recordEvent(session,{type:'ai.generate',actor,payload:{target:'document',acceptedLength:text.length,outputDigest:`sha256:${contentDigest}`}});lastText=text;await persist();await journalText(text,contentDigest);notify();return{session,textLength:text.length}}if(action==='append'){const text=String(args.text||''),next=(editor().innerText||'')+text,contentDigest=await digest(text),fullDigest=await digest(next);editor().innerText=next;session=await provenance().recordEvent(session,{type:'text.insert',actor,payload:{offset:next.length-text.length,length:text.length,contentDigest:`sha256:${contentDigest}`}});lastText=next;await persist();await journalText(next,fullDigest);notify();return{session,textLength:next.length}}throw new Error(`Unsupported text action: ${action}`)}
function init(){const node=editor();if(!node)return;node.addEventListener('input',scheduleSnapshot);node.addEventListener('paste',async event=>{if(!session)newSession();if(session.finalizedAt)return;const pasted=event.clipboardData?.getData('text/plain')||'';session=await provenance().recordEvent(session,{type:'external.paste',actor:{kind:'external',id:'clipboard'},payload:{length:pasted.length,contentDigest:`sha256:${await digest(pasted)}`}});await persist();notify()});document.querySelector('[data-new-session="text"]')?.addEventListener('click',newSession);addEventListener('creator-suite:open-session',event=>{if(event.detail?.session?.mediaType==='text')openSession(event.detail.session).catch(console.error)});globalThis.CivweaveCreatorToolsV1?.registerEditor('text',{actions:['replaceAll','generate','append'],execute});newSession()}
globalThis.CivweaveCreatorTextV1=Object.freeze({init,newSession,openSession,sealSession,getSession:()=>session,getText:()=>editor()?.innerText||'',recordHumanSnapshot});
})();
