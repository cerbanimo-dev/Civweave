(()=>{
'use strict';
const clean=(value,max=1200)=>String(value??'').trim().slice(0,max);
function describe(summary={}){const human=Number(summary.actorCounts?.human||0),ai=Number(summary.actorCounts?.['civweave-ai']||0),external=Number(summary.actorCounts?.external||0),deterministic=Number(summary.actorCounts?.deterministic||0),origin=String(summary.origin||'unknown');if(origin==='ai-generated'&&human>0)return{canonicalOrigin:origin,label:'Mixed: AI + Human',shortLabel:'AI + Human',tone:'ai',detail:`Tracked history contains ${ai} AI event${ai===1?'':'s'} and ${human} human event${human===1?'':'s'}.`};if(origin==='ai-generated')return{canonicalOrigin:origin,label:'AI Generated',shortLabel:'AI Generated',tone:'ai',detail:'Tracked origin includes Civweave AI generation or transformation.'};if(origin==='human-authored')return{canonicalOrigin:origin,label:'Human Created',shortLabel:'Human',tone:'human',detail:'Tracked creation history contains human-origin operations and no AI/external origin event.'};if(origin==='deterministic-generated')return{canonicalOrigin:origin,label:'Deterministic',shortLabel:'Deterministic',tone:'deterministic',detail:`Generated by non-AI deterministic machinery (${deterministic} tracked event${deterministic===1?'':'s'}).`};return{canonicalOrigin:'unknown',label:'External / Unknown',shortLabel:'Unknown',tone:external>0?'external':'unknown',detail:external>0?'Imported or pasted content is present without trusted creation history.':'Reliable creation origin has not been established.'}}
function describeReview(finding={}){const outcome=clean(finding?.outcome||finding?.reviewOutcome,60);if(outcome==='verified')return{outcome,label:'Provenance Verified',shortLabel:'Verified',tone:'verified',detail:'The available provenance chain supports the recorded claim.'};if(outcome==='verified-with-ai')return{outcome,label:'Verified with AI',shortLabel:'Verified + AI',tone:'ai',detail:'The available provenance chain verifies tracked AI involvement.'};if(outcome==='unknown-origin')return{outcome,label:'Origin Still Unknown',shortLabel:'Unknown',tone:'unknown',detail:'Review did not establish a stronger creation-origin claim.'};if(outcome==='broken-chain')return{outcome,label:'Broken Provenance Chain',shortLabel:'Broken chain',tone:'warning',detail:'Review found a structural or cryptographic break in the available provenance chain.'};if(outcome==='anomalous')return{outcome,label:'Provenance Anomaly',shortLabel:'Anomalous',tone:'warning',detail:'Review found provenance evidence that needs attention; this is not a stylistic AI detector result.'};if(outcome==='needs-human-review'||outcome==='needs-escalation')return{outcome:'needs-human-review',label:'Human Review Needed',shortLabel:'Review needed',tone:'review',detail:'The evidence does not support an automatic provenance conclusion.'};return{outcome:'unreviewed',label:'Not Reviewed',shortLabel:'Unreviewed',tone:'neutral',detail:'No additive provenance review finding is attached.'}}
function describeArtifact({summary={},finding=null}={}){return Object.freeze({originBadge:Object.freeze(describe(summary)),reviewBadge:Object.freeze(describeReview(finding||{})),creationOriginImmutable:true,reviewIsAdditive:true})}
globalThis.CivweaveCreatorProvenanceLabelV1=Object.freeze({describe,describeReview,describeArtifact});
})();
